# History.md - History Archive (deep rows)

NOT read by default - consult only when digging into old changes.
Moved here verbatim by project-os/rotate-history.ps1. Movement only: rows are never rewritten, compressed, or deleted.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-04 | Plan v1 | `PLAN.md` written and revised through Claude, Gemini and GPT reviews; `.gitignore`, `.gitattributes`; git initialized on main. | Read back after each review round; every verdict logged in `PLAN.md` section 12. No code exists, so nothing ran. | Pass | low | none, first commit `a244ab1` | delete `PLAN.md`; nothing depends on it |
| 2026-09-04 | Install ProjectOS | Kit copied in; placeholders replaced; description, six invariants, worst bug classes, commit policy, reply dial and rule 22 set from Rotem's answers; MCP folders and `Installation.md` removed; hooks reworded for Rogers and installed to `.claude/settings.local.json`. | Hook command run from the settings file in bash and PowerShell: the standing-rules text came back in both. Both rotation scripts dry-run: paths resolved, nothing to move. No placeholder left; every `project-os/*.md` link resolves. Self-review of the kit edits: clashes listed in the install report, none overridden. | Pass | medium | `a244ab1` | `git checkout a244ab1 -- .`, delete `CLAUDE.md`, `project-os/`, `.claude/settings.local.json` |
| 2026-09-04 | Apply install verdicts | Conversations rule 12 exception for mockups; CLAUDE.md rule 13 exception for `PLAN.md`; CLAUDE.md rule 23, model and effort at pickup; PLAN.md dev-server wording in three spots; hooks text reworded and reinstalled with replace; private memory trimmed to one pointer. | Hook command run from the settings file: the new line 4 came back. The three edited rules read back. No placeholder or dangling link introduced. | Pass | medium | `a244ab1` | `git checkout a244ab1 -- .` for the tracked files; re-run the hook installer with the previous text |
| 2026-09-04 | Plan 0.4, remote and push | `git remote add origin`, `git push -u origin main`; PLAN.md 0.4 ticked. | `git ls-remote` reached the empty repo; push reported new branch main tracking origin/main. | Pass | low | `a244ab1` | `git remote remove origin`; delete the branch on GitHub |
| 2026-09-04 | Push policy correction | CLAUDE.md Go commit step 7; PLAN.md steps 1.5, 4.10, 5.5, 6.2 and the execution rules; hook line 9 reworded and reinstalled; Mistakes.md promoted row. | Hook command run from the settings file: line 9 came back with the new text. grep for "push" in PLAN.md read back. | Pass | medium | `ad4b412` | `git checkout ad4b412 -- CLAUDE.md PLAN.md project-os`; re-run the hook installer with replace |
| 2026-09-04 | Plan 0.2, wrangler login | Rotem ran `npx wrangler login`; PLAN.md 0.2 ticked. | `npx wrangler whoami` printed the OAuth login for rotem914@gmail.com and the account id. Subdomain on workers.dev not checked yet; it is checked at 6.1. | Pass | low | `ad4b412` | `npx wrangler logout` |
| 2026-09-04 | Plan 1.1, scaffold | Cloudflare `vite-react-template` scaffolded in `.tmp/`, then moved to the root: `src/worker/index.ts` (Hono), `src/react-app/` (React), `vite.config.ts`, `wrangler.json`, four tsconfigs, eslint. Renamed to `rogers` in package.json and wrangler.json; template registry block dropped; the template's own README and .gitignore not copied; `*.tsbuildinfo` added to .gitignore. | `npm install` clean after the move. `npm run check` ran green: `tsc` clean, `vite build` built `dist/client` in 448 ms, `wrangler deploy --dry-run` read the config as rogers and uploaded nothing. Browser: Rotem started the dev server, I drove his page at localhost:5173. It renders, the API button fetched `/api/` and the label became Name from API is: Cloudflare, so React reaches the Worker. Console clean: only Vite connect and the React DevTools notice. | Pass | low | `ad4b412` | delete the scaffold files listed above; `git checkout ad4b412 -- .gitignore` |
| 2026-09-04 | Plan 1.2, restructure | `src/react-app` to `src/web`, `src/worker` to `src/api`, new `src/shared/types.ts` with the Health type the Worker returns. tsconfig.app and tsconfig.worker includes rewritten, `index.html` script path, `wrangler.json` main path plus `assets.run_worker_first: ['/api/*']`. Added `/api/health`; the template's `/api/` demo route stays until 1.3. | `npm run check` green: `tsc` clean, `vite build` in 350 ms, `wrangler deploy --dry-run` read the new main path. After Rotem restarted his dev server, on localhost:5173: `/api/health` returned 200 `{"ok":true,"service":"rogers"}` as application/json; `/p/some-project` returned the app shell HTML, so deep links load; `/api/unknown` returned Hono's 404 as text/plain, so `/api/*` really reaches the Worker instead of falling through to the SPA. Page renders in a fresh tab with a clean console. Review (rule 17, medium): 2 findings. Introduced and fixed: `worker-configuration.d.ts` still imported `./src/worker/index`, regenerated with `npm run cf-typegen`, and it was silent because skipLibCheck hides errors inside a .d.ts. Pre-existing, flagged not fixed: the `check` script's first stage is plain `tsc`, which compiles nothing on a solution config with `files: []`, proven with `tsc --listFilesOnly`; the real typecheck is `tsc -b`, which passes. Blast radius: shared, the consumers are index.html, three tsconfigs, wrangler.json and the generated types, all traced and rechecked. Map.md updated in the same change. | Pass | medium | `ad4b412` | rename the two folders back, `git checkout ad4b412 -- .` for the configs |
| 2026-09-04 | Fix the check command | `package.json` check script: `tsc` to `tsc -b`. Plain `tsc` on a solution config with `files: []` compiles nothing, so the stage was a no-op. | Injected `const broken: number = "not a number"` into `src/shared/types.ts`: the check failed with TS2322, naming file and line. Restored the file, check green again. | Pass | low | `ad4b412` | revert the one word in package.json |
| 2026-09-04 | Plan 1.3, dark foundation | New `src/web/styles/tokens.css`, the whole palette as Tailwind v4 theme tokens. `src/web/index.css` rewritten: Tailwind import, `color-scheme: dark`, body ground, a bidi helper for mixed Hebrew and English, one focus ring. `src/web/App.tsx` replaced by the shell, a sticky 56px top bar over a centred content column. `index.html`: title Rogers, color-scheme and theme-color meta, an inline dark background before any stylesheet. Tailwind plugin added to `vite.config.ts`. Deleted with the template page: `App.css`, `src/web/assets/*`, and the demo `/api/` route. | `npm run check` green. In the browser, computed values read off the live page rather than a screenshot: colorScheme dark, html and body `rgb(23,24,26)`, text `rgb(230,231,233)`, muted `rgb(154,160,166)`, header 56px and sticky with the border token, content column 672px. Title Rogers. Narrow width 375, resized then reloaded: no horizontal overflow, bar still 56px. Console clean. No-flash guard confirmed present in the built `dist/client/index.html`, above the stylesheet link. Review (rule 17, medium): 2 findings, both mine, both fixed. `--color-faint` measured 3.55 on the page and 3.14 on a card, under the 4.5 needed for small label text, so it moved to `#888d92` at 5.31 and 4.69; the other tokens measure 14.36, 6.73, 11.02 and 7.44. The two hex values in `index.html` duplicate `--color-bg` and cannot use the token, so a comment now names the coupling. Blast radius local: only main.tsx imports the stylesheet, and nothing referenced the deleted files. | Pass | medium | `ad4b412` | `git checkout ad4b412 -- .` and restore the template files from git |
| 2026-09-04 | Plan 1.4, routes and API client | New `src/web/platform/platform.ts` (web or Tauri, API base address, path or hash routing), `session.ts` (draft parking in sessionStorage, expiry registry, one-shot reload), `api-client.ts` (`apiFetch` with `redirect: "manual"`, `isSessionExpired`, `ApiError`), `src/web/lib/useApi.ts` (loading, error, refetch, with a run counter so a slow first response cannot overwrite a newer one), `src/web/components/TopBar.tsx`, three pages under `src/web/pages/`. `App.tsx` became the router; react-router 7 added. | `npm run check` green. On the live page: Home reads `/api/health` through the client and shows the answer, proving screen to hook to client to Worker. `/n/abc123` loaded directly renders and survives a hard reload with its address intact; `/p/kitchen-reno` and an unknown path both return the app shell, and the unknown one renders the not-found screen. Back links navigate client-side, proven with a window marker that a reload would have wiped. Session expiry, six cases through `isSessionExpired`: real JSON false, HTML under a 200 true, 302 true, 401 true, 403 true, and a genuine JSON 500 false so a server bug never reads as a logout. Full rescue: a registered editor parked mixed Hebrew and English text, the page reloaded, the text came back intact, a second read returned null and no keys were left behind. Console clean. Review (rule 17, medium): 1 finding, mine, fixed, the not-found link was a plain anchor forcing a full reload and is now a router link. Blast radius local: only `main.tsx` consumes `App`. Map.md updated in the same change. | Pass | medium | `ad4b412` | delete `src/web/platform`, `lib`, `pages`, `components`, restore `App.tsx` from git, uninstall react-router |
| 2026-09-04 | Plan 1.5, commit phase 1 | Staged 27 new files and 7 changed ones: the scaffold, the dark foundation, the three screens, the platform folder, and the process docs. Committed as `b4d67e1` on main. | Both rotation scripts run first, nothing to move. `npm run check` green before staging. Read the staged list file by file: no env file, no key, nothing generated; `dist`, `node_modules`, `.wrangler` and `.tmp` stayed ignored. Working tree clean afterwards. Not pushed: that is Rotem's, per the Go commit flow. | Pass | low | `ad4b412` | `git reset --soft ad4b412` while it is unpushed |
| 2026-09-04 | Plan 2.1, create D1 | `npx wrangler d1 create rogers-db`, created in region EEUR, id `32967c7a-1e86-4ef2-9094-a7c0a08984ac`. Added the `d1_databases` block to `wrangler.json` bound as `DB`, the Cloudflare convention, rather than the `rogers_db` wrangler suggested. Regenerated `worker-configuration.d.ts`. | `wrangler d1 list` shows the database, production, 0 tables. `npm run check` green and the dry-run deploy now prints `env.DB (rogers-db) D1 Database`, so the binding is really read from the config. Generated types show `DB: D1Database` on Env. | Pass | low | `254ad98` | `npx wrangler d1 delete rogers-db` and drop the block from wrangler.json |
| 2026-09-04 | Plan 2.2, first migration | New `migrations/0001_init.sql`: `projects` and `notes` per PLAN.md section 4, including `pinned_at`, `archived_at` on both, `images` as a JSON array, and the `notes_by_project` index. Added `db:migrate:local` and `db:migrate:remote` scripts. | Applied locally, 4 commands, migration reported applied. Both tables and the index listed from `sqlite_master`. Behaviour proven with five throwaway rows rather than assumed: the project page query returned pinned later, pinned earlier, unpinned newer, unpinned older, and the archived row did not appear, which is the pinned-first Keep order. `EXPLAIN QUERY PLAN` shows `SEARCH notes USING INDEX notes_by_project` with no separate sort step. The foreign key is enforced: a note pointing at a missing project was rejected. Test rows deleted; both tables back to 0. `npm run check` green. Review (rule 17, high, schema): no findings; the migration only creates, so the additive invariant holds trivially, and `.wrangler` stays gitignored so no local database can be committed. | Pass | high | `254ad98` | drop both tables and delete the migration; nothing depends on them yet |
