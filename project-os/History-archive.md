# History.md - History Archive (deep rows)

NOT read by default - consult only when digging into old changes.
Moved here verbatim by project-os/rotate-history.ps1. Movement only: rows are never rewritten, compressed, or deleted.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-04 | Plan v1 | `PLAN.md` written and revised through Claude, Gemini and GPT reviews; `.gitignore`, `.gitattributes`; git initialized on main. | Read back after each review round; every verdict logged in `PLAN.md` section 12. No code exists, so nothing ran. | Pass | low | none, first commit `a244ab1` | delete `PLAN.md`; nothing depends on it |
| 2026-09-04 | Install ProjectOS | Kit copied in; placeholders replaced; description, six invariants, worst bug classes, commit policy, reply dial and rule 22 set from Rotem's answers; MCP folders and `Installation.md` removed; hooks reworded for Rogers and installed to `.claude/settings.local.json`. | Hook command run from the settings file in bash and PowerShell: the standing-rules text came back in both. Both rotation scripts dry-run: paths resolved, nothing to move. No placeholder left; every `project-os/*.md` link resolves. Self-review of the kit edits: clashes listed in the install report, none overridden. | Pass | medium | `a244ab1` | `git checkout a244ab1 -- .`, delete `CLAUDE.md`, `project-os/`, `.claude/settings.local.json` |
| 2026-09-04 | Apply install verdicts | Conversations rule 12 exception for mockups; CLAUDE.md rule 13 exception for `PLAN.md`; CLAUDE.md rule 23, model and effort at pickup; PLAN.md dev-server wording in three spots; hooks text reworded and reinstalled with replace; private memory trimmed to one pointer. | Hook command run from the settings file: the new line 4 came back. The three edited rules read back. No placeholder or dangling link introduced. | Pass | medium | `a244ab1` | `git checkout a244ab1 -- .` for the tracked files; re-run the hook installer with the previous text |
| 2026-09-04 | Plan 0.4, remote and push | `git remote add origin`, `git push -u origin main`; PLAN.md 0.4 ticked. | `git ls-remote` reached the empty repo; push reported new branch main tracking origin/main. | Pass | low | `a244ab1` | `git remote remove origin`; delete the branch on GitHub |
| 2026-09-04 | Push policy correction | CLAUDE.md Go commit step 7; PLAN.md steps 1.5, 4.10, 5.5, 6.2 and the execution rules; hook line 9 reworded and reinstalled; Mistakes.md promoted row. | Hook command run from the settings file: line 9 came back with the new text. grep for "push" in PLAN.md read back. | Pass | medium | `ad4b412` | `git checkout ad4b412 -- CLAUDE.md PLAN.md project-os`; re-run the hook installer with replace |
| 2026-09-04 | Plan 0.2, wrangler login | Rotem ran `npx wrangler login`; PLAN.md 0.2 ticked. | `npx wrangler whoami` printed the OAuth login for rotem914@gmail.com and the account id. Subdomain on workers.dev not checked yet; it is checked at 6.1. | Pass | low | `ad4b412` | `npx wrangler logout` |
| 2026-09-04 | Plan 1.1, scaffold | Cloudflare `vite-react-template` scaffolded in `.tmp/`, then moved to the root: `src/worker/index.ts` (Hono), `src/react-app/` (React), `vite.config.ts`, `wrangler.json`, four tsconfigs, eslint. Renamed to `rogers` in package.json and wrangler.json; template registry block dropped; the template's own README and .gitignore not copied; `*.tsbuildinfo` added to .gitignore. | `npm install` clean after the move. `npm run check` ran green: `tsc` clean, `vite build` built `dist/client` in 448 ms, `wrangler deploy --dry-run` read the config as rogers and uploaded nothing. Browser: Rotem started the dev server, I drove his page at localhost:5173. It renders, the API button fetched `/api/` and the label became Name from API is: Cloudflare, so React reaches the Worker. Console clean: only Vite connect and the React DevTools notice. | Pass | low | `ad4b412` | delete the scaffold files listed above; `git checkout ad4b412 -- .gitignore` |
| 2026-09-04 | Plan 1.2, restructure | `src/react-app` to `src/web`, `src/worker` to `src/api`, new `src/shared/types.ts` with the Health type the Worker returns. tsconfig.app and tsconfig.worker includes rewritten, `index.html` script path, `wrangler.json` main path plus `assets.run_worker_first: ['/api/*']`. Added `/api/health`; the template's `/api/` demo route stays until 1.3. | `npm run check` green: `tsc` clean, `vite build` in 350 ms, `wrangler deploy --dry-run` read the new main path. After Rotem restarted his dev server, on localhost:5173: `/api/health` returned 200 `{"ok":true,"service":"rogers"}` as application/json; `/p/some-project` returned the app shell HTML, so deep links load; `/api/unknown` returned Hono's 404 as text/plain, so `/api/*` really reaches the Worker instead of falling through to the SPA. Page renders in a fresh tab with a clean console. Review (rule 17, medium): 2 findings. Introduced and fixed: `worker-configuration.d.ts` still imported `./src/worker/index`, regenerated with `npm run cf-typegen`, and it was silent because skipLibCheck hides errors inside a .d.ts. Pre-existing, flagged not fixed: the `check` script's first stage is plain `tsc`, which compiles nothing on a solution config with `files: []`, proven with `tsc --listFilesOnly`; the real typecheck is `tsc -b`, which passes. Blast radius: shared, the consumers are index.html, three tsconfigs, wrangler.json and the generated types, all traced and rechecked. Map.md updated in the same change. | Pass | medium | `ad4b412` | rename the two folders back, `git checkout ad4b412 -- .` for the configs |
| 2026-09-04 | Fix the check command | `package.json` check script: `tsc` to `tsc -b`. Plain `tsc` on a solution config with `files: []` compiles nothing, so the stage was a no-op. | Injected `const broken: number = "not a number"` into `src/shared/types.ts`: the check failed with TS2322, naming file and line. Restored the file, check green again. | Pass | low | `ad4b412` | revert the one word in package.json |
| 2026-09-04 | Plan 1.3, dark foundation | New `src/web/styles/tokens.css`, the whole palette as Tailwind v4 theme tokens. `src/web/index.css` rewritten: Tailwind import, `color-scheme: dark`, body ground, a bidi helper for mixed Hebrew and English, one focus ring. `src/web/App.tsx` replaced by the shell, a sticky 56px top bar over a centred content column. `index.html`: title Rogers, color-scheme and theme-color meta, an inline dark background before any stylesheet. Tailwind plugin added to `vite.config.ts`. Deleted with the template page: `App.css`, `src/web/assets/*`, and the demo `/api/` route. | `npm run check` green. In the browser, computed values read off the live page rather than a screenshot: colorScheme dark, html and body `rgb(23,24,26)`, text `rgb(230,231,233)`, muted `rgb(154,160,166)`, header 56px and sticky with the border token, content column 672px. Title Rogers. Narrow width 375, resized then reloaded: no horizontal overflow, bar still 56px. Console clean. No-flash guard confirmed present in the built `dist/client/index.html`, above the stylesheet link. Review (rule 17, medium): 2 findings, both mine, both fixed. `--color-faint` measured 3.55 on the page and 3.14 on a card, under the 4.5 needed for small label text, so it moved to `#888d92` at 5.31 and 4.69; the other tokens measure 14.36, 6.73, 11.02 and 7.44. The two hex values in `index.html` duplicate `--color-bg` and cannot use the token, so a comment now names the coupling. Blast radius local: only main.tsx imports the stylesheet, and nothing referenced the deleted files. | Pass | medium | `ad4b412` | `git checkout ad4b412 -- .` and restore the template files from git |
| 2026-09-04 | Plan 1.4, routes and API client | New `src/web/platform/platform.ts` (web or Tauri, API base address, path or hash routing), `session.ts` (draft parking in sessionStorage, expiry registry, one-shot reload), `api-client.ts` (`apiFetch` with `redirect: "manual"`, `isSessionExpired`, `ApiError`), `src/web/lib/useApi.ts` (loading, error, refetch, with a run counter so a slow first response cannot overwrite a newer one), `src/web/components/TopBar.tsx`, three pages under `src/web/pages/`. `App.tsx` became the router; react-router 7 added. | `npm run check` green. On the live page: Home reads `/api/health` through the client and shows the answer, proving screen to hook to client to Worker. `/n/abc123` loaded directly renders and survives a hard reload with its address intact; `/p/kitchen-reno` and an unknown path both return the app shell, and the unknown one renders the not-found screen. Back links navigate client-side, proven with a window marker that a reload would have wiped. Session expiry, six cases through `isSessionExpired`: real JSON false, HTML under a 200 true, 302 true, 401 true, 403 true, and a genuine JSON 500 false so a server bug never reads as a logout. Full rescue: a registered editor parked mixed Hebrew and English text, the page reloaded, the text came back intact, a second read returned null and no keys were left behind. Console clean. Review (rule 17, medium): 1 finding, mine, fixed, the not-found link was a plain anchor forcing a full reload and is now a router link. Blast radius local: only `main.tsx` consumes `App`. Map.md updated in the same change. | Pass | medium | `ad4b412` | delete `src/web/platform`, `lib`, `pages`, `components`, restore `App.tsx` from git, uninstall react-router |
| 2026-09-04 | Plan 1.5, commit phase 1 | Staged 27 new files and 7 changed ones: the scaffold, the dark foundation, the three screens, the platform folder, and the process docs. Committed as `b4d67e1` on main. | Both rotation scripts run first, nothing to move. `npm run check` green before staging. Read the staged list file by file: no env file, no key, nothing generated; `dist`, `node_modules`, `.wrangler` and `.tmp` stayed ignored. Working tree clean afterwards. Not pushed: that is Rotem's, per the Go commit flow. | Pass | low | `ad4b412` | `git reset --soft ad4b412` while it is unpushed |
| 2026-09-04 | Plan 2.1, create D1 | `npx wrangler d1 create rogers-db`, created in region EEUR, id `32967c7a-1e86-4ef2-9094-a7c0a08984ac`. Added the `d1_databases` block to `wrangler.json` bound as `DB`, the Cloudflare convention, rather than the `rogers_db` wrangler suggested. Regenerated `worker-configuration.d.ts`. | `wrangler d1 list` shows the database, production, 0 tables. `npm run check` green and the dry-run deploy now prints `env.DB (rogers-db) D1 Database`, so the binding is really read from the config. Generated types show `DB: D1Database` on Env. | Pass | low | `254ad98` | `npx wrangler d1 delete rogers-db` and drop the block from wrangler.json |
| 2026-09-04 | Plan 2.2, first migration | New `migrations/0001_init.sql`: `projects` and `notes` per PLAN.md section 4, including `pinned_at`, `archived_at` on both, `images` as a JSON array, and the `notes_by_project` index. Added `db:migrate:local` and `db:migrate:remote` scripts. | Applied locally, 4 commands, migration reported applied. Both tables and the index listed from `sqlite_master`. Behaviour proven with five throwaway rows rather than assumed: the project page query returned pinned later, pinned earlier, unpinned newer, unpinned older, and the archived row did not appear, which is the pinned-first Keep order. `EXPLAIN QUERY PLAN` shows `SEARCH notes USING INDEX notes_by_project` with no separate sort step. The foreign key is enforced: a note pointing at a missing project was rejected. Test rows deleted; both tables back to 0. `npm run check` green. Review (rule 17, high, schema): no findings; the migration only creates, so the additive invariant holds trivially, and `.wrangler` stays gitignored so no local database can be committed. | Pass | high | `254ad98` | drop both tables and delete the migration; nothing depends on them yet |
| 2026-09-04 | Plan 2.3, migrate the live database | `npm run db:migrate:remote` applied `0001_init.sql` to `rogers-db` on Cloudflare, 4 commands. | Checked before touching it: the remote held only Cloudflare's own `_cf_KV`, so no data was at risk, and exactly one migration was pending. After: `notes_by_project`, `notes`, `projects` and `d1_migrations` present; column lists read back from `pragma_table_info` and match the local database exactly, `id,project_id,title,body,images,pinned_at,created_at,updated_at,archived_at` and `id,name,color,created_at,updated_at,archived_at`. Both tables 0 rows. `wrangler d1 migrations list --remote` now reports nothing to apply. Review (rule 17, high, live schema): no findings; the migration only creates tables, so nothing existing could be dropped or rewritten. | Pass | high | `254ad98` | drop both tables on the remote and clear the `d1_migrations` row; nothing reads them yet |
| 2026-09-04 | Plan 2.4, shared types | `src/shared/types.ts` rewritten: `Project`, `Note`, `NotePreview` for reading, `CreateProjectBody`, `UpdateProjectBody`, `CreateNoteBody`, `UpdateNoteBody` for writing, plus `UploadResult` and `ApiErrorBody`. The wire shape is deliberately not the row shape: camelCase, no `archived_at`, `images` as an array, and `pinnedAt` when reading against `pinned` when writing. Regenerated `worker-configuration.d.ts`. | `npm run check` green, `Env` carries `DB: D1Database`. Contract cross-checked by script rather than by eye: it read the live column lists from the database and the field lists from the types, and reported every wire field backed by a column, every column accounted for apart from `archived_at` by design, and no archived field on any read type. Review (rule 17, low, but it defines the API contract): no findings. Decisions entry added for the wire-versus-row split. | Pass | low | `254ad98` | `git checkout 254ad98 -- src/shared/types.ts` and rerun cf-typegen |
| 2026-09-04 | Plan 3.1, project endpoints | New `src/api/rows.ts`, the row types and the only row-to-wire mappers, with image parsing that never throws. New `src/api/projects.ts`, `GET /api/projects` (live only, note count as a subquery, oldest first) and `POST /api/projects` (browser id honoured, name trimmed, Worker writes both timestamps). `src/api/index.ts` mounts them and adds `onError` and `notFound`. | Against the running server: empty list `[]`; create returned 201 with the full project; a browser-supplied id was honoured; `"  Car  "` was stored trimmed; the list returned both oldest first. Five bad bodies, missing name, blank name, a number, junk text and no body at all, each returned 400 in the error shape. Note count proven with rows inserted straight into the database: two live and one archived gave 2, not 3. All test rows deleted; local list back to `[]` and the live database confirmed still 0 and 0. `npm run check` green. Review (rule 17, medium): 1 finding, mine, fixed. A duplicate id answered a plain-text 500, and the client reads any non-JSON answer under `/api` as an expired Access session, so a server error would have signed Rotem out instead of showing an error. `onError` and `notFound` now answer JSON, proven through the real client: 400, 404 and 500 all read as failures rather than a logout, and `apiFetch` threw `ApiError` carrying the Worker's own message. Added as BugAtlas row 1. | Pass | medium | `fc31e43` | delete `src/api/projects.ts` and `rows.ts`, restore `index.ts` from git |
| 2026-09-04 | Plan 3.2, update and archive projects | `src/api/projects.ts`: `PATCH /api/projects/:id` writing only the fields the request carries, and `DELETE /api/projects/:id` setting `archived_at` instead of removing the row. Column list extracted so every project answer has one shape. | Fifteen cases against the running server. Update: set a colour, then rename alone and the colour survived, which is the case a blanket UPDATE would have destroyed; an explicit null cleared the colour; an empty body was a no-op returning the project; blank name 400; a number as colour 400; a missing project 404. Archive: the project left the live list, the row stayed in the database with `archived_at` set, a second archive answered calmly rather than failing, patching an archived project 404, archiving a missing one 404. Test rows deleted, list back to `[]`. `npm run check` green. Review (rule 17, high, it touches the never-hard-delete invariant): no findings; the invariant is upheld and proven by reading the row back after archiving. | Pass | high | `fc31e43` | `git checkout fc31e43 -- src/api/projects.ts` |
| 2026-09-04 | Working mode: run continuously | `CLAUDE.md` rule 22 rewritten: work each plan step in order and continue without waiting, stopping only for a real problem, for a step that is Rotem's, or before an action that reaches the public, with the deploy at 6.1 named as the clear case. Mistakes row added. | Read the rule back; the one-action-at-a-time part is unchanged, only the approval gate moved. | Pass | low | `fc31e43` | `git checkout fc31e43 -- CLAUDE.md` |
| 2026-09-04 | Plan 3.3, note list and create | New `src/api/notes.ts`: `GET` and `POST` on `/api/projects/:projectId/notes`, mounted in `index.ts`. List filters archived notes, orders `pinned_at DESC, created_at DESC` and clips the body in SQL using the `PREVIEW_LENGTH` now exported from `rows.ts`, so a project of long notes does not haul every word out of the database. Create accepts an optional browser id and allows an empty note, which the composer discards later. | Against the running server: empty list `[]`; create with a title, with a body only, and entirely empty, all 201; list returned all three; a missing project 404 on both routes, which also proved the id in the mount path reaches the router. Pinned ordering through the real endpoint: a pinned note rose to the top of the list. Clipping: a 400 character body came back as a 300 character preview. Text: Hebrew sent from the browser stored and read back byte for byte, `שלום עולם mixed English 123`, title and body both intact. Test rows deleted, list back to `[]`. `npm run check` green after fixing two type errors, the mount-path parameter is not provably present so both handlers now treat a missing id as no such project. Review (rule 17, medium): 1 finding, in my own checking rather than the code. Hebrew sent through curl on this machine arrives as `3F3F`, question marks, because the shell strips it before the request leaves, which reads exactly like an encoding bug in the app. Recorded as QA.md section 10b: non-ASCII is checked from the browser, never through a shell command. | Pass | medium | `fc31e43` | delete `src/api/notes.ts` and its two mount lines |
| 2026-09-04 | Plan 3.4, single note endpoints | `src/api/notes.ts` gained the `notes` router mounted at `/api/notes`: `GET /:id` full note, `PATCH /:id` writing only the fields present, `DELETE /:id` setting `archived_at`. Pinning writes a timestamp, unpinning writes null, and pinning an already pinned note leaves its timestamp alone so a repeat click cannot reorder the pinned section. Images are replaced as a whole ordered list. | Twenty-one cases, all from the browser rather than the shell, per QA section 10b. The invariant first: saving the title alone left the body untouched, saving the body alone left the title untouched, and an explicit empty string still cleared a field on purpose. Pin set a timestamp, a second pin kept the original time, unpin cleared it. Images stored in order and survived a later title-only save. Four bad payloads, a number as title, a mixed images array, a string as pinned, and an empty body, answered 400 or a no-op 200 as designed. Archive: the note left the list, reading and saving it then answered 404, a second archive answered calmly, a missing note 404. Concurrency, the case the invariant exists for: a title save and a body save fired at the same moment on one note, both survived, because each writes only its own column. Hebrew round-tripped intact throughout. Test rows deleted. `npm run check` green. Review (rule 17, high, autosave and the never-lose-text invariant): no findings. | Pass | high | `fc31e43` | remove the `notes` router and its mount line |
| 2026-09-04 | Plan 3.5, input checks | New `src/api/http.ts` holding `readJson`, `errorBody` and `isUsableId`, replacing the copy of the body parser that had appeared in both route files. Creates are now retry safe: `ON CONFLICT (id) DO NOTHING` then read back, so a repeated create answers 200 with the existing row instead of the plain 500 flagged at 3.1. A note id already used in a different project answers 409 rather than handing one project's note to another. Client-supplied ids are checked for length and characters before they reach the database or an address. | From the browser: creating the same project twice returned 201 then 200 with the same id, no crash. Five unusable ids, empty, 65 characters, containing a slash, containing a space, and a number, all 400. A note id reused inside its own project answered 200 with the original note; the same id from another project answered 409. Final sweep across thirteen routes covering every method and both failure kinds: every one answered JSON, and none could be mistaken by the client for an expired login. Test rows deleted. `npm run check` green. Review (rule 17, medium): no new findings; the 3.1 finding is closed. BugAtlas row 1 widened, every answer under `/api` is JSON including successes, and 204 is named as a trap because an empty body carries no content type and reads exactly like the login page. | Pass | medium | `fc31e43` | `git checkout fc31e43 -- src/api` and delete `http.ts` |
| 2026-09-04 | Stale dev server diagnosis | No code change. Diagnosed a blank Home screen reported by the console as `Invalid hook call` and `more than one copy of React`. | Ruled out the code: `npm ls react` showed one deduped copy, the build passed, a hard reload and a clean tab both still failed. Proved the cause by comparing the `?v=` hashes the server was serving, `62638cb3` for react, `61fd4380` for react-dom, `6e5937b5` for react-router, against `browserHash b989cab7` in the Vite metadata: four different optimization runs being served at once. Rotem restarted the server and Home rendered immediately. Recorded as BugAtlas row 2. | Pass | low | `07078a8` | none, nothing changed |
| 2026-09-04 | Plan 4.1, Home tiles | `src/web/pages/Home.tsx` rewritten: a responsive grid of project tiles reading `/api/projects`, and a plus tile that becomes an inline name field. Enter creates, Escape cancels, a failed save keeps the typed name on screen. The id is made in the browser so a retry is recognised as the same project. Tile borders take the project colour when it has one. | Driven in the browser: the plus tile opened a focused field carrying `dir=auto`; Escape cancelled and created nothing; Enter created a Hebrew project and the padded name `  שיפוץ מטבח  ` was stored trimmed. Tiles rendered with note counts, the plus tile last. Layout measured rather than eyeballed: three columns of 237px inside a 768px centred column, each tile 4:3, no horizontal overflow. Rotem's own test project was present and left untouched. `npm run check` green. Review (rule 17, medium): no code findings, but one real blocker found first, see the row above. | Pass | medium | `07078a8` | `git checkout 07078a8 -- src/web/pages/Home.tsx` |
| 2026-09-04 | Plan 4.2, tile menu | New `src/web/components/ProjectTile.tsx`, moved out of Home as it grew. The tile is a div with a covering link and the menu button above it, because a control nested inside a link is neither reliably clickable nor announced properly. Menu offers rename in place, a row of seven colour swatches, and archive. Escape closes and returns focus to the button. | Driven in the browser on my own test project, never on Rotem's. Menu opened with the three items and `aria-expanded` true. Rename saved the Hebrew name and left the colour alone; setting green saved `#81c995`, left the name alone, and the tile border measured `rgb(129, 201, 149)`. Escape closed the menu and focus returned to the menu button. Archive removed the tile from the list. Rotem's own project was untouched throughout. `npm run check` green. Review (rule 17, medium): no findings. | Pass | medium | `07078a8` | delete `ProjectTile.tsx` and `git checkout 07078a8 -- src/web/pages/Home.tsx` |
| 2026-09-04 | Plan 4.3, project page | `src/web/pages/Project.tsx` rewritten: back arrow, the project name renameable in the header, and the note list in a 672px column. New `src/web/components/NoteRow.tsx`, a full-width row with the title and a three-line preview, linking to the note page. The project name comes from the projects list, since there is no endpoint for one project, which also answers what a stale link should do. | Driven in a test project of my own with five notes. Rows rendered newest first, each linking to `/n/:id`; an untitled note showed its text alone and a wholly empty one said so. Header rename saved and Escape reverted without saving. Review (rule 17, medium): 2 findings, both mine, both fixed, both silent. A 500 character unbroken string ran past the edge of its row: `break-words` added. Then the clamp was found dead, because `block` sits with `line-clamp-3` and Tailwind emits `.block` afterwards, so `display:block` beat `display:-webkit-box`; proved by reading both rules' positions out of `document.styleSheets`, indexes 58 and 57. After the fix the preview measures three lines, clips, wraps and stays inside its row at every width. Both are now BugAtlas rows 3 and 4, the second covering a dev-server trap found on the way: Tailwind only regenerates when the CSS entry changes, so a class used for the first time in a component does nothing until `index.css` is touched. | Pass | medium | `07078a8` | `git checkout 07078a8 -- src/web/pages/Project.tsx` and delete `NoteRow.tsx` |
| 2026-09-04 | Plan 4.4 to 4.6, composer and editor | New `src/web/lib/autosave.ts`, the `NoteSaver`: at most one save in flight per note, edits during a save coalesced into one pending patch sent the moment it returns, answers never written back into the editor, failed fields merged back under newer text, flush with `keepalive` for the way out, unsaved text parked in sessionStorage on failure or dispose. New `Composer.tsx`: collapsed bar, expands with the caret in the body, creates the note on the first keystroke with a browser-made id, pin, delete, Close, click outside and Escape close, an empty draft is archived on close. New `AutoGrowTextarea.tsx`. `Note.tsx` rewritten as the editor with saved indicator, pin in the header, flush on blur and back, empty note discarded on back, parked text restored on mount. `TopBar` gained `onBack`. Composer mounted in `Project.tsx`. | All driven in the browser, in my test project, with a fetch spy counting saves. 4.4: opens with the body focused, Close, click outside and Escape all collapse, none of them creates a note. 4.5: a burst of 25 keystrokes became 1 create and 1 save with never more than 1 in flight; the row appeared at the top on Close; open then Close added nothing and sent no create; a draft typed then erased was archived on close. 4.6: with every save's answer held for 1.5 s, text typed while a save was in flight won: 2 saves, max 1 in flight, server holds the newer text, status went Saving then Saved. Pin wrote a timestamp and the row rose to the top. Blur flushed the title; the back arrow flushed a last line before navigating. An expiring login with unsaved text on screen: after the reload the field showed the text again and it reached the server, nothing left parked. An empty note opened and left was archived. Paragraph direction measured as `unicode-bidi: plaintext`. Review (rule 17, high): 1 finding, mine, fixed: the editor's saver was created during render and disposed in an effect cleanup, so React's development double mount killed it before the first keystroke and nothing was ever sent. The saver now lives inside the mount effect and `dispose()` parks unsaved text, BugAtlas row 5. Known limit: a `keepalive` flush carries at most 64 KB, so a very long note flushed only by closing the tab could exceed it; every other path is unaffected. | Pass | high | `07078a8` | `git checkout 07078a8 -- src/web` and delete the five new files |
| 2026-09-04 | Plan 4.7 to 4.10, sections, menus, shortcuts, feedback | New `Menu.tsx`, one actions menu for rows, the note page and later tiles: Escape and click outside close it, focus returns to the button. `NoteRow.tsx` rebuilt as a div with a covering link, a hover pin and a menu with Archive. `Project.tsx` splits the list into Pinned and Others with headers only when something is pinned, N opens the composer unless a field has focus, row skeleton while loading, toast on a load failure. `Composer` exposes `open()` through a ref for the shortcut. `Note.tsx` gained a menu with Archive, Escape goes back the same way the arrow does, skeleton and toast. New `Feedback.tsx` with `Skeleton`, `TileSkeleton`, `RowSkeleton`, `Toast`; Home uses the tile skeleton and the toast. | Driven in the browser. Sections: Pinned above Others with the pinned note in it; pinning Hooks from its row moved it to the top of Pinned and the server agreed; unpinning moved it back; with nothing pinned both headers disappear. Row menu Archive removed a note from the list and the server count dropped by one. N opened the composer, n inside the field stayed a letter, Escape closed it. Note page: Escape went back to the project; Archive from the header menu left the page and the list shrank by one. Home with the project request held for 1.5 s: skeleton present and no tiles at 300 ms, three tiles and no skeleton after; with the request failing, the toast read the server's message, positioned fixed. My first skeleton check was invalid because synthetic navigation did not remount Home, so it was redone through real links. Review (rule 17, medium): no findings. | Pass | medium | `07078a8` | `git checkout 07078a8 -- src/web` and delete `Menu.tsx`, `Feedback.tsx` |
| 2026-09-04 | Plan 5.1, create R2 | Rotem enabled R2 in the dashboard, then `npx wrangler r2 bucket create rogers-images`. Added `r2_buckets` to `wrangler.json` bound as `IMAGES`; regenerated `worker-configuration.d.ts`. | The first attempt failed with Cloudflare's code 10042, R2 not enabled on the account, which is a dashboard switch only the owner can flip; Rotem flipped it. `wrangler r2 bucket list` shows the bucket. `npm run check` green and the dry-run deploy lists `env.IMAGES (rogers-images) R2 Bucket` beside the database. | Pass | low | `d81c84a` | `npx wrangler r2 bucket delete rogers-images` and drop the block |
| 2026-09-04 | Plan 5.2, upload and image routes | New `src/api/images.ts`: `POST /api/upload` takes the raw image body, accepts PNG, JPEG, GIF, WebP and AVIF, refuses over 10 MB by the declared length and again by the real length, makes a uuid key with the extension of the type, stores in R2 with the content type. `GET /api/images/:key` checks the key shape before touching the bucket, serves with the stored type, `Cache-Control: private, max-age=31536000, immutable` and the ETag, and answers 304 to a matching If-None-Match. Mounted under `/api`. | From the browser with a PNG drawn on a canvas: upload 201 with a uuid-plus-png key; served back 200 as image/png, same byte size, immutable cache header, ETag present; the bytes decoded to the 64 by 48 picture that was drawn; a revisit with the ETag answered 304; a missing key 404; a key that was a path 400; text/plain 415; an empty body 400; ten megabytes plus one byte 413. `npm run check` green. Review (rule 17, medium): no findings; keys are made server-side so a client can never name a path. | Pass | medium | `d81c84a` | delete `images.ts` and its two mount lines; the test object stays in the bucket until the cleanup step |
| 2026-09-04 | Plan 5.3 to 5.5, pictures in the app | New `src/web/lib/upload.ts`: accepted types, a 10 MB courtesy check, `uploadImage` through the API client with the file's own content type, `imageSrc`. New `ImageStrip.tsx`: thumbnails, a remove button on hover, and the lightbox, which closes on Escape or a click and stops Escape from reaching the note page. `Composer.tsx`: paste, drag with a highlight, drop, an image button in the collapsed bar and in the toolbar behind one hidden file input, uploading status, a paste as the first change creates the note, and an empty check that counts pictures. `Note.tsx`: the same paste, drop and picker, thumbnails under the text, removal through the normal save. `NoteRow.tsx`: small thumbnails. | In the browser, in a fresh test project of my own, with pictures drawn on a canvas. Composer: a paste as the very first thing showed a thumbnail, created the note with one png key, the served bytes matched the pasted file, and after Close the row carried the thumbnail and did not read Empty. Note page: a paste made two thumbnails; a dragover turned the border to the accent colour and a drop made three, border back to none, server holding three; removing the middle one left two on screen and on the server; the lightbox opened on click with the picture, closed on Escape without leaving the note, and closed on click; status Saved. Before all that the dev server had gone stale a second time, proven by two different version stamps for the same React file, and Rotem restarted it. `npm run check` green. Review (rule 17, high, the image flows): no findings. | Pass | high | `d81c84a` | `git checkout d81c84a -- src/web` and delete `upload.ts`, `ImageStrip.tsx` |
| 2026-09-04 | Plan 6.1 and 6.2, deploy | `npm run build` then `npx wrangler deploy`: 4 assets uploaded, both bindings listed, version `5fe869ee`. `package.json` deploy script now builds first. `CLAUDE.md` table gained the live address. | The first minute answered Cloudflare's own error 1042 on every path, which is the workers.dev address propagating on a fresh account; a retry answered normally. Then, creating nothing: `/api/health` 200 JSON; a deep path returned the app shell with the dark colour-scheme meta and the title Rogers; `/api/projects` returned `[]`, the live database untouched; an unknown API path answered JSON 404; a bad image key 400. Review (rule 17, high, public): nothing to fix, and the login step follows in the same sitting. | Pass | high | `4ed176e` | `npx wrangler delete` takes the Worker down |
| 2026-09-04 | Plan 7.1, Cloudflare Access | Rotem, in the dashboard: Worker page, Access tab, "Protect this Worker behind Access", scope All traffic, policy Cloudflare account members Allow, session 7 days which is the dialog's maximum. Two traps avoided on the way: the "Email domain" policy would have admitted every Gmail user, and "Previews only" would have left the real address public. | From a shell with no session: `/`, `/api/health`, `/api/projects` and an image address all answered 302 to `roteme-pages.cloudflareaccess.com`. The email-code sign-in itself is Rotem's incognito check, 7.2. | Pass | high | `ab56316` | dashboard, Access tab, remove the policy |
| 2026-09-04 | Plan 7.3, PWA | `public/manifest.webmanifest`, standalone, dark colours, 192 and 512 icons plus a maskable one. Icons generated by a small PNG encoder written for the purpose, the accent disc with a dark notch on the page ground. `public/sw.js`: a service worker that caches exactly one file, the offline page, and answers only failed navigations with it, so nothing behind Access can ever be cached as data. `public/offline.html`, dark, with a retry button. `index.html`: manifest link with `crossorigin="use-credentials"`, which Access requires, and the new icon as favicon and apple-touch-icon; the template's `vite.svg` removed. `main.tsx` registers the worker on load. Deployed as version `6409e199`, 8 files. | On the local server: the manifest link carries use-credentials, the manifest reads name Rogers, standalone, three icons, dark theme; all three icon addresses 200; favicon is the new icon; the offline page carries the dark ground. The worker could not be registered inside the browser pane, `unknown error when fetching the script`, while a plain fetch of `/sw.js` answered 200 text/javascript, which points at the pane's automation layer rather than the code; the install and the airplane-mode check are Rotem's, in Edge, as the plan says. Build carries all five files. | Pass, install pending Rotem | medium | `ab56316` | delete the four public files, restore `index.html` and `main.tsx` from git |
| 2026-09-04 | Plan 7.2, the login check | No code change. PLAN.md status line and CLAUDE.md state line updated to V1 complete. | Rotem, in the browser: opened the live address, received the email code, signed in, and reported everything working inside the domain. | Pass | low | `fa3c050` | none |
| 2026-09-04 | Drag to reorder projects on Home | New `migrations/0002_project_position.sql`, one additive nullable `position INTEGER`. `src/api/projects.ts`: the list now sorts `position IS NULL, position ASC, created_at ASC`, and a new `PUT /api/projects/order` writes a position to every id the browser sends, in one D1 batch. `src/shared/types.ts` gained `ReorderProjectsBody`. `src/web/pages/Home.tsx`: each tile sits in a draggable wrapper, hovering another tile previews the move, the drop saves it, a cancelled drag puts the preview back, and a failed save falls back to the server order with a toast. `src/web/components/ProjectTile.tsx`: the overlay link is not draggable, without which the link drags its own address instead of the tile. | Driven in the browser at localhost:5173 by dispatching real drag events, because a tool-driven mouse drag cannot start a native one; that method is now QA section 10c. Reorder: First test! dragged to the end previewed on hover, held after the drop, the order route answered 200, and the order survived a reload. Cancel: a drag let go with no drop reverted to the order it started from. Rename guard: a drag begun inside the rename field was prevented and carried nothing, so the tile stays put while text is being edited. Stale order: after a drop, creating a fourth project through the plus tile showed all four tiles rather than dropping one, which is the fallback doing its job; that test project was archived and the original order dragged back. Error paths, all JSON so none can read as an expired login: no ids 400, a string instead of a list 400, a path as an id 400, a duplicated id 400, and an id the database does not have ignored with the rest applied, 200. Layout measured rather than eyeballed: no horizontal overflow at 375, 639 or 641, two columns below the breakpoint and three above, and the new wrapper measures exactly the same box as the tile, 4:3 intact. Clicking a tile still opens its project, which is what turning off the link drag could have broken. Checks green. Lint reports one error, pre-existing, in useApi.ts. Console: passed, known existing error only, the service-worker registration failure inside the browser pane recorded at plan step 7.3, plus the 400s from my own error probes. Midway a shell command of mine executed a rollback line out of its own text and reverted all four source files; they were rewritten to the same content, checks rerun, and the drag test rerun against the restored code before this row was written. Mistakes row added. Review (rule 17, high, schema plus a write to every project row): no findings. Blast radius shared and traced: ProjectTile has one consumer, Home; the only other query touching the projects table is an existence check in notes.ts, unaffected by a new column; updatedAt is read nowhere in the app, so bumping it on a reorder shows nothing; the insert names its columns explicitly, so a new project gets a null position. | Pass | high | a0cfdeb | restore src/api, src/web and src/shared from a0cfdeb, delete the migration, and drop the column; nothing reads it once the code is back |
| 2026-09-04 | Full paths in commands | Conversations.md rule 12 sharpened: a command carries the full absolute path it needs, never a bare command that only works from one directory, and its reasoning paragraph says why. | Rotem corrected me after I handed him a migration command prefixed with "run this in the project folder". The rule for paths already existed and I had followed it only for file paths. Read the rule back; it sits with the existing absolute-path lines and the two do not contradict. No Mistakes row: the rule home was obvious, so the rule was written instead. | Pass | low | a0cfdeb | restore project-os/Conversations.md from a0cfdeb |
| 2026-09-04 | Migrate the live database, project position | Rotem ran the remote migration from his own terminal, applying 0002_project_position.sql to rogers-db on Cloudflare, 2 commands. | My own attempt was refused twice by this session tooling, and the one remote call that did run answered Cloudflare error 7403 despite a valid login carrying D1 write access; his run went through cleanly, so the 7403 was on my side and transient. After: the remote migrations list reports nothing left to apply. The column is additive and nullable, so every existing project row keeps its values and reads back unchanged, and the currently deployed Worker never selects the column, so the live app is unaffected until the next deploy. Review (rule 17, high, live schema): no findings; an ALTER TABLE ADD COLUMN cannot drop or rewrite a row. | Pass | high | a0cfdeb | drop the column on the remote and clear its d1_migrations row; nothing deployed reads it |
| 2026-09-04 | Drag to reorder notes inside a project | New migrations/0003_note_position.sql, one additive nullable position column on notes. src/api/notes.ts: the list order became a pinned split, then position with undragged notes FIRST, then the original pair, so an untouched database answers exactly as before; a new PUT on the project notes route writes a position to every id one section sends, in one D1 batch, scoped by project id and deliberately not touching updated_at, which belongs to the save path; pinning or unpinning now clears the position. src/api/rows.ts declares the new column and the list query selects it. New src/web/lib/reorder.ts holds the two pure helpers, moved out of Home so both screens share one idea of what a drag does. src/web/pages/Project.tsx: each section carries its own drag state, which is what makes a cross-section drop impossible. src/web/components/NoteRow.tsx: the covering link is no longer draggable. | Driven in the browser in a throwaway project of my own, never on Rotem's notes, by dispatching real drag events per QA section 10c. Order preserved first: four fresh notes listed newest first, unchanged. Drag: the last row dragged to the top previewed on hover, held after the drop, and the Worker agreed. The two behaviours that had to survive: a note created after the list was arranged still came back at the top, and pinning put a note on top of Pinned, then a second pin went above it, most recently pinned first, which is what clearing the position buys. Cross-section: a row dragged from Others onto a pinned row moved nothing, the pinned list refused the drop, and the row went back. Inside Pinned the drag worked and persisted. Cancel with no drop reverted. Error paths, all JSON so none can read as an expired login: no ids 400, a path as an id 400, a duplicate 400, an unknown project 404, and an id the project does not have ignored with the rest applied, 200. Layout: no overflow at 375, and the row measures the same box it did before. Clicking a row still opens the note. Home re-tested after its helpers moved out: still drags and saves. Checks green, lint reports the one pre-existing error in useApi.ts. Console: passed, known existing error only, the service-worker registration failure from plan step 7.3, plus my own probes. The test project was archived and Rotem's project order restored. Review (rule 17, high, schema plus a write to note rows): 1 finding, mine, fixed, the row type had gained a column the list query did not select. Blast radius shared and traced: NoteRow and Section have one consumer each, the reorder never reads or writes note text or updated_at, and the insert names its columns so a new note gets a null position. | Pass | high | e05a49e | restore src/api, src/web and src/shared from e05a49e, delete the migration and reorder.ts, and drop the column |
| 2026-09-04 | Deploy the two drag orders | npm run deploy on Rotem's instruction: built the working tree and uploaded it, 3 changed assets, version 174c2d32-f6e4-494d-b10f-e1264b592aa3. No code change in this row. | Checked before deploying that the live database was already migrated, since the new list queries read a column that had to exist first: the remote migrations list reported nothing to apply, so both position columns were there. Rotem had run the notes one himself. After: the live address and /api/projects both answered 302 to the Access login, so the login still covers the pages and the API. The drag itself could not be exercised live from here, because everything behind Access needs Rotem's own session; his check. Note for the next reader: this deployed the WORKING TREE, so the note reordering was live before it was committed. | Pass, live check is Rotem's | high | e05a49e | npx wrangler rollback, or redeploy from e05a49e |
| 2026-09-04 | Live check of the two drag orders | No code change. Closes the open live check on the deploy row above. | Rotem, in his own signed-in browser on rogers.rotem914.workers.dev: dragged a project tile and a note row and reported both working. | Pass | low | e05a49e | none |
| 2026-09-04 | Composer opens on the title | Rotem's edit, not mine, found uncommitted in the working tree at commit time. src/web/components/Composer.tsx: a ref on the title field, and the open effect focuses it instead of the body. | Not checked by me: it is his change and his call. It built and typechecked as part of the same green check, and it was already live, since the deploy earlier today built the working tree. Committed here rather than left out, so the repository matches what is deployed. | Pass on build only | low | e05a49e | git checkout e05a49e -- the composer file |
| 2026-09-04 | Correction: the composer's title focus | No new code. The change carried in `b27754f` to `src/web/components/Composer.tsx`, a ref on the title field and the open effect focusing it, was mine, made in this session on Rotem's request that the field which opens be the title rather than the body. Another session committed and deployed it while my QA was still running, and recorded it as his and as unchecked. | Driven at localhost:5173 in a throwaway project of my own, archived afterwards, so no note of Rotem's was touched. A real pointer click on "Take a note..." opened the composer with `document.activeElement` the Title input. Typing "Focus lands here" landed in the title, the body stayed empty, and the server held the same title. The N shortcut opened it on the title too, Escape closed it, and his own project still held exactly its two notes, so nothing was created. Paste and drop, which he asked about in the same sitting, were exercised in the same pass: a pasted PNG with focus in the title, and again with focus in the body, each appended a key and a thumbnail; a dropped PNG turned the border to the accent colour during the drag and back after it; the note ended with three images on the server. `npm run check` green. Console: passed, known existing errors only, the service-worker registration failure from plan step 7.3 plus two 404s from my own navigation to a project id the local database does not have. Review (rule 17, medium): no findings introduced. Blast radius local: Composer has one consumer, Project.tsx, and the two other ways into the same open effect, the N shortcut and an image paste while collapsed, were both exercised. One leftover reported and not fixed, waiting on his verdict: the `bodyField` ref is now written and never read. | Pass | medium | e05a49e | `git checkout e05a49e -- src/web/components/Composer.tsx` |
| 2026-09-04 | Remove the composer's unused body ref | `src/web/components/Composer.tsx`: the `bodyField` ref and the `ref={bodyField}` on the note textarea, both dead since the open effect moved to the title. The textarea keeps its own internal ref, so the auto-grow is untouched. Rotem's verdict, asked and given in the same sitting. | `npm run check` green. Driven at localhost:5173 in a throwaway project of my own, archived afterwards. Clicking "Take a note..." still opened the composer with `document.activeElement` the Title input. Four lines of text put into the note field grew it from 260 to 806 pixels, so the auto-grow still runs without the outside ref, and the full four lines read back from the server. A pasted PNG still appended a thumbnail and one image key. Console: passed, known existing error only, the service-worker registration failure from plan step 7.3. Risk low, so no review armed. | Pass | low | b27754f | `git checkout b27754f -- src/web/components/Composer.tsx` |
| 2026-09-05 | Drop the loading placeholders on the project and note pages | `src/web/pages/Project.tsx`: the `RowSkeleton` in the project-loading branch and the one under the composer while the notes load, both gone, and the import with them; the loading branch still returns early with an empty `main`, which is what keeps "That project is not here any more" from flashing before the answer arrives. `src/web/pages/Note.tsx`: the same, its `Skeleton` and import removed. Nothing else touched: Home keeps its tile placeholder, and `Feedback.tsx` keeps exporting all three. | Driven at localhost:5173 with `window.fetch` patched to hold every `/api` call for 1.2 seconds, so the load could actually be watched, and by client-side navigation from Home so the patch survived. Entering a project with two notes: zero placeholder elements at 250, 500, 750 and 1000 ms, no wrong message, then the real list. Entering a note from that list: the same four samples clean, then the note's own title and text in the fields. Entering an empty project: "Nothing in this project yet" appeared only after the load, never during it. A first probe of mine was invalid because it counted `animate-pulse`, which these placeholders never had; it was redone counting the `aria-hidden` boxes they actually render. Not-found paths re-checked by real navigation: a bogus note id gives "That note is not here any more" with the toast, a bogus project id gives its own line, no placeholder in either. A synthetic `pushState` plus `popstate` did NOT remount the note page, so that route was checked by real navigation instead. `npm run check` green. Console: passed, known existing errors only, the service-worker registration failure from plan step 7.3 plus the 404s from my own bogus ids. Review (rule 17, medium): no findings introduced. Blast radius local: both pages render only themselves, `Feedback.tsx` is unchanged, and Home was not touched. Reported and not fixed, waiting on his verdict: `RowSkeleton` is now used nowhere, and Home still shows tile placeholders. | Pass | medium | 16d136c | `git checkout 16d136c -- src/web/pages/Project.tsx src/web/pages/Note.tsx` |
| 2026-09-05 | Drop Home's tile placeholders and the placeholder components | Rotem's yes to both questions on the row above. `src/web/pages/Home.tsx`: the `TileSkeleton` line above the grid and its import, gone; the plus tile and the empty line are still gated on `!loading`, so neither appears before the answer. `src/web/components/Feedback.tsx`: `Skeleton`, `TileSkeleton` and `RowSkeleton` deleted, since removing Home's use left all three unreachable, and the file header rewritten to describe the toast alone. `Toast` untouched. | Driven at localhost:5173 with `window.fetch` again holding every `/api` call for 1.2 seconds, reached by clicking back to Home from a project so the patch survived. Home: zero placeholder elements across four samples, only the Rogers bar, then the three real tiles with their names and note counts and the plus tile. The drag wrappers are still there, three of them, and clicking a tile still opened its project. `npm run check` green, which is also what proves nothing still imports the deleted components. Console: passed, known existing error only, the service-worker registration failure from plan step 7.3. Review (rule 17, medium): no findings introduced. Blast radius local and now closed: a repository-wide search for `Skeleton` returns nothing outside this row's own deletions. | Pass | medium | 16d136c | `git checkout 16d136c -- src/web/pages/Home.tsx src/web/components/Feedback.tsx` |
| 2026-09-05 | Google Sans on every page | `index.html`: two preconnects and one stylesheet link for Google Sans from Google Fonts, asked for as a weight RANGE so the variable file arrives, one download per script rather than one per weight. `src/web/index.css`: "Google Sans" first in the body font stack, the old system stack kept behind it as the fallback while it downloads and if it never arrives. `src/web/components/AutoGrowTextarea.tsx`: the height measurement pulled into one `fit()` and run a second time on `document.fonts.ready`, which is a guard this change needed, not a request of Rotem's. Nothing else touched; `public/offline.html` keeps the system stack on purpose, since a page shown with no network cannot fetch a webfont. | Driven at localhost:5173, Rotem's own server, on all three screens. The family really is applied and really is Google Sans, not a fallback: computed `font-family` reads "Google Sans" on the body, on every button, on the title input and on the note field, and `document.fonts` shows two subset files actually loaded, latin and hebrew, the hebrew one carrying U+590-5FF. Layout proven not to move: with the old stack forced back on the same page, header height, every note row height and document scroll width came back byte-identical at 375 and at 1280. No horizontal overflow and no clipped element at 375, 639, 640 and 1280; both sides of the one breakpoint behave, two tile columns below and three above. The dark-first-frame invariant was tested rather than assumed, with a throwaway page in `.tmp/` copying this head and a stylesheet pointed at an unroutable address: the ground still painted `#17181a` behind the hanging request, so a slow or dead Google Fonts cannot cause a light flash. The composer was opened and closed with Escape: both fields in Google Sans, auto-grow exact, and the project still held its 5 notes, so nothing was created. No note of Rotem's was edited at any point. `npm run check` green. Console: passed, known existing error only, the service-worker registration failure inside the browser pane recorded at plan step 7.3. Review (rule 17, medium): 1 finding, mine, fixed. The note field sizes itself by reading `scrollHeight` once, and Google Sans sets some lines taller than the fallback, so a height measured before the font arrived stayed one line short behind `overflow-hidden` and hid text the owner had typed; measured on a detached probe, 15 of 240 sampled Hebrew and English paragraphs needed one more line, and the worst replay left 26 pixels, a full line, invisible. The `document.fonts.ready` remeasure closes it: the same replay ends at exactly the height Google Sans needs. Blast radius shared and traced: the body font stack reaches every screen, and Home, a project and a note were each opened and measured, including buttons, the title input and the note field; `AutoGrowTextarea` is the only code in the repository that measures text, confirmed by searching for scrollHeight, offsetHeight, clientHeight and getBoundingClientRect. Two things reported, not fixed, waiting on his verdict: every page load now asks Google Fonts for the stylesheet, which is the one outbound request this otherwise private app makes, and self-hosting the two woff2 files would remove it; and the font is one render-blocking third-party request on a cold load, which delays the first frame of CONTENT though never the dark ground. | Pass | medium | 16d136c | `git checkout 16d136c -- index.html src/web/index.css src/web/components/AutoGrowTextarea.tsx` |
| 2026-09-05 | Re-lay out the header, the note page and Home (FAST MODE burst) | `src/web/components/TopBar.tsx`: background and bottom border gone, the fixed 56px height became a minimum, 32px of top padding, the content in a centred 1168px container that now carries the 16px side padding, the `‹` glyph replaced by a 24px chevron SVG centred in a 48px box, the title 32px. `src/web/pages/Project.tsx`: the project name 32px in both its reading and renaming states, the column starts 56px below the bar, the note list gap 14px. `src/web/components/NoteRow.tsx`: the card and its click overlay 10px radius. `src/web/components/Composer.tsx`: 10px radius on the collapsed shell, its button and the expanded card; the small buttons inside kept theirs. `src/web/components/ImageStrip.tsx`: a third size, `lg`, that renders a picture at its own aspect, full column width, capped at 1160px. `src/web/pages/Note.tsx`: the page widened to 1160px, pictures at `lg`, the body text in a centred 720px column at 18px with a 112px minimum height, and the Title field moved out of the body to the top of that column, 48px under the bar and 32px above the body. The bar's title is empty there now, and the `/api/projects` fetch that fed it, plus the `Project` type import, are gone with it. `src/web/components/ProjectTile.tsx`: the project name 24px. `src/web/pages/Home.tsx`: the grid in fixed 272px columns with a 16px gap, in a 1168px container, starting 56px below the bar. | FAST MODE was on for the whole burst, so Rotem checked each round himself in his own app at localhost:5173 and asked for the next one; that is what replaces the per-round browser QA and the rule-17 review here. `npm run check` was run by me and passed three times inside the burst, after the note title moved into the bar, after the body Title field was removed, and after it was placed in the 720px column, and again at this commit. Not verified by me and worth a look: the project tile's rename field still uses the old size, so the text jumps when you click to rename; the note page's loading and not-found states kept their narrow `max-w-2xl` box; and with no background on a sticky bar, page content now scrolls visibly behind it. | Pass | medium | 16d136c | `git checkout 16d136c -- src/web/components/TopBar.tsx src/web/components/ImageStrip.tsx src/web/components/ProjectTile.tsx src/web/pages/Home.tsx src/web/pages/Note.tsx src/web/pages/Project.tsx src/web/components/NoteRow.tsx src/web/components/Composer.tsx` — note the last two also carry the earlier uncommitted placeholder round, so that goes back with them. |
| 2026-09-05 | Self-host Google Sans | Rotem's instruction, after he asked whether the typeface was legal to use and then whether hosting it ourselves was. It is: Google published Google Sans under the SIL Open Font License 1.1 on 2025-12-09, the served binaries carry `Copyright 2025 The Google Sans Project Authors (github.com/googlefonts/googlesans)` and point their licence URL at openfontlicense.org, and the upstream repository is OFL-1.1 with no Reserved Font Name, its `TRADEMARKS.txt` restricting only the Google NAME, never the typeface. New `src/web/styles/fonts/`: three variable woff2 subsets, latin, latin-ext and hebrew, each covering weight 400 to 700, plus `OFL.txt` and `TRADEMARKS.txt`, which is condition 2 of the licence. `src/web/index.css` gained the three matching `@font-face` rules with the same unicode ranges Google serves; the body font stack is untouched. `index.html` lost the two preconnects and the Google stylesheet link, so it is byte-identical to what it was before Google Sans arrived. `AutoGrowTextarea.tsx` untouched: its fonts-ready remeasure still guards the swap window, which is now much shorter. | Driven at localhost:5173, Rotem's own server. The point of the change was measured directly: zero requests to fonts.googleapis.com or fonts.gstatic.com on Home, on a project and on a note, and no Google stylesheet link left in the head. The fonts come from our own origin instead, latin and hebrew both 200 with no failed font request anywhere. The family really is applied: computed `font-family` reads "Google Sans" on the body, on the note field, on the title input and on all 13 controls of the project page. Hebrew is genuinely Google Sans and not a fallback, measured at 103.1 pixels against Arial's 88 for the same word. Production paths checked separately from dev: the build emits all three files into `dist/client/assets` with content hashes, and the built CSS points at those hashed names, which is what buys them immutable caching; the licence files correctly do NOT ship to the browser, since the binaries carry the copyright in their own metadata, which the licence accepts. No horizontal overflow and no clipped element at 375, 639, 640 and 1280. Note page at 375: the field still fits its text exactly and the note read back unchanged. Composer opened and closed with Escape: both fields in Google Sans, caret in the title, and the project still held exactly 5 notes, so nothing was created. No note of Rotem's was edited at any point. `npm run check` green. Console: passed, known existing error only, the service-worker registration failure inside the browser pane from plan step 7.3. Review (rule 17, medium): no defects introduced. Blast radius shared and traced: the font reaches every screen, and Home, a project and a note were each opened and measured; `public/offline.html` keeps the system stack on purpose, since a page shown with no network cannot fetch a webfont; `sw.js` caches only the offline page so it never touches fonts. Two limits named rather than fixed: an alphabet outside Latin, Latin extended and Hebrew now falls back to the system font, where the Google CDN would have fetched that subset on demand, and the fonts have not been proven to load THROUGH Cloudflare Access, because that can only be seen on the live site, so it needs one look after the next deploy. Note for the next reader: commit `8cc36b7` landed from another session while this work was in progress and swept in the earlier CDN version of Google Sans together with unrelated layout work, so this row's rollback target is that commit, not the one before it. | Pass, live check pending | medium | `8cc36b7` | `git checkout 8cc36b7 -- index.html src/web/index.css` and delete `src/web/styles/fonts/` |
| 2026-09-05 | Deploy everything waiting | `npm run deploy` on Rotem's "deploy all", which is the approval rule 22 requires before anything reaches the public. No code change in this row. It shipped MORE than the fonts: the previous live version was `174c2d32` from the drag-order deploy, so commit `8cc36b7` from another session had never gone out, and this deploy carried its header, note page, Home grid and placeholder removals live alongside the self-hosted typeface. Uploaded 6 changed assets, the three Google Sans woff2 files among them, 5 already present. Version `80cb9449-41ef-4d0c-8e87-8dac80872253`. | Checked before deploying that the live database needed nothing, since a deploy of unmigrated code is the way this project could break itself: `wrangler d1 migrations list --remote` answered "No migrations to apply", and this change is CSS and static files only, no schema. `npm run check` green first. After, from a shell holding no session: the app root, `/api/projects`, the deployed stylesheet and the deployed hebrew font file ALL answered 302 to `roteme-pages.cloudflareaccess.com`, so the login still covers the pages, the API and now the fonts, and nothing became publicly readable by moving the typeface into our own assets. The deploy built the WORKING TREE, so the font work went live before it was committed, exactly as the drag-order deploy did. Not checked, and it needs Rotem's own signed-in browser: that the font actually RENDERS through Access. A webfont is fetched in CORS mode with same-origin credentials, so the Access cookie should ride along on a same-origin request and the file should serve, but "should" is not a check, and the failure mode is silent, the live app quietly falling back to the system font while looking fine. | Pass, live render check is Rotem's | high | `174c2d32` | `npx wrangler rollback` to version `174c2d32-f6e4-494d-b10f-e1264b592aa3` |
| 2026-09-05 | Live render check, Google Sans through Access | No code change. Closes the open item on the deploy row above, which shipped with the render unverified. | Rotem, in his own signed-in browser on rogers.rotem914.workers.dev: the text renders in Google Sans. That settles the one question a shell with no session could not answer, since a font is fetched in CORS mode with same-origin credentials and only a real session proves the Access cookie rides along. The failure mode it rules out was silent: the live app falling back to the system font while looking perfectly fine. | Pass | low | `0099895` | none |
| 2026-09-05 | Put Rogers on rogers.rotem-e.com | `wrangler.json`: a `routes` entry declaring `rogers.rotem-e.com` as a custom domain, three lines. Nothing in the code named a hostname, so nothing else moved. Rotem added the hostname to the Access application "rogers - Cloudflare Workers" in the Zero Trust dashboard BEFORE the deploy, so the new address was never public for a moment. Deployed on his "DEPLOY", version `cf49ee70-42dc-4bac-a82b-c802b7d41163`. Side effect, reported not decided: declaring a route makes Wrangler disable the workers.dev address unless `workers_dev: true` is set, so `rogers.rotem914.workers.dev` now answers 404. | Diagnosis first, all on his phone over 4G: the bare Cloudflare login host loaded in Chrome and Safari, an Incognito tab hung the same, Safari hung on the Rogers address, and `welcome.developers.workers.dev` hung too, which pinned the block on the workers.dev domain family and cleared the login page, the service worker and the app. From here with an iPhone identity the old address always answered a 302 to Access in 3 seconds. After the deploy, from a shell holding no session: `/`, `/api/projects`, `/manifest.webmanifest` and `/assets/` on the new address all 302 to `roteme-pages.cloudflareaccess.com/cdn-cgi/access/login/rogers.rotem-e.com`, so pages, API, manifest and assets are all behind the login. `npm run check` green and `wrangler deploy --dry-run` clean before deploying. Not checked yet: the phone itself on the new address, which is Rotem's. Review (rule 17, high): the diff is three config lines and correct; blast radius traced, contained to hostnames: the service worker, manifest and API client are all path-relative and host-agnostic, and Access detection keys on a non-JSON answer, not a hostname. One finding, introduced: the old address disabled, reported above rather than fixed, since keeping it is his call. | Pass, phone check is Rotem's | high | `0099895` | `git checkout 0099895 -- wrangler.json` then `npm run deploy`, which brings the workers.dev address back; removing the hostname from the Access application is Rotem's, in the dashboard. |
| 2026-09-05 | Phone check of rogers.rotem-e.com | No code change. Closes the open item on the custom-domain row above, which shipped with the phone unverified. | Rotem, on his iPhone over the same 4G that hung on the old address: the Home screen with the projects grid rendered dark, and Add to Home Screen installed it as a standalone app, so the manifest is served and read through the login on the new hostname too. On the desktop, Chrome offered Install on the new address and opened Rogers in its own window. Still open, his verdict: the old workers.dev address, dark since the deploy. | Pass | low | `0099895` | none |
| 2026-09-05 | Retire the workers.dev address | `wrangler.json`: `workers_dev: false`, one line, on Rotem's "lets delete it". It records the verdict the custom-domain row left open; the address was already dark since that deploy, so nothing live changes and no deploy was needed. The Access application still lists the Worker itself as a destination, which is harmless and untouched. | `npm run check` green. The old address still answers 404 from a shell, and the new one still 302s to the login, both confirmed after the edit. | Pass | low | `0099895` | Remove the line, or set it to `true`, and `npm run deploy` |
| 2026-09-05 | Home layout pass, mobile and desktop | Fast mode, one owner-checked round each. `tokens.css`: `--radius-card` 8px to 13px, which lifts every `rounded-card` surface, not only tiles. `Home.tsx`: grid gap 20px, container `max-w-[1324px]` with `px-[72px]` and `max-[900px]:px-[18px]`, breakpoint A `max-[900px]` swapping the fixed 272px tracks for `minmax(185px,1fr)`, breakpoint B `max-[430px]` forcing one column and giving all three grid cells a fixed 144px height with the 4:3 ratio dropped, tile padding 24px, and a `pointerdown` listener while naming that commits the typed name when the click lands outside the card. `TopBar.tsx`: the same container width and padding so the title starts on the tiles' left edge. `ProjectTile.tsx`: the note-count line removed on every size, the failed-rename message kept in its place. `ProjectTile`, `NoteRow` and the composer button: `transition-colors duration-[144ms] ease-out`. Side effects reported and kept: the radius token is shared, and the 1324px width was required or 72px of padding would have left room for only three columns. A borderless tile was tried on his word and reverted on his word, because the border is what shows a project's colour. | Rotem checked each round in his own running app, which is what fast mode trades the per-round QA for. In the browser myself: at 1300px the four tiles sit in one row with the title on their left edge; typing a name and committing it created the project, which appeared in the grid with no console error and a clean POST, and I archived that test project afterwards. `npx tsc --noEmit` clean after the outside-click listener. Two deploys on his "DEPLOY", versions `cb9e6a7b` and `c11ae418`. Not verified by me: the 430px and 900px breakpoints on a real phone. | Pass | medium | `b68d33d` | `git checkout b68d33d -- src/web` |
| 2026-09-05 | Bigger picture thumbnails in a note row | `ImageStrip.tsx`, one branch of the size map: the `sm` box goes from `size-12` to `size-[216px]`, tried at 256px first on his word and lowered to 216 on his word. `sm` is asked for in exactly one place, `NoteRow.tsx`, so nothing else could move. | In the running app, the project with two pictures: both thumbnails measure 216 by 216 by `getBoundingClientRect`, render square and side by side in the row, and both image requests answer 200. The composer and note page were left on `md` and `lg` and were not touched. Console: no new errors; the service-worker registration failure and two 404s are from dev and from my own navigation to a live-only project id. | Pass | low | `562830f` | `git checkout 562830f -- src/web/components/ImageStrip.tsx` |
| 2026-09-05 | Undo and redo, plus a fast-mode spacing burst | Fast mode. New `src/web/lib/undo.ts`: an `UndoStack` of `{title, body, images}` snapshots, typing in one field folded into a step within 1000ms, pictures always their own step, 200 steps kept, and `undoShortcut()` keyed on `event.code` with the key name as fallback. `Note.tsx` and `Composer.tsx`: `change()` takes a `record` flag, records after every user edit, a `travel()` applies an undo or redo through `change(patch, false)` sending only the fields that differ, the existing Escape key listeners also route the shortcut, a restored parked draft resets the stack on the note page and the composer's `reset()` resets it there. Spacing, one round each on his word: `NoteRow.tsx` `py-3` to `py-4`; `ImageStrip.tsx` gap per size, 16px for a row, 24px on the note page, 8px in the composer, and the remove button `size-5 text-xs` to `size-8 text-[24px] leading-none`. Decision recorded in Decisions. | On the note page in the running app, driven by synthetic key events since the browser tool sends no key code: two typing bursts a second apart made two steps, undo gave `alpha ` then empty, redo and Ctrl+Y gave them back; removing a picture then undo restored it and redo removed it again; every step went out as a PATCH, and a reload showed the note back at empty with both pictures, exactly as found. `tsc --noEmit` clean twice. Not driven: the composer, which has the same wiring; Rotem checks that. The spacing rounds were his own checks. Deployed on his "DEPLOY", version `b6295090`. | Pass, composer round is Rotem's | high | `562830f` | `git checkout 562830f -- src/web && git rm -q src/web/lib/undo.ts` then `npm run deploy` |
| 2026-09-06 | Background colour and card colour | `tokens.css`: `--color-bg` `#17181a` to `#14141A`, and a new `--color-card: #23242f`. `index.html`: the two repeated copies of the ground colour, the `theme-color` meta and the first-paint inline background, moved with it. `ProjectTile.tsx:84`, `NoteRow.tsx:50` and `Composer.tsx:260`: `border border-border bg-surface` to `bg-card`. Nothing else uses `bg-card`, so no other surface moved. | `npm run check` (typecheck plus build) ran clean after each of the two edits. Not driven in a browser: no interaction changed, only two colour values and three class lists; Rotem watches the running app. | Pass | low | `b5dacae` | `git checkout b5dacae -- src/web/styles/tokens.css index.html src/web/components/ProjectTile.tsx src/web/components/NoteRow.tsx src/web/components/Composer.tsx` |
| 2026-09-06 | Hover colour for the three cards | `tokens.css`: new `--color-card-hover: #2b2d3b`. `ProjectTile.tsx:84`, `NoteRow.tsx:50` and `Composer.tsx:264`: `hover:bg-surface-hover` to `hover:bg-card-hover`. The other `hover:bg-surface-hover` uses, menus, icon buttons and the composer actions, were left alone. | `npm run check` (typecheck plus build) ran clean. Not driven in a browser: one token and three class lists, no behaviour change. | Pass | low | previous row | `git checkout <prev> -- src/web/styles/tokens.css src/web/components/ProjectTile.tsx src/web/components/NoteRow.tsx src/web/components/Composer.tsx` |
| 2026-09-06 | Deploy the colour pass | Deployed on his "DEPLOY": the new ground, card colour and card hover are live at rogers.rotem-e.com, version `2cdaefc8-6459-48aa-ae95-23c50be51d2f`. 3 of 11 assets changed, the html, the css and the js. | The deploy reported success and bound the same D1 and R2 as before; Access still fronts the domain, so nothing new is public. | Pass | low | version `b6295090` | `npx wrangler rollback` to the previous version |
| 2026-09-06 | Tabs on the project page | New `migrations/0004_tabs.sql`: a `tabs` table and a nullable `tab_id` on notes, additive, no row rewritten; Main is never a row, null means Main. `src/shared/types.ts`: `Tab`, `CreateTabBody`, and `tabId` on `Note` and on `CreateNoteBody`. `src/api/rows.ts`: the tab row and its mapper. New `src/api/tabs.ts`: list, create with "New tab" as the default name, remove by archiving; mounted in `src/api/index.ts`. `src/api/notes.ts`: the list takes `?tab=`, none meaning Main, which is every note in no live tab, so a removed tab's notes show in Main without a write; the order PUT answers the same scope; create accepts `tabId` and refuses one that is not this project's. New `src/web/components/TabStrip.tsx`: Main and the tabs as links to `?tab=`, a 32px plus with a 16px icon, a 32px remove cross on every tab but Main. `src/web/pages/Project.tsx`: reads the tab from the address, holds the notes until the tabs answer, opens a new tab once the refetched list knows it, hands the tab to the composer, hides the list while another tab's answer is in flight. `src/web/components/Composer.tsx`: a `tabId` prop sent on create. `src/web/pages/Note.tsx`: the back arrow carries the tab. Two side effects, named for veto: an empty tab says "Nothing in this tab yet" instead of "in this project", and a note's back arrow now returns to its tab. | `npm run check` green, three times. The migration applied locally, 4 commands. The queries were driven against the local database with three throwaway notes of my own in a throwaway project, all deleted after, Rotem's 21 notes untouched: Main returned the untabbed note only, each tab's scope its own note, and after archiving a tab its note came back in Main with its row unchanged. NOT driven in a browser: the dev server was not running, connection refused on 5173, and rule 16 forbids starting it; the pass in QA section 10d is owed and gets its own row. Review (rule 17, high): 1 finding, mine, fixed: switching tabs left the previous tab's notes on screen for one round trip, because the read hook keeps old data across a path change; the list is now hidden until an answer for the open tab arrives, and the check ran green after. Blast radius shared and traced: `Note` gained `tabId` and its consumers only type it; the notes list without `?tab=` now means Main, one consumer, the project page; the composer gained a required prop, one consumer, the project page; the tile's note count still counts every live note. One consequence, not a defect: the notes load one round trip later than before, after the tabs, which is what keeps a tab's address from flashing Main's list. Prerequisite of the next deploy: the remote migration, or the live list query fails on the missing table and every project page shows "Could not load the notes". | Build and data pass, browser pass owed | high | `b5dacae` | `git checkout b5dacae -- src/api src/shared src/web/pages src/web/components/Composer.tsx`, which also reverts the uncommitted colour-pass edits in the composer, then delete `src/api/tabs.ts`, `src/web/components/TabStrip.tsx` and `migrations/0004_tabs.sql`; the local table and column are additive and can stay |
| 2026-09-06 | Tabs browser pass, stale-list guard redone, and tab rename | `src/web/pages/Project.tsx`: the guard that hides the previous tab's notes while a switch is in flight was rebuilt, because the first version accepted the old list in the same commit that changed the tab; it now remembers the list a switch started from and hides exactly that one until a fresh answer replaces it, set during render on purpose. Rename, on Rotem's instruction mid-task: `src/shared/types.ts` gained `UpdateTabBody`; `src/api/tabs.ts` a PATCH by id that trims the name, refuses an empty one with 400, answers 404 for a removed tab or another project's, and treats a request with no name as a no-op; `src/web/pages/Project.tsx` a `renameTab` that sends it and refreshes the strip; `src/web/components/TabStrip.tsx`: the open tab's name is a button that turns into a field, Enter and a click outside save, Escape discards, a refused name turns the field red and keeps it open, and the button carries `aria-current` so the open tab still reads as current. Main stays a link with no rename, since it is not a row. | Driven at localhost:5173 in a throwaway project "Tabs QA" of my own, archived after; Rotem's five projects and 21 notes untouched. The plus measured 32 by 32 with a 16 by 16 icon, alone until the first click, then Main and New tab with the new one open, its address carrying `?tab=`, "Nothing in this tab yet", a 32px cross with a 16px icon, and the plus last. A title typed in the tab's composer created the note with the tab on the server and in the tab's list, Main's server list stayed empty, and a reload of the tab's address asked only for the tab's notes. A note typed in Main stayed out of the tab. The back arrow from the tab's note linked to the tab and landed there. Stale-list guard, with the notes answer held for a second: the first guard still showed the tab's row under Main at 1089 ms, which is the finding; after the rebuild both directions sampled at 321 and 851 ms showed no rows and no empty text, then the right list. A second tab added and opened. At 375 wide, resized then reloaded, the strip fit one line ending at 319 with zero overflow; zero overflow at 1280 too. Two readings of 81 and 175 were taken while the pane was hidden, with a 0-wide viewport, so they are the pane and not the page, and QA 10d now says so. Removing the tab holding a note while Main was open put the note in Main, its row still pointing at the archived tab; removing the last tab while it was open dropped `?tab` from the address, took the strip away, and Main showed both notes. Pin and unpin still split Pinned and Others. Keyboard: Tab reaches the plus and it shows the accent ring. Rename: the field opened prefilled and focused inside the card-coloured chip, a Hebrew name typed through the browser saved on Enter byte-exact, a suffix saved on a click outside, Escape discarded typed text, one PATCH per save and no double, and the name came back after a reload with plaintext bidi on the chip. `npm run check` green after each edit. Console: passed, known existing error only, the service-worker registration failure inside the browser pane from plan step 7.3. Review (rule 17, high): 1 finding, mine, the first guard, fixed and re-verified in the browser; the rename diff introduced none; blast radius local, the strip has one consumer and the route is new. | Pass | high | `b5dacae` | as the row above |
| 2026-09-06 | Migrate the live database and deploy the tabs | No code change. `npm run db:migrate:remote` applied `0004_tabs.sql` to rogers-db on Cloudflare, 4 commands, from this session this time, where the position migration had once been refused with a 7403. Then `npm run deploy` on Rotem's "DEPLOY": built the working tree, uploaded 3 changed assets, the html, the css and the js, 8 already present, version `e1ac5d1f-2251-4a20-a51c-9148bdb3ea45`, on the custom domain. | Before the deploy: the remote migrations list showed only 0004 pending, and after it nothing to apply; a remote read answered 0 tabs and 15 of 15 notes with a null tab, so every existing note is in Main and no row was rewritten. Migration first on purpose, since the new Main query reads the tabs table and would answer a 500 on every project page without it. After the deploy, from a shell holding no session: the app root, `/api/projects`, the tabs route and a notes address with `?tab=` all answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. Not checked, and it needs Rotem's own signed-in browser: the strip on the live site. This deployed the working tree, so the tabs went live before they were committed; the `Go commit` after this row closes that gap. | Pass, live look is Rotem's | high | version `2cdaefc8` | `npx wrangler rollback` to version `2cdaefc8-6459-48aa-ae95-23c50be51d2f`; the table and column are additive and stay |
| 2026-09-06 | The tab strip's shape, and undo for tabs (FAST MODE burst) | Rotem checked every round in his own app; this row is the burst's one record. `TopBar.tsx`: a new `center` slot turns the bar into three columns, gutter, the 752px page column, gutter, so the tabs start on the cards' own left edge at every width; the title keeps the same 1324px container and 72px inset the other screens use, so a project's name lands exactly where Rogers lands on Home, and the back arrow hangs out of the flow in the gutter on a flex item of no width. Below 900px the columns stack and the arrow rejoins the row. `Project.tsx`: the strip moved out of `main` into that slot. `TabStrip.tsx`: chips are `px-4 py-[6px]` at 18px, 39 tall; the plus is built from the same two numbers, 39 square, round, hidden until the strip is hovered or focused, 20px icon, pointer cursor; the open tab's name shows a text cursor; the remove cross is gone and removing is on the tab's own right-click menu; chips fade their hover over 144ms ease-out, and so does the back arrow. Rename now covers Main: new `migrations/0005_main_tab_name.sql`, one nullable column on projects, since Main has no tab row of its own; `projects.ts` reads and writes it, `types.ts` and `rows.ts` carry it. Undo and redo for tabs: new `TabHistory` in `undo.ts` keeping the actions rather than snapshots, `tabs.ts` gained `archived: false` so a removed tab can come back with its notes, and `Project.tsx` records add, remove and rename and steps through them on Ctrl+Z when the keystroke is not in a field. | Fast mode, so Rotem's own checks in his running app replace the per-round QA and the rule-17 review. `npm run check` was run by me after every round and passed every time, this commit included. Two things I did drive myself, both because he reported them wrong: the plus measured 24 wide against a 39 chip, since `aspect-square` loses to the icon's width in a flex row, and after the rebuild it measured 39 by 39 with a 20px icon; and the chip padding he thought was uneven measured 16px on both sides, with the ink bearing 1px on the left and 0 on the right, so nothing was changed there. Not verified by me: the right-click menu, the hover fades, Main's rename, and undo of add, remove and rename, all his own checks. | Pass on his checks | medium | `5427bba` | `git checkout 5427bba -- src/web src/api src/shared` then delete `migrations/0005_main_tab_name.sql`; the column is additive and can stay |
| 2026-09-06 | Migrate the live database and deploy the strip | No code change. `npm run db:migrate:remote` applied `0005_main_tab_name.sql` to rogers-db on Cloudflare, 2 commands, then `npm run deploy` on Rotem's "DEPLOY": 3 changed assets, the html, the css and the js, version `d017f296-b06d-466f-8f86-e61868776150`. | Migration first on purpose: the project list query now selects the new column, so deploying ahead of it would fail every screen. After it, a remote read answered 7 projects with 0 renamed Mains and the same 15 notes, so nothing was rewritten and every project still reads "Main". After the deploy, from a shell holding no session: the app root, `/api/projects` and the tabs route all answered 302 to `roteme-pages.cloudflareaccess.com`. This deployed the working tree, so the burst went live before it was committed; the commit below closes that gap. | Pass | high | version `e1ac5d1f` | `npx wrangler rollback` to version `e1ac5d1f-2251-4a20-a51c-9148bdb3ea45`; the column is additive and stays |
| 2026-09-06 | Composer surface and a round back arrow | Rotem's edits, not mine, found in the working tree at commit time. `Composer.tsx`: the expanded card went from `border border-border bg-surface` to `bg-card`, and the drag-over state from an accent border to `ring-2 ring-accent`, so accepting a picture no longer shifts anything. `TopBar.tsx`: the back arrow is `rounded-full` instead of the card radius; my own hover fade landed on the same line in the same burst. | Not checked by me: they are his changes and his call. They built and typechecked inside the same green `npm run check`, and they were already live, since the deploy above built the working tree. Committed here rather than left out, so the repository matches what is deployed. | Pass on build only | low | `5427bba` | `git checkout 5427bba -- src/web/components/Composer.tsx` for the composer; the arrow's radius is one class in `TopBar.tsx` |
| 2026-09-06 | Page ground to #111217 | `tokens.css`: `--color-bg` from `#14141A` to `#111217`. `index.html`: the two copies of the same colour, the `theme-color` meta and the inline first-paint background, moved with it, which is what keeps the dark-first-frame invariant true. Nothing else changed; the card, hover and border colours are untouched. | Driven at localhost:5173 on Home. All five places agree: the token reads `#111217`, `html` and `body` both compute `rgb(17, 18, 23)`, the meta says `#111217`, and so does the shell's inline style, so no light frame can appear before the stylesheet lands. The card colour still reads `#23242f`, so only the ground moved. `npm run check` green. Console: passed, known existing error only, the service-worker registration failure inside the browser pane from plan step 7.3. Reported and not fixed, waiting on his verdict: `public/manifest.webmanifest` and `public/offline.html` still carry `#17181a`, the ground from two colours ago, so the installed app's splash and the offline page are a shade lighter than the app. | Pass | low | `ecd52d8` | `git checkout ecd52d8 -- src/web/styles/tokens.css index.html` |
| 2026-09-06 | The splash and the offline page follow the ground | Rotem's verdict on the drift reported in the row above. `public/manifest.webmanifest`: `background_color` and `theme_color` from `#17181a` to `#111217`, so the installed app's launch screen matches. `public/offline.html`: its `theme-color` meta and the `html` background, the same move. Deliberately left alone: the offline page's button still uses `#17181a` for its text, because that is ink on the accent fill, the `--color-accent-ink` role, not a ground. | Both files are served as-is rather than built, so they were read back over the running server and both answered `#111217`. The offline page was then opened in the browser: `html` computes `rgb(17, 18, 23)`, its meta reads `#111217`, and the button still fills accent yellow with `rgb(23, 24, 26)` ink, so only the grounds moved. `npm run check` green, and the manifest still parses as JSON, which is the one way this file can break the installed app. Not checked, and it needs Rotem's phone: the launch screen itself, which only shows on a real install. | Pass, the launch screen is Rotem's | low | `ecd52d8` | `git checkout ecd52d8 -- public/manifest.webmanifest public/offline.html` |
| 2026-09-06 | Card colour and card hover | `tokens.css`, two values: `--color-card` `#23242f` to `#21222C`, `--color-card-hover` `#2b2d3b` to `#252632`. No component changed. Their comments were corrected in the same edit, because they named three consumers and there are now five. | Blast radius traced before the edit: `bg-card` and `bg-card-hover` are asked for in exactly four files, and they cover the project tile, the note row, the composer collapsed and expanded, and the open tab chip; the chip comes along with the cards, which is worth knowing since it is not a card. Driven at localhost:5173: Home's tiles, the note row, the collapsed composer, the expanded composer and the open tab chip all compute `rgb(33, 34, 44)`, which is `#21222C`, and the ground still reads `rgb(17, 18, 23)`. The composer was opened and closed with Escape, and the project still held its 7 rows, so nothing was created. `npm run check` green. Console: passed, known existing error only, the service-worker registration failure inside the browser pane from plan step 7.3. NOT verified, and it is a tool limit rather than a finding: the hover colour itself. The pane's synthetic pointer does set `:hover` on the row, and the row keeps its base colour, so no repaint could be measured; the generated stylesheet was read over HTTP instead and carries `--color-card-hover: #252632` with the rule pointing at it, and each new hex appears exactly once with no trace of the old ones. A `bg-card-hover` probe answered transparent, which is expected: only the `hover:` variant is ever used, so the bare class is never generated, and that probe proves nothing either way. | Pass, the hover repaint is unverified | low | `ecd52d8` | `git checkout ecd52d8 -- src/web/styles/tokens.css` |
| 2026-09-06 | Home's new-project tile: the plus icon and its edge | `Home.tsx`: the `+` text character replaced by the same plus SVG the tab strip draws, at 24px with a 2px stroke, and the edge from `border border-dashed border-border` to `border-2 border-card-edge`, solid on Rotem's word rather than the dashed line it had. `tokens.css`: a new `--color-card-edge: #22242F`, because a raw hex in a component is a bug in this project and no existing token carried that colour. Also tried and put back on Rotem's word, so no net change: the card colour at `#1E1F29`. | Measured in the running app. The tile: 272 by 204, a 2px solid edge on all four sides computing `rgb(34, 36, 47)`, which is `#22242F`, the 13px card radius unchanged, and the icon exactly centred, 0 off on both axes, measuring 24 tall with its stroke rendering 1:1 inside a 24 viewBox. The typed character is gone from the button's text. The pane reports no width of its own, so the tile was measured inside a probe of the real grid, 1180px wide with four 272px columns, and the probe was removed after. Both border rules are in the generated stylesheet, the new edge colour and the accent one the hover still uses. `npm run check` green. Left in place and now inert, worth a word: `text-2xl` on that button, which sized the character that is no longer there. The hover was then changed on his word: the edge no longer turns accent, the tile fills `#16171D` instead, a second new token for the same raw-hex reason, and the button carries the project's own `transition-colors duration-[144ms] ease-out`. The generated stylesheet carries that token and its hover rule, the computed transition reads 0.144s on the colour properties, and no border hover is left in the file. Not verified: the fill itself, since the pane sets the hover state without repainting it. The whole tile also takes a pointer cursor, measured `pointer` on the button and on the icon inside it. The icon's accent-yellow hover was then dropped on his word too, so the fill is the whole hover. Its colour is a third new token, `#59618B`, measured painting `rgb(89, 97, 139)` on both the stroke and the text colour it inherits from. | Pass, the hover is unverified | low | `ecd52d8` | `git checkout ecd52d8 -- src/web/pages/Home.tsx src/web/styles/tokens.css` |
| 2026-09-06 | Deploy the colour pass and the new tile | No code change. `npm run deploy` on Rotem's "DEPLOY": 5 changed assets, 6 already present, version `6d12171d-3282-4f82-b721-87068045a16a`. It carries everything since the last deploy: the `#111217` ground in the app, the splash and the offline page, the `#21222C` cards with their `#252632` hover, and Home's new-project tile with its drawn plus, solid edge, fill hover and pointer cursor. | The live database was checked first and needed nothing: the remote migrations list answered "No migrations to apply", and this change is CSS and one component, no schema. That list failed once with a transient wrangler error and answered cleanly on the retry, which is the same flakiness recorded on 2026-09-04. `npm run check` green before the deploy. After it, from a shell holding no session: the app root, `/api/projects`, the manifest and the offline page all answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public, the manifest included. This deployed the working tree, so the colour pass went live before it was committed. Not checked, and it needs Rotem's own signed-in browser: the colours as rendered live, and the two hovers this pane could never repaint. | Pass, the live look is Rotem's | high | version `d017f296` | `npx wrangler rollback` to version `d017f296-b06d-466f-8f86-e61868776150` |
| 2026-09-06 | Rotem's logo replaces the word Rogers on Home | New `src/web/components/Logo.tsx`: his own `rogers_logo.svg`, which he pointed me at outside the project, copied in as one component. Two things were adapted rather than pasted: every `#E6E7E9` became `currentColor`, so the artwork takes the bar's own text colour and no raw hex sits in a component, and the clip id `clip0_4_17` became `rogers-logo-clip`, since an id must be unique in the whole page and a Figma export's is not. `Home.tsx`: the title `"Rogers"` became the component, inside a 48px slot. `Map.md` lists the new component. | Driven at localhost:5173. The logo renders: 9 shapes, 153 by 34, computing `rgb(230, 231, 233)`, which is the text token, with the accessible name "Rogers" so the app is still named for a screen reader; the clip reference resolves, and no text is left in the header. The 48px slot was a fix inside this change, not a request: swapping 48px of line box for a 34px image had shortened the bar from 80 to 66 and pulled the grid up with it, and with the slot the header measures 80 again and the grid starts at 80, where it did. At 375 wide, resized then reloaded, the logo sits at 18 and ends at 171, well inside the window, with no horizontal overflow. `npm run check` green. Console: passed, known existing error only, the service-worker registration failure inside the browser pane from plan step 7.3. Left alone, since he did not ask: the project and note pages, which have no title of their own, and `public/offline.html`, which still writes Rogers as text. | Pass | low | `fdc60d0` | `git checkout fdc60d0 -- src/web/pages/Home.tsx project-os/Map.md` then delete `src/web/components/Logo.tsx` |
| 2026-09-06 | The bar's top padding, 32 to 56 | `TopBar.tsx`: `pt-8` to `pt-14` on the header, in both of its layouts, the plain one Home and the note page use and the three-column one the project page uses. One component, so both had to move or the two bars would sit at different heights. | Measured on the project page at 1280 wide: the header computes 56px of top padding and stands 104 tall, its 48px row intact, with the tab strip still on one line and no horizontal overflow. Home reads the same 56 and 104, with the logo starting 63 from the top and the grid at 104. `npm run check` green. Two readings were discarded because the browser pane had collapsed to no width between calls, which reports a 0-wide viewport and turns the page's minimum width into a false overflow; the numbers above are the ones taken at a real width. Not re-measured: the note page, which shares the same component and the same two classes. | Pass | low | `fdc60d0` | `git checkout fdc60d0 -- src/web/components/TopBar.tsx`, which also reverts the logo-era hover fade on the back arrow |
| 2026-09-06 | Deploy the logo and the bar spacing | No code change. `npm run deploy` on Rotem's "DEPLOY", version `84d57745-75b0-4f1f-8838-b9e0e2581c89`. It carries the drawn logo on Home and the bar's 56px top padding on every screen. | The live database was checked first and answered "No migrations to apply"; this change is one component and two classes, no schema. `npm run check` green before it. After, from a shell holding no session: the app root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`. This deployed the working tree, so both changes went live before being committed; the commit below closes that. Not checked, and it needs his own signed-in browser: the logo as rendered live. | Pass, the live look is Rotem's | high | version `6d12171d` | `npx wrangler rollback` to version `6d12171d-3282-4f82-b721-87068045a16a` |
| 2026-09-06 | Live render check, the logo | No code change. Closes the open item on the deploy row above, which shipped with the live render unverified. | Rotem, in his own signed-in browser on rogers.rotem-e.com: the logo renders. That settles what a shell with no session could not, since every address behind Access answers the login instead of the page. The failure mode it rules out is the inline artwork not drawing at all, or drawing in the wrong colour, while the build and the local app looked perfect. | Pass | low | `aeb2c59` | none |
| 2026-09-06 | Back arrow size and hover shape | `src/web/components/TopBar.tsx`: the arrow svg `size-6` -> `size-8`, and the link `rounded-card` -> `rounded-full`. | Read the rendered class list for the Back link on a project page; the 48px box, the -8px left margin and the hover colours are unchanged, only the glyph size and the radius. Not driven in a browser: Rotem checks each round himself. | Pass, look is Rotem's | low | 5427bba | revert the two class tokens in TopBar.tsx |
| 2026-09-06 | Composer container matches the note card | `src/web/components/Composer.tsx`: the open composer `bg-surface` -> `bg-card`, the border removed, and the drag state moved from `border-accent` to `ring-2 ring-accent`. | Read the class list either side of the change; the 10px radius, the padding, the raised shadow and the `border-accent` drag state are untouched, so only the fill and the resting hairline moved. Not driven in a browser: Rotem checks each round himself. | Pass, look is Rotem's | low | 5427bba | restore the two class tokens in Composer.tsx |
| 2026-09-07 | Instant lists on navigation | `src/web/lib/useApi.ts`: an opt-in `remember` option keeps the last answer per path in a page-lifetime map and shows it at once while the fetch runs; only the newest run writes it. `src/web/pages/Home.tsx` and `src/web/pages/Project.tsx` opt in for projects, tabs and notes; the note page does not. `Project.tsx`: the notes wait for the tabs only when the address names a tab. | In the running app at localhost:5173, timed with a mutation observer on the page root. Note page, back arrow to Dig: one render at 4ms carrying the composer and all 7 rows, no frame with the composer alone; before, the rows waited two round trips. Project to Home: one render at 9ms with 5 tiles. Edit test: appended a marker to a note body, "Saved", back: the row read the old preview at 9ms and the fresh one at 134ms; reopening the note showed the marker, proving the editor reads the server, not the memory; the marker was then removed and the server body read back empty. Tab switches on Dig, cold and warm, four times: the previous tab's rows were hidden before the new list appeared, never shown under the new name; warm switches landed in 15 to 26ms. Network: every screen still fetches fresh copies on open; requests appear twice in dev because of React strict mode, pre-existing. Console: only the service worker failing to register inside the embedded browser pane; `/sw.js` itself answers 200 as JavaScript, so that is the pane, not the app. `npm run check` green. Review per Code_review.md over the uncommitted diff: no findings introduced; blast radius Shared, the three consumers of the read hook, each driven. | Pass | medium | `910c34d` | `git checkout 910c34d -- src/web/lib/useApi.ts src/web/pages/Home.tsx src/web/pages/Project.tsx` |
| 2026-09-07 | Hover prefetch on the Home tiles | `src/web/lib/useApi.ts`: a `prefetch(path)` that fetches into the same remembered map, skipped when the answer is known or in flight, failures dropped. `src/web/components/ProjectTile.tsx`: the overlay link calls it for the project's tabs and Main's notes on pointer enter and on focus. | In the running app after a full reload, so nothing was remembered: hovering the Dig tile sent exactly the tabs and notes requests for it; a second hover sent none; the click then produced one render at 35ms with the composer and all 7 rows, where the same first visit before this change showed the composer at 10ms and the rows at 156ms. Same on the "First test" tile, whose Main is empty: one render with the empty message, and no later change, so the fresh answer agreed. A synthetic pointerenter did not reach React; pointerover did, which is what a real mouse sends. Console: only the pre-existing service worker failure of the embedded pane. `npm run check` green. Review per Code_review.md over the diff: no findings; reach is the tile and the shared memory map, Local. | Pass | medium | `910c34d` | `git checkout 910c34d -- src/web/components/ProjectTile.tsx` and remove `prefetch` from useApi.ts |
| 2026-09-07 | Small copies of pictures for lists | `src/api/images.ts`: `PUT /api/images/:key/thumb` stores a copy under `thumb/<key>` after checking the key shape, the type, the size and that the original exists; `GET /api/images/:key?size=sm` serves the copy and falls back to the original. `src/web/lib/upload.ts`: `makeThumbnail` draws a WebP with the shorter side at 480px and the longer capped at 1600px, skipped for GIFs and for pictures already that small; it is sent after the original and before the key is returned. `imageSrc(key, size)`. `src/web/components/ImageStrip.tsx`: sm and md ask for the copy, lg and the lightbox the original. | In the running app, in a throwaway project made and then archived by the pass: a drawn 2400 by 1400 PNG of 1.37 MB added through the note page's picker. Requests went upload, then thumb, both 201, then "Saved". `?size=sm` answered image/webp, 46 KB, 823 by 480; without it image/png, 1.37 MB. The note page img src is the original; the project row img src carries `?size=sm` and loaded at 823 by 480 once the pane had width. An older picture with no copy answered `?size=sm` with its PNG original, 200. A traversal key and a copy for a missing image both 404. Found and fixed before the browser pass: the copy route first answered 204 with no body, which `api-client.ts` reads as an expired login and would have reloaded the page mid-upload; it answers JSON now. Console: the pane's service worker failure and my two deliberate 404 probes. `npm run check` green. Review per Code_review.md: the one finding above, fixed; reach is `imageSrc` and its single consumer `ImageStrip`, Local. | Pass | medium | `910c34d` | `git checkout 910c34d -- src/api/images.ts src/web/lib/upload.ts src/web/components/ImageStrip.tsx`; the `thumb/` objects already written are harmless and unread |
| 2026-09-07 | Remembered lists in session storage | `src/web/lib/useApi.ts`: the remembered map is read from `sessionStorage` under `rogers.lists.v1` at boot and written through on every fresh answer, both wrapped in try/catch so a missing or full storage only costs a round trip. The version in the key is bumped when a list's shape changes. | In the running app: after opening Dig, the storage held exactly three paths, the projects, Dig's tabs and Dig's notes, 2.2 KB, and no note path. Then Home was loaded as a fresh document, so the memory started from storage alone, and the Dig tile was clicked with no hover: one render at 14ms carrying the composer, all 7 rows and 3 tab chips, while the tabs and notes answers arrived at 80ms. Paint timing itself could not be read: the embedded pane records no paint entries and hides between calls, so lazy pictures never loaded either; the behavioral proof above stands in. Console: the pane's service worker failure only. `npm run check` green after a rename: the hook's local `remember` flag shadowed a function of the same name and the typecheck caught it. Review per Code_review.md: no findings; reach is the read hook alone, Local. | Pass | medium | `910c34d` | drop `readStored` and `store` from useApi.ts, or clear `sessionStorage` key `rogers.lists.v1` to reset a browser |
| 2026-09-07 | Prefetch the other tabs on open | `src/web/pages/Project.tsx`: an effect on the tabs answer calls `prefetch` for every tab that is not the open one, and for Main when a tab is open. | In the running app with the session storage cleared and Dig opened cold: after the tabs answered at 172ms, both tab lists were requested once at 185ms and answered at 225ms, and the storage then held five paths. The first click on a tab hid the old list at 11ms and showed the tab's state from memory at 17ms; its refresh answered at 53ms and changed nothing. Before this change the same first switch waited a full round trip, measured earlier today at 65ms. Console: the pane's service worker failure only. `npm run check` green. Review per Code_review.md: no findings; reach is the project page alone, Local. | Pass | medium | `910c34d` | remove the prefetch effect from Project.tsx |
| 2026-09-07 | Lists into local storage | `src/web/lib/useApi.ts`: `sessionStorage` -> `localStorage`, same key `rogers.lists.v1`, comment records the owner's choice and its cost. | In the running app after a reload: `localStorage` holds the lists under the key, and a fresh Home paints from it. The behavioral proof from the session-storage row holds unchanged, since only the store moved. `npm run check` green. Review per Code_review.md: no findings; reach is the read hook, Local. Trade-off recorded in Decisions. | Pass | medium | `910c34d` | put `sessionStorage` back on the two lines |
| 2026-09-07 | Backfill the small copies, and stop caching the stand-in | `src/api/images.ts`: `GET /api/thumbs/missing` lists the bucket and answers the non-GIF originals with no `thumb/` copy; the serve route marks an original standing in for a missing copy `private, no-cache` instead of a year immutable. `src/shared/types.ts`: `MissingThumbs`. `src/web/lib/upload.ts`: `backfillThumbnails` asks that list and, one picture at a time, downloads the original, draws the copy and sends it through the shared `putThumbnail`; `makeThumbnail` takes a Blob and returns a small picture as its own copy so nothing is asked for twice. `src/web/App.tsx`: starts it 3 seconds after the app opens. | In the running app: the first page load after the change backfilled every original in the local bucket before my probe could even read the list; the bucket's own metadata table shows a `thumb/` row for all 14 originals, the 226 KB PNG's copy 16 KB. That first run exposed a flaw, found and fixed: the browser had kept the full PNG under the `?size=sm` address, immutable, from a probe made before the copy existed; the stand-in now answers `private, no-cache`, read back as such on a fresh raw upload, while a real copy still answers immutable. Live run on that raw 1200 by 800 upload: it was the one key in the missing list; after a reload the log shows the list at 3.8 s, the download, the copy sent, the list then empty, and `?size=sm` a 4 KB WebP at 720 by 480. Console: the pane's service worker failure only. `npm run check` green. Review per Code_review.md: the cache flaw above, fixed; the one raw `fetch` outside the API client is for bytes, guarded by a content-type check, and noted in code; reach is the image routes and the app root, Local. Two test originals from the pass remain in the local dev bucket, unreferenced. | Pass | medium | `910c34d` | remove the route, the routine and the App effect; the copies already stored keep serving |
| 2026-09-07 | Deploy the navigation batch | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `0111a26`; version `442c6ceb-fab9-479d-8e7f-51f60e03e01c`. | The live database answered "No migrations to apply" first; the batch has no schema change. `npm run check` green before the commit. After, from a shell holding no session, four addresses answered 302 to `roteme-pages.cloudflareaccess.com`: the root, the projects list, the new missing-copies route, and a picture asked for at the small size; so the new route and the small size sit behind Access like everything else. Not checked, and it needs his own signed-in browser: the live backfill run and the first instant paint. | Pass, the live run is Rotem's | high | version `84d57745` | `npx wrangler rollback` to version `84d57745-75b0-4f1f-8838-b9e0e2581c89` |
| 2026-09-07 | Rotem's new logo | `src/web/components/Logo.tsx`: the artwork replaced with `rogers_logo.svg` from his logo folder. The viewBox 153 -> 154, the mark centred at 17 instead of 16, the inner ring r10.5 at stroke 1 -> r11 at stroke 2, a second triangle under the first, and the designer's clip dropped, since nothing crosses the box any more. Two substitutions only, the same rule as the first logo: `#E6E7E9` -> `currentColor`, `#2D41D7` -> `var(--color-brand)`. `tokens.css`: that token is new, `--color-brand: #2d41d7`, because a raw hex in a component is a bug here. | The eight path `d` values were diffed against his file and are identical; the two circles differ only by the colour swaps. The component's own markup was then rendered in a browser on the bar's colours, at 34px and at 170px: blue disc, light outer and inner rings, the two-tone triangle, the wordmark reading Rogers. The token survives the build, read back as `--color-brand:#2d41d7` in `dist/client/assets/index-*.css`; that was the real risk, a theme variable pruned as unused and the disc resolving to no fill. Nothing else references the old clip id. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's top bar was never opened, and starting a server is Rotem's (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, the Logo component has one consumer, Home's top bar, and the token is new so nothing else reads it. | Pass, the app itself unopened | medium | `9acee4e` | `git checkout 9acee4e -- src/web/components/Logo.tsx src/web/styles/tokens.css` |
| 2026-09-07 | The mark as the tab and app icon | `public/icons/icon.svg`: his `rogers_logo_icon.svg` copied in byte for byte. `index.html`: the tab now asks for that SVG first, with the existing 192 PNG left underneath as the fallback for browsers that refuse one. `public/icons/icon-192.png` and `icon-512.png`: redrawn from the same SVG onto the `#17181a` square they already used, the mark filling 80% of the canvas, which is the safe circle a maskable icon gets masked to. They replace the yellow ring placeholder from setup. The manifest is untouched, since neither file name moved. | The rasterised PNG was compared side by side against the same SVG drawn by a browser at the same size: same structure, same two colours. Both files read back at 192 and 512 with the dark square opaque into the corners. `npm run check` green, and the build now carries 15 assets instead of 14, the added SVG. NOT checked: the tab icon and the installed app icon in a real browser, which need the running app; a Windows install also keeps its old icon until it is reinstalled. Review per Code_review.md over this diff: no findings; reach is Local, the two PNG names are unchanged so the manifest and the apple-touch link keep working, and the SVG's own clip id cannot collide from a separate file. | Pass, the tab unopened | medium | `9acee4e` | `git checkout 9acee4e -- index.html public/icons/icon-192.png public/icons/icon-512.png` then delete `public/icons/icon.svg` |
| 2026-09-07 | Deploy the new mark | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `03c09a5`; version `78d99fef-6e34-4d34-b18d-3bb7c8f09443`, 6 assets changed. | No migration check needed, since this batch is one component, one token, one meta line and three icon files, with no schema in it. `npm run check` green before the commit. After the deploy, from a shell holding no session, three addresses answered 302 to `roteme-pages.cloudflareaccess.com`: the root, the new `/icons/icon.svg` and `/icons/icon-512.png`, so the icons did not become public. Then in Rotem's own signed-in Chrome: Home renders with the new mark in the bar, blue disc and light rings, beside the wordmark, which is what a shell with no session can never show; the mark is proof the `--color-brand` token survived into the live stylesheet. `/icons/icon.svg` was opened live and drew the mark. Worth his verdict, not fixed: on that white page the light outer ring disappears into the background, so in a light browser tab strip the icon reads as a blue disc rather than a ringed mark. Not checked: the installed Windows app icon, which only changes when the app is reinstalled. | Pass | high | version `442c6ceb` | `npx wrangler rollback` to version `442c6ceb-fab9-479d-8e7f-51f60e03e01c` |
| 2026-09-07 | The redrawn mark, logo and icon | `src/web/components/Logo.tsx`: his new `rogers_logo.svg`. The viewBox back to 153, the mark centred at 16 again, the inner ring and the two-tone triangle gone, replaced by one rounded triangle and a diagonal line, both at stroke 3. The wordmark returns to its original x positions. Same two substitutions as before: `#E6E7E9` -> `currentColor`, the disc fill `#2D41D7` -> `var(--color-brand)`. `public/icons/icon.svg`: his new `rogers_logo_icon.svg`, copied byte for byte. `public/icons/icon-192.png` and `icon-512.png`: redrawn from it onto the same `#17181a` square, mark at 80% of the canvas. `index.html` and the manifest untouched, since no file name moved. | The eight path `d` values were diffed against his logo file and are identical; `icon.svg` is byte identical to his icon file. The component markup was rendered in a browser on the bar's colours at 34px and at 120px, and the 512 and 192 PNGs beside it: blue disc, one light ring, the triangle with its diagonal, the wordmark reading Rogers, and the 32px version still legible. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's bar was never opened; the server is Rotem's to start (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, the Logo component has one consumer, Home's top bar, the two PNG names did not move, and `--color-brand` is still read only by this component. | Pass, the app itself unopened | medium | `275dbee` | `git checkout 275dbee -- src/web/components/Logo.tsx public/icons/` |
| 2026-09-07 | Deploy the redrawn mark | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `0a48fb0`; version `ab2297db-076d-443e-8320-bb6b0a02205b`. | No migration check needed: one component and three icon files, no schema. `npm run check` green before the commit. After the deploy, from a shell holding no session, the root and `/icons/icon.svg` both answered 302, so nothing became public. Then in Rotem's own signed-in Chrome: Home renders the redrawn mark in the bar, blue disc, one ring, the triangle with its diagonal, beside the wordmark. Not checked, and it is his to do: the installed Windows app, which keeps its old icon until he uninstalls and installs it again. | Pass | high | version `78d99fef` | `npx wrangler rollback` to version `78d99fef-6e34-4d34-b18d-3bb7c8f09443` |
| 2026-09-07 | The mark, third pass | `src/web/components/Logo.tsx`: his newest `rogers_logo.svg`. The ring r15 at stroke 2 -> r14.5 at stroke 3, the triangle lowered, its top edge from y8 to y9 and its point from 26.7 to 25.75, and the diagonal unchanged. His file wraps the mark in a clip again, so the group and the `clipPath` come back, with the id namespaced to `rogers-logo-clip` because an id must be unique in the whole page and a Figma export's is not. The wordmark is untouched. Same two substitutions: `#E6E7E9` -> `currentColor`, the disc `#2D41D7` -> `var(--color-brand)`. `public/icons/icon.svg`: his new icon file, byte for byte. `icon-192.png` and `icon-512.png`: redrawn from it, same dark square, same 80% of the canvas. | The eight path `d` values were diffed against his logo file and are identical, and `icon.svg` is byte identical to his icon file. Rendered in a browser on the bar's colours at 34px and 120px with both PNGs beside them: thicker light ring, the triangle with its diagonal, blue disc, the wordmark reading Rogers, and the 32px copy still legible. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's bar was never opened; the server is Rotem's (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, one consumer, Home's top bar, the icon file names did not move, and the clip id appears nowhere else. | Pass, the app itself unopened | medium | `fb36e09` | `git checkout fb36e09 -- src/web/components/Logo.tsx public/icons/` |
