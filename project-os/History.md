# Rogers — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate-history.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate-history.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-04 | app | **The Worker and the app now agree on what a project and a note look like.** One file describes every answer and every request, so the two halves cannot drift apart. Archived notes are impossible to send by accident. |
| 2026-09-04 | api | **Projects can be created and listed.** Each one reports how many live notes it holds, counted in the same query, and archived notes are never counted. Every failure now answers in the same shape, which stopped a server error from looking like being signed out. |
| 2026-09-04 | api | **A project can be renamed, recoloured and put away.** Putting one away hides it but keeps every word of it, and its notes stay whole underneath. Renaming no longer risks wiping the colour. |
| 2026-09-04 | process | **Work now runs step after step without stopping for approval.** Rotem lifted the per-step gate; the assistant stops only for a real problem, for something only he can do, or before anything that reaches the public. |
| 2026-09-04 | api | **Notes can be added to a project and listed back in Keep's order.** Pinned notes come first, then the newest, and a long note sends only its opening to the list. Hebrew and English together survive the round trip intact. |
| 2026-09-04 | api | **A note can be opened, saved, pinned and put away.** Saving one part of a note never disturbs the other parts, which is the promise that autosave depends on. Putting a note away keeps every word and every picture. |
| 2026-09-04 | api | **A lost connection can no longer turn a saved thing into an error.** Sending the same create twice now answers with what is already there instead of failing, and every bad request is answered clearly rather than crashing. |
| 2026-09-04 | tooling | **A blank screen turned out to be the dev server, not the code.** A server left running across dependency installs serves mismatched copies of React and blanks the page while the build stays green. Restarting it fixes it, and the symptom is now written down. |
| 2026-09-04 | app | **Home shows your projects and makes new ones.** Each tile carries its name and how many notes are in it. The plus tile turns into a name field where the new tile will appear, so making a project is one click and one line. |
| 2026-09-04 | app | **A project tile can be renamed, given a colour, or put away.** The menu appears on the tile itself, renaming happens in place, and putting a project away hides it without losing anything. |
| 2026-09-04 | app | **A project opens as a list of its notes, newest first.** Each note is a full-width row showing its title and the start of its text, and the project can be renamed from its own header. A pasted link no longer runs off the edge of a row. |
| 2026-09-04 | app | **Notes can be written, from the project page and on their own page, and nothing typed gets lost.** The composer opens on a click, saves from the first keystroke, and discards an empty draft. The note page saves as you type, on blur, on the way back, when the tab closes, and even when the login expires mid-sentence. |
| 2026-09-04 | app | **The project page reads like Keep now.** Pinned notes sit in their own section above the rest, a pin and a menu appear when a row is hovered, N starts a note, Escape leaves one, and loading shows the shape of what is coming instead of a word. |
| 2026-09-04 | data | **Rogers has somewhere to keep pictures.** The image bucket exists on Cloudflare and the Worker is wired to it; nothing is stored in it yet. |
| 2026-09-04 | api | **Pictures can be stored and shown.** An upload answers with a key a note can keep, and the picture is served back by that key with long caching, so a revisit costs a header exchange rather than a download. |
| 2026-09-04 | app | **Pictures go into notes by paste, by drop, or through a button, and show as thumbnails.** A pasted screenshot becomes part of the note at once, rows show small thumbnails, a click opens the picture full size, and a picture can be removed. |
| 2026-09-04 | deploy | **Rogers is on the internet.** Live at rogers.rotem914.workers.dev, wired to the real database and bucket, both still empty. Public until the login is switched on, which is the very next step. |
| 2026-09-04 | auth | **Rogers is behind a login.** Every address, the pages, the API and the pictures, now sends a stranger to a Cloudflare sign-in page; only members of Rotem's Cloudflare account get in, with an email code, for 7 days at a time. |
| 2026-09-04 | app | **Rogers can be installed as a Windows app from the browser.** It has its own icon and window, and opening it with no network shows a dark Rogers page instead of a browser error. |
| 2026-09-04 | done | **V1 is finished.** Rotem signed in through the email code and used Rogers on its live address; every phase of the plan from 0 to 7 is ticked. |
| 2026-09-04 | app | **Projects can be dragged into any order on Home.** The whole tile is the handle, with no grip to find, the grid rearranges under the mouse, and the order is saved and survives a reload. A database that has never been reordered keeps its creation order exactly. |
| 2026-09-04 | process | **A command in a reply now carries its full path.** Telling Rotem to run something "in the project folder" is no longer allowed; the command has to work pasted into any terminal. |
| 2026-09-04 | data | **The live database can hold a project order too.** Both copies now carry the same column, so dragging tiles will work on the live app the moment it is deployed. Nothing visible changed yet; the deployed code does not read the column. |
| 2026-09-04 | app | **Notes can be dragged into any order inside a project.** Each section is arranged on its own, so a row cannot be pinned by dropping it; the pin button still does that. A new note still arrives at the top, and pinning still puts a note on top of Pinned. |
| 2026-09-04 | deploy | **Both drag orders are live.** Projects on Home and notes inside a project can be dragged on rogers.rotem914.workers.dev, and the login still stands in front of everything. |
| 2026-09-04 | done | **Rotem confirmed both drags work on the live app.** The live check the deploy row was waiting on. |
| 2026-09-04 | app | **The composer opens with the caret in the title.** Rotem's own edit; it used to open in the body the way Keep does. |
| 2026-09-04 | app | **Correction: that title focus was my change, and it was checked.** The row above credits Rotem and records it as unchecked. It was made in this session, on his request, and driven in the browser. |
| 2026-09-04 | app | **The composer's leftover body-focus handle is gone.** Dead since the caret moved to the title. Nothing a person can see changed. |
| 2026-09-05 | app | **No more placeholder rows while a project or a note opens.** The load is instant, so the grey shapes only flickered. Home still has its own. |
| 2026-09-05 | app | **Home's tile placeholders are gone too, and the placeholder components with them.** `Feedback.tsx` is now the toast alone. |
| 2026-09-05 | design | **Every screen is set in Google Sans.** Loaded from Google Fonts with its Hebrew alphabet, so a note that mixes Hebrew and English stays in one typeface. The note field also remeasures itself once the font lands, so a first visit cannot hide the last line of a note. |
| 2026-09-05 | design | **Header, note page and Home re-laid out to Rotem's own measurements.** The top bar lost its background and border and now shares a 1168px container with the page, its back arrow is a 24px icon in a 48px box, a note's title moved out of the bar into the 720px text column while its pictures run the full width, and Home's tiles are a fixed 272px in a 1152px grid. |
| 2026-09-05 | design | **Google Sans is served by Rogers itself now, not by Google.** No page load calls out any more, and the licence ships beside the font files as the licence requires. Latin, Latin extended and Hebrew are covered; any other alphabet falls back to the system font. |
| 2026-09-05 | deploy | **Everything waiting is live.** Google Sans served by Rogers itself, the loading placeholders gone, and another session's header, note page and Home layout work, all in one deploy. The login still stands in front of every address, the font files included. |
| 2026-09-05 | done | **Google Sans really does render on the live site.** Rotem checked it signed in; the font files serve through the login, which was the one thing that could only be seen there. |
| 2026-09-05 | deploy | **Rogers has its own address, rogers.rotem-e.com, because mobile carriers block workers.dev.** Rotem's iPhone hung on the old address over 4G, and so did Cloudflare's own workers.dev demo page, so the fault was the domain family, not Rogers. The login covers the new address; the old workers.dev address went dark as a side effect, pending his verdict. |
| 2026-09-05 | done | **Rogers opens on the iPhone over 4G and sits on its Home Screen as an app.** Rotem checked the new address on the phone; the projects grid loaded, and Add to Home Screen gave it an icon and its own window, which is what the workers.dev block had been hiding. |
| 2026-09-05 | deploy | **The old workers.dev address is retired on purpose.** Rotem's verdict on the side effect: it stays dark, and the config now says so explicitly instead of by omission. |
| 2026-09-05 | design | **Home is laid out to Rotem's mobile and desktop measurements, and the note count is gone from the tiles.** Cards round at 13px and pad at 24px, the grid gaps 20px inside a 1180px column with 72px of desktop breathing room, breakpoint A at 900px lets tiles stretch and breakpoint B at 430px stacks them one per row at 144px tall. Clicking off the new-project card now saves the name instead of losing it, and tile, note row and "Take a note" fade their hover over 144ms. |
| 2026-09-05 | design | **A note row's picture thumbnails are 216px instead of 48px.** Only the row size changed; the composer's 96px thumbnails and the note page's full-width pictures are untouched. |
| 2026-09-05 | app | **Undo and redo, for text and pictures, on the note page and in the composer.** Ctrl+Z, Ctrl+Shift+Z or Ctrl+Y, Cmd on a Mac. Alongside it the note row got 16px top and bottom, its pictures a 16px gap, the note page pictures a 24px gap, and the remove-picture button is a 32px circle with a 24px cross. |
| 2026-09-06 | design | **The page ground is #14141A and the three main cards are #23242F with no hairline.** The project tile, the note row and the collapsed composer lost their border and took the new card colour; the top bar, menus, the expanded composer and the thumbnails keep `surface` and their borders. |
| 2026-09-06 | design | **The three cards hover at #2B2D3B.** The project tile, the note row and the "Take a note" button use their own hover colour now instead of the shared `surface-hover`; everything else still hovers at `#2a2b2f`. |
| 2026-09-06 | app | **A project can be split into tabs, each with its own list of notes.** A plus above "Take a note…" makes the first one: the project's own list becomes a tab called Main and "New tab" opens beside it, empty. A note typed in a tab stays in it, the back arrow from a note returns to its tab, and removing a tab sends its notes back to Main. The browser pass is still owed: the dev server was down. |
| 2026-09-06 | app | **Tabs checked in the browser, and a tab can be renamed.** The open tab's name is a field on click: Enter or a click outside saves, Escape discards, Hebrew survives. The pass also caught my own guard against a stale list failing, and its replacement holds: switching tabs never shows the last tab's notes under the new name. |
| 2026-09-06 | design | **The tabs moved up beside the project name, and the strip got its shape.** The project name now starts exactly where Rogers starts on Home, with the back arrow hanging out of the flow to its left. Tabs are 18px text in a 39px chip, the plus is a round 39px button that only appears on hover, removing a tab is on its right-click menu, and Main can be renamed like any other tab. |
| 2026-09-06 | design | **Rotem's own two edits: the open composer and the back arrow.** The expanded composer lost its hairline for the card colour, and shows an accent ring while a picture is dragged onto it. The back arrow is a full circle. |
| 2026-09-06 | design | **The page ground is #111217, everywhere the app paints a ground.** The token, both copies in the page shell, the installed app's splash and the offline page all moved together, so the first frame and the launch screen are the same dark as the app. |
| 2026-09-06 | design | **Every card is #21222C, and hovers at #252632.** One token pair carries the project tile, the note row, the composer open and closed, and the open tab, so they all moved together. |
| 2026-09-06 | design | **Home's new-project tile draws the same plus the tabs use, inside a solid 2px edge.** The typed plus character is gone, so one drawn shape now means add in both places. Hovering it fills the tile instead of lighting its edge, over the same 144ms the other cards use. |
| 2026-09-06 | deploy | **The logo and the taller bar are live.** Version `84d57745`, no schema involved. |
| 2026-09-06 | done | **The logo really does render on the live site.** Rotem checked it signed in, which is the one thing a shell with no session could not answer. |
| 2026-09-06 | design | **The bar sits 56px from the top of the window instead of 32.** Every screen moved together, so the content below each one starts 24px lower. |
| 2026-09-06 | design | **Home's title is Rotem's logo now, drawn, not typed.** The mark and the wordmark take the bar's own text colour, and the bar kept its height, so nothing below it moved. |
| 2026-09-06 | deploy | **The colour pass and the new-project tile are live.** The darker ground, the card colours, the splash and offline page, and the tile with its drawn plus, all in one deploy, version `6d12171d`. |
| 2026-09-06 | deploy | **Tabs are live at rogers.rotem-e.com.** The live database got the tabs table and the note column first, so the new list query had something to read, then the deploy, version `e1ac5d1f`. The login still fronts every address, the new tabs route included. |
| 2026-09-06 | design | **The back arrow is a 32px icon in a fully round hover.** Only the top bar arrow changed: the glyph went from 24px to 32px inside the same 48px hit area, and its hover shape went from the 13px card radius to a full circle. |
| 2026-09-06 | design | **The open composer sits on the same card as the collapsed one.** It takes `bg-card` instead of `bg-surface` and has no border at all; the picture drag now shows a 2px accent ring instead of an accent border. |
| 2026-09-07 | app | **Going back to a list is instant.** Home, a project and its tabs show the list they last showed the moment they open, then quietly refresh it, so returning from a note no longer shows "Take a note" alone and the notes a beat later. The note editor is deliberately not remembered: it always opens from the server copy. On a first visit Main's notes are fetched beside the tabs instead of after them. |
| 2026-09-07 | app | **A project opens with its list already there on the first visit too.** Hovering or focusing a tile on Home fetches that project's tabs and Main's notes ahead of the click, once, so the project page paints in one piece even when nothing was remembered yet. |
| 2026-09-07 | app | **A note row shows a small copy of each picture instead of the original.** When a picture is added, the browser draws a WebP copy no taller than 480px and stores it beside the original; the row and the composer ask for that copy and get the original where none exists, so pictures added before today still show, at their old cost. The note page and the lightbox always show the original. |
| 2026-09-07 | app | **The remembered lists survive a reload.** Home, a project and its tabs keep their last lists in the tab's session storage, so a reload, or the trip through the login when the session expires, paints the lists at once and refreshes them behind. Session storage on purpose: it dies with the tab, so a preview of a private note never outlives the window. The note itself is still never stored. |
| 2026-09-07 | app | **The first switch to a tab is instant.** Opening a project fetches the lists of its other tabs, and Main's when a tab is open, right after the tabs are known, so switching paints from memory and refreshes behind. Each list is asked for once per page; the switch itself keeps it fresh. |
| 2026-09-07 | app | **The remembered lists now live in the browser's long-term memory.** A new tab, a restarted browser and a reload all paint the lists at once. Rotem's call, knowing the cost: titles and previews stay on the machine until Rogers overwrites them, logged out or not. |
| 2026-09-07 | app | **Old pictures get their small copies by themselves.** A few seconds after Rogers opens, the browser asks which pictures have no copy, makes each one and stores it, one at a time; once all have one, the check costs one request. A picture standing in for a missing copy is no longer kept by the browser for a year, so the copy takes over as soon as it exists. |
| 2026-09-07 | deploy | **Instant navigation and small picture copies are live at rogers.rotem-e.com.** The commit `0111a26` deployed as version `442c6ceb`; the backfill of old pictures runs the first time Rotem opens the live app, a few seconds in. |
| 2026-09-07 | design | **The logo is Rotem's new two-colour mark.** The disc is brand blue behind the light rings and the wordmark, which still take the bar's own text colour. |
| 2026-09-07 | design | **The browser tab and the installed app carry the mark.** The tab draws the mark itself at whatever size it needs, and the installed app icon replaces the yellow ring left over from setup. |
| 2026-09-07 | deploy | **The new mark is live at rogers.rotem-e.com.** The logo renders in Rotem's own signed-in browser, and the tab icon is served from the same address. |
| 2026-09-07 | design | **The mark is Rotem's redrawn version.** One ring instead of two, a rounded triangle with a diagonal cut through it, and the same artwork now serves as the tab and app icon. |
| 2026-09-07 | deploy | **The redrawn mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser; the installed Windows app still needs a reinstall to take the new icon. |
| 2026-09-07 | design | **The mark got a thicker ring and a lower triangle.** Rotem's next pass on the same artwork, in the logo and in the tab and app icons together. |
| 2026-09-07 | deploy | **The third pass of the mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser. |
| 2026-09-08 | design | **The mark's weights were tuned again.** A thinner ring, a slightly wider disc and a heavier triangle, in the logo and in the tab and app icons together. |
| 2026-09-08 | deploy | **The tuned mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser. |
| 2026-09-08 | ui | **A tab chip is clickable across its whole box.** Clicking anywhere on a tab, its padding included, opens it; before, only the letters themselves answered. |
| 2026-09-08 | deploy | **The clickable tab chip is live at rogers.rotem-e.com.** Rotem confirmed it in his own running app before the deploy; the live copy itself still answers the login to anyone without a session. |
| 2026-09-08 | app | **A project list comes back where you left it, but only from a note.** Scrolling well down, opening a note and pressing back now lands on the same row instead of at the top, and every tab keeps its own place. Opening the project from Home starts at the top, Rotem's call; so does a reload. |
| 2026-09-08 | deploy | **The remembered scroll place is live at rogers.rotem-e.com.** Coming back from a note lands on the same row; opening a project from Home still starts at the top. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate-history.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-07 | Hover prefetch on the Home tiles | `src/web/lib/useApi.ts`: a `prefetch(path)` that fetches into the same remembered map, skipped when the answer is known or in flight, failures dropped. `src/web/components/ProjectTile.tsx`: the overlay link calls it for the project's tabs and Main's notes on pointer enter and on focus. | In the running app after a full reload, so nothing was remembered: hovering the Dig tile sent exactly the tabs and notes requests for it; a second hover sent none; the click then produced one render at 35ms with the composer and all 7 rows, where the same first visit before this change showed the composer at 10ms and the rows at 156ms. Same on the "First test" tile, whose Main is empty: one render with the empty message, and no later change, so the fresh answer agreed. A synthetic pointerenter did not reach React; pointerover did, which is what a real mouse sends. Console: only the pre-existing service worker failure of the embedded pane. `npm run check` green. Review per Code_review.md over the diff: no findings; reach is the tile and the shared memory map, Local. | Pass | medium | `910c34d` | `git checkout 910c34d -- src/web/components/ProjectTile.tsx` and remove `prefetch` from useApi.ts |
| 2026-09-07 | Small copies of pictures for lists | `src/api/images.ts`: `PUT /api/images/:key/thumb` stores a copy under `thumb/<key>` after checking the key shape, the type, the size and that the original exists; `GET /api/images/:key?size=sm` serves the copy and falls back to the original. `src/web/lib/upload.ts`: `makeThumbnail` draws a WebP with the shorter side at 480px and the longer capped at 1600px, skipped for GIFs and for pictures already that small; it is sent after the original and before the key is returned. `imageSrc(key, size)`. `src/web/components/ImageStrip.tsx`: sm and md ask for the copy, lg and the lightbox the original. | In the running app, in a throwaway project made and then archived by the pass: a drawn 2400 by 1400 PNG of 1.37 MB added through the note page's picker. Requests went upload, then thumb, both 201, then "Saved". `?size=sm` answered image/webp, 46 KB, 823 by 480; without it image/png, 1.37 MB. The note page img src is the original; the project row img src carries `?size=sm` and loaded at 823 by 480 once the pane had width. An older picture with no copy answered `?size=sm` with its PNG original, 200. A traversal key and a copy for a missing image both 404. Found and fixed before the browser pass: the copy route first answered 204 with no body, which `api-client.ts` reads as an expired login and would have reloaded the page mid-upload; it answers JSON now. Console: the pane's service worker failure and my two deliberate 404 probes. `npm run check` green. Review per Code_review.md: the one finding above, fixed; reach is `imageSrc` and its single consumer `ImageStrip`, Local. | Pass | medium | `910c34d` | `git checkout 910c34d -- src/api/images.ts src/web/lib/upload.ts src/web/components/ImageStrip.tsx`; the `thumb/` objects already written are harmless and unread |
| 2026-09-07 | Remembered lists in session storage | `src/web/lib/useApi.ts`: the remembered map is read from `sessionStorage` under `rogers.lists.v1` at boot and written through on every fresh answer, both wrapped in try/catch so a missing or full storage only costs a round trip. The version in the key is bumped when a list's shape changes. | In the running app: after opening Dig, the storage held exactly three paths, the projects, Dig's tabs and Dig's notes, 2.2 KB, and no note path. Then Home was loaded as a fresh document, so the memory started from storage alone, and the Dig tile was clicked with no hover: one render at 14ms carrying the composer, all 7 rows and 3 tab chips, while the tabs and notes answers arrived at 80ms. Paint timing itself could not be read: the embedded pane records no paint entries and hides between calls, so lazy pictures never loaded either; the behavioral proof above stands in. Console: the pane's service worker failure only. `npm run check` green after a rename: the hook's local `remember` flag shadowed a function of the same name and the typecheck caught it. Review per Code_review.md: no findings; reach is the read hook alone, Local. | Pass | medium | `910c34d` | drop `readStored` and `store` from useApi.ts, or clear `sessionStorage` key `rogers.lists.v1` to reset a browser |
| 2026-09-07 | Prefetch the other tabs on open | `src/web/pages/Project.tsx`: an effect on the tabs answer calls `prefetch` for every tab that is not the open one, and for Main when a tab is open. | In the running app with the session storage cleared and Dig opened cold: after the tabs answered at 172ms, both tab lists were requested once at 185ms and answered at 225ms, and the storage then held five paths. The first click on a tab hid the old list at 11ms and showed the tab's state from memory at 17ms; its refresh answered at 53ms and changed nothing. Before this change the same first switch waited a full round trip, measured earlier today at 65ms. Console: the pane's service worker failure only. `npm run check` green. Review per Code_review.md: no findings; reach is the project page alone, Local. | Pass | medium | `910c34d` | remove the prefetch effect from Project.tsx |
| 2026-09-07 | Lists into local storage | `src/web/lib/useApi.ts`: `sessionStorage` -> `localStorage`, same key `rogers.lists.v1`, comment records the owner's choice and its cost. | In the running app after a reload: `localStorage` holds the lists under the key, and a fresh Home paints from it. The behavioral proof from the session-storage row holds unchanged, since only the store moved. `npm run check` green. Review per Code_review.md: no findings; reach is the read hook, Local. Trade-off recorded in Decisions. | Pass | medium | `910c34d` | put `sessionStorage` back on the two lines |
| 2026-09-07 | Backfill the small copies, and stop caching the stand-in | `src/api/images.ts`: `GET /api/thumbs/missing` lists the bucket and answers the non-GIF originals with no `thumb/` copy; the serve route marks an original standing in for a missing copy `private, no-cache` instead of a year immutable. `src/shared/types.ts`: `MissingThumbs`. `src/web/lib/upload.ts`: `backfillThumbnails` asks that list and, one picture at a time, downloads the original, draws the copy and sends it through the shared `putThumbnail`; `makeThumbnail` takes a Blob and returns a small picture as its own copy so nothing is asked for twice. `src/web/App.tsx`: starts it 3 seconds after the app opens. | In the running app: the first page load after the change backfilled every original in the local bucket before my probe could even read the list; the bucket's own metadata table shows a `thumb/` row for all 14 originals, the 226 KB PNG's copy 16 KB. That first run exposed a flaw, found and fixed: the browser had kept the full PNG under the `?size=sm` address, immutable, from a probe made before the copy existed; the stand-in now answers `private, no-cache`, read back as such on a fresh raw upload, while a real copy still answers immutable. Live run on that raw 1200 by 800 upload: it was the one key in the missing list; after a reload the log shows the list at 3.8 s, the download, the copy sent, the list then empty, and `?size=sm` a 4 KB WebP at 720 by 480. Console: the pane's service worker failure only. `npm run check` green. Review per Code_review.md: the cache flaw above, fixed; the one raw `fetch` outside the API client is for bytes, guarded by a content-type check, and noted in code; reach is the image routes and the app root, Local. Two test originals from the pass remain in the local dev bucket, unreferenced. | Pass | medium | `910c34d` | remove the route, the routine and the App effect; the copies already stored keep serving |
| 2026-09-07 | Deploy the navigation batch | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `0111a26`; version `442c6ceb-fab9-479d-8e7f-51f60e03e01c`. | The live database answered "No migrations to apply" first; the batch has no schema change. `npm run check` green before the commit. After, from a shell holding no session, four addresses answered 302 to `roteme-pages.cloudflareaccess.com`: the root, the projects list, the new missing-copies route, and a picture asked for at the small size; so the new route and the small size sit behind Access like everything else. Not checked, and it needs his own signed-in browser: the live backfill run and the first instant paint. | Pass, the live run is Rotem's | high | version `84d57745` | `npx wrangler rollback` to version `84d57745-75b0-4f1f-8838-b9e0e2581c89` |
| 2026-09-07 | Rotem's new logo | `src/web/components/Logo.tsx`: the artwork replaced with `rogers_logo.svg` from his logo folder. The viewBox 153 -> 154, the mark centred at 17 instead of 16, the inner ring r10.5 at stroke 1 -> r11 at stroke 2, a second triangle under the first, and the designer's clip dropped, since nothing crosses the box any more. Two substitutions only, the same rule as the first logo: `#E6E7E9` -> `currentColor`, `#2D41D7` -> `var(--color-brand)`. `tokens.css`: that token is new, `--color-brand: #2d41d7`, because a raw hex in a component is a bug here. | The eight path `d` values were diffed against his file and are identical; the two circles differ only by the colour swaps. The component's own markup was then rendered in a browser on the bar's colours, at 34px and at 170px: blue disc, light outer and inner rings, the two-tone triangle, the wordmark reading Rogers. The token survives the build, read back as `--color-brand:#2d41d7` in `dist/client/assets/index-*.css`; that was the real risk, a theme variable pruned as unused and the disc resolving to no fill. Nothing else references the old clip id. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's top bar was never opened, and starting a server is Rotem's (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, the Logo component has one consumer, Home's top bar, and the token is new so nothing else reads it. | Pass, the app itself unopened | medium | `9acee4e` | `git checkout 9acee4e -- src/web/components/Logo.tsx src/web/styles/tokens.css` |
| 2026-09-07 | The mark as the tab and app icon | `public/icons/icon.svg`: his `rogers_logo_icon.svg` copied in byte for byte. `index.html`: the tab now asks for that SVG first, with the existing 192 PNG left underneath as the fallback for browsers that refuse one. `public/icons/icon-192.png` and `icon-512.png`: redrawn from the same SVG onto the `#17181a` square they already used, the mark filling 80% of the canvas, which is the safe circle a maskable icon gets masked to. They replace the yellow ring placeholder from setup. The manifest is untouched, since neither file name moved. | The rasterised PNG was compared side by side against the same SVG drawn by a browser at the same size: same structure, same two colours. Both files read back at 192 and 512 with the dark square opaque into the corners. `npm run check` green, and the build now carries 15 assets instead of 14, the added SVG. NOT checked: the tab icon and the installed app icon in a real browser, which need the running app; a Windows install also keeps its old icon until it is reinstalled. Review per Code_review.md over this diff: no findings; reach is Local, the two PNG names are unchanged so the manifest and the apple-touch link keep working, and the SVG's own clip id cannot collide from a separate file. | Pass, the tab unopened | medium | `9acee4e` | `git checkout 9acee4e -- index.html public/icons/icon-192.png public/icons/icon-512.png` then delete `public/icons/icon.svg` |
| 2026-09-07 | Deploy the new mark | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `03c09a5`; version `78d99fef-6e34-4d34-b18d-3bb7c8f09443`, 6 assets changed. | No migration check needed, since this batch is one component, one token, one meta line and three icon files, with no schema in it. `npm run check` green before the commit. After the deploy, from a shell holding no session, three addresses answered 302 to `roteme-pages.cloudflareaccess.com`: the root, the new `/icons/icon.svg` and `/icons/icon-512.png`, so the icons did not become public. Then in Rotem's own signed-in Chrome: Home renders with the new mark in the bar, blue disc and light rings, beside the wordmark, which is what a shell with no session can never show; the mark is proof the `--color-brand` token survived into the live stylesheet. `/icons/icon.svg` was opened live and drew the mark. Worth his verdict, not fixed: on that white page the light outer ring disappears into the background, so in a light browser tab strip the icon reads as a blue disc rather than a ringed mark. Not checked: the installed Windows app icon, which only changes when the app is reinstalled. | Pass | high | version `442c6ceb` | `npx wrangler rollback` to version `442c6ceb-fab9-479d-8e7f-51f60e03e01c` |
| 2026-09-07 | The redrawn mark, logo and icon | `src/web/components/Logo.tsx`: his new `rogers_logo.svg`. The viewBox back to 153, the mark centred at 16 again, the inner ring and the two-tone triangle gone, replaced by one rounded triangle and a diagonal line, both at stroke 3. The wordmark returns to its original x positions. Same two substitutions as before: `#E6E7E9` -> `currentColor`, the disc fill `#2D41D7` -> `var(--color-brand)`. `public/icons/icon.svg`: his new `rogers_logo_icon.svg`, copied byte for byte. `public/icons/icon-192.png` and `icon-512.png`: redrawn from it onto the same `#17181a` square, mark at 80% of the canvas. `index.html` and the manifest untouched, since no file name moved. | The eight path `d` values were diffed against his logo file and are identical; `icon.svg` is byte identical to his icon file. The component markup was rendered in a browser on the bar's colours at 34px and at 120px, and the 512 and 192 PNGs beside it: blue disc, one light ring, the triangle with its diagonal, the wordmark reading Rogers, and the 32px version still legible. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's bar was never opened; the server is Rotem's to start (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, the Logo component has one consumer, Home's top bar, the two PNG names did not move, and `--color-brand` is still read only by this component. | Pass, the app itself unopened | medium | `275dbee` | `git checkout 275dbee -- src/web/components/Logo.tsx public/icons/` |
| 2026-09-07 | Deploy the redrawn mark | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `0a48fb0`; version `ab2297db-076d-443e-8320-bb6b0a02205b`. | No migration check needed: one component and three icon files, no schema. `npm run check` green before the commit. After the deploy, from a shell holding no session, the root and `/icons/icon.svg` both answered 302, so nothing became public. Then in Rotem's own signed-in Chrome: Home renders the redrawn mark in the bar, blue disc, one ring, the triangle with its diagonal, beside the wordmark. Not checked, and it is his to do: the installed Windows app, which keeps its old icon until he uninstalls and installs it again. | Pass | high | version `78d99fef` | `npx wrangler rollback` to version `78d99fef-6e34-4d34-b18d-3bb7c8f09443` |
| 2026-09-07 | The mark, third pass | `src/web/components/Logo.tsx`: his newest `rogers_logo.svg`. The ring r15 at stroke 2 -> r14.5 at stroke 3, the triangle lowered, its top edge from y8 to y9 and its point from 26.7 to 25.75, and the diagonal unchanged. His file wraps the mark in a clip again, so the group and the `clipPath` come back, with the id namespaced to `rogers-logo-clip` because an id must be unique in the whole page and a Figma export's is not. The wordmark is untouched. Same two substitutions: `#E6E7E9` -> `currentColor`, the disc `#2D41D7` -> `var(--color-brand)`. `public/icons/icon.svg`: his new icon file, byte for byte. `icon-192.png` and `icon-512.png`: redrawn from it, same dark square, same 80% of the canvas. | The eight path `d` values were diffed against his logo file and are identical, and `icon.svg` is byte identical to his icon file. Rendered in a browser on the bar's colours at 34px and 120px with both PNGs beside them: thicker light ring, the triangle with its diagonal, blue disc, the wordmark reading Rogers, and the 32px copy still legible. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's bar was never opened; the server is Rotem's (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, one consumer, Home's top bar, the icon file names did not move, and the clip id appears nowhere else. | Pass, the app itself unopened | medium | `fb36e09` | `git checkout fb36e09 -- src/web/components/Logo.tsx public/icons/` |
| 2026-09-07 | Deploy the third pass | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `7f7bf45`; version `d5c1147b-93d3-4061-98da-5fd7068552e6`. | No migration check needed: one component and three icon files, no schema. `npm run check` green before the commit. After the deploy, from a shell holding no session, the root and `/icons/icon.svg` both answered 302, so nothing became public. Then in Rotem's own signed-in Chrome: Home renders the new mark in the bar, thicker ring and the lowered triangle, beside the wordmark. Not checked, and it is his to do: the installed Windows app, which keeps its old icon until he uninstalls and installs it again. | Pass | high | version `ab2297db` | `npx wrangler rollback` to version `ab2297db-076d-443e-8320-bb6b0a02205b` |
| 2026-09-08 | The mark, fourth pass | Three numbers, nothing else. `src/web/components/Logo.tsx`: the disc r14.5 -> r14.84, its ring stroke 3 -> 2.32, and both triangle strokes 3 -> 3.2. Every path, the wordmark, the clip and the two colour substitutions are untouched. `public/icons/icon.svg`: his matching icon file, byte for byte. `icon-192.png` and `icon-512.png`: redrawn from it, same dark square, same 80% of the canvas. | The eight path `d` values were diffed against his logo file and are identical, and the three weights and the radius were read back and match his file exactly; `icon.svg` is byte identical to his icon file. Rendered in a browser on the bar's colours at 34px and 120px with both PNGs beside them: thinner ring, heavier triangle, the wordmark reading Rogers, and the 32px copy still legible. `npm run check` green. NOT driven in the running app: nothing answers on localhost:5173, so Home's bar was never opened; the server is Rotem's (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, one consumer, Home's top bar, and no file name moved. | Pass, the app itself unopened | medium | `2475a63` | `git checkout 2475a63 -- src/web/components/Logo.tsx public/icons/` |
| 2026-09-08 | Deploy the fourth pass | No code change. `npm run deploy` on Rotem's "DEPLOY", after `Go commit` landed `c8c0308`; version `6624cb20-96b8-407c-95e7-0769d5250774`. | No migration check needed: one component and three icon files, no schema. `npm run check` green before the commit. After the deploy, from a shell holding no session, the root and `/icons/icon.svg` both answered 302, so nothing became public. Then in Rotem's own signed-in Chrome: Home renders the tuned mark in the bar beside the wordmark. Not checked, and it is his to do: the installed Windows app, which keeps its old icon until he uninstalls and installs it again. | Pass | high | version `d5c1147b` | `npx wrangler rollback` to version `d5c1147b-93d3-4061-98da-5fd7068552e6` |
| 2026-09-08 | Whole tab chip clickable | `src/web/components/TabStrip.tsx`: the chip's `px-4 py-[6px]` moved off the wrapper onto the name inside it, so the link, the rename button and the rename field each carry it and each fills the chip. No other class, size or colour changed. | `npm run check` green. The built stylesheet was read back: the reset gives every element `padding:0`, the rename field included, so relocating the two utilities leaves the 39px chip box exactly as it was. NOT driven in the running app: nothing answers on localhost:5173, so no chip was clicked; the server is Rotem's to start (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, `TabStrip` has one consumer, the project page. Named side effect: the open tab's rename target now fills its chip too, since all three states share the same padding. | Pass, the app itself unopened | medium | `6af30e1` | `git checkout 6af30e1 -- src/web/components/TabStrip.tsx` |
| 2026-09-08 | Deploy the clickable tab chip | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `3374908c-f251-45a7-8417-36cd2140c6ff`, 2 assets uploaded. It carries the tab chip whose whole box is the link. | No migration check needed: one component, four class tokens, no schema. `npm run check` was green on this code earlier today, and Rotem drove the chip himself in his running app and passed it. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit below closes that. Not checked, and it needs his own signed-in browser: the chip clicked on the live copy. | Pass | high | version `6624cb20` | `npx wrangler rollback` to version `6624cb20-96b8-407c-95e7-0769d5250774` |
| 2026-09-08 | Back to the place a project list was left | `src/web/pages/Project.tsx`: a module map of scroll offsets keyed by the address, the project id plus the tab named in the URL. A scroll listener follows the offset in a ref, and the effect's cleanup decides what to do with it by reading the address bar, which has already moved by then: a note keeps the offset, anything else drops it, and the page still being itself means React only re-ran the effect, on a tab switch or on its development double mount. A layout effect puts the page back once, the first time the project and its rows are both on screen, so the restore is never spent on the empty loading shell and never runs on a tab switch. `project-os/QA.md`: section 10f, the round trip's own check. | `npm run check` green, exit 0, no error or warning lines. NOT driven in the running app: nothing answers on localhost:5173 and no other port is listening, so the round trip was never clicked; the server is Rotem's to start (rule 16). Review per `project-os/Code_review.md` over the uncommitted diff: one finding, introduced and fixed before delivery, the restore fired while the loading shell was on screen, where the page is empty and short, spending the single attempt on a scroll to 0; it now waits for the rows. Reach is Local: the map is private to this page, `Project.tsx` has one consumer, the route in `App.tsx`, and no shared component, token, data shape or storage key is touched. Rotem vetoed the first shape of this, which restored on every arrival at the address, including from Home; the departure test above is what replaced it. | Pass, the app itself unopened | medium | `1d47565` | `git checkout 1d47565 -- src/web/pages/Project.tsx` |
| 2026-09-08 | Deploy the remembered scroll place | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `c8823d27-b815-4984-a409-8a737352ee8c`, 2 assets uploaded. It carries the project page that comes back to the row a note was opened from. | No migration check needed: one component file, no schema. `npm run check` green on this code before the deploy, exit 0, no error or warning lines. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit below closes that. Not checked, and it needs his own browser: the scroll round trip itself, live or local, since nothing answers on localhost:5173 and the deployed copy is behind the login. | Pass | high | version `3374908c` | `npx wrangler rollback` to version `3374908c-f251-45a7-8417-36cd2140c6ff` |
