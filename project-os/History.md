# Rogers — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate-history.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate-history.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-04 | deploy | **Rogers is on the internet.** Live at rogers.rotem914.workers.dev, wired to the real database and bucket, both still empty. Public until the login is switched on, which is the very next step. |
| 2026-09-04 | auth | **Rogers is behind a login.** Every address, the pages, the API and the pictures, now sends a stranger to a Cloudflare sign-in page; only members of Rotem's Cloudflare account get in, with an email code, for 7 days at a time. |
| 2026-09-04 | app | **Rogers can be installed as a Windows app from the browser.** It has its own icon and window, and opening it with no network shows a dark Rogers page instead of a browser error. |
| 2026-09-04 | done | **V1 is finished.** Rotem signed in through the email code and used Rogers on its live address; every phase of the plan from 0 to 7 is ticked. |
| 2026-09-04 | app | **Projects can be dragged into any order on Home.** The whole tile is the handle, with no grip to find, the grid rearranges under the mouse, and the order is saved and survives a reload. A database that has never been reordered keeps its creation order exactly. |
| 2026-09-04 | process | **A command in a reply now carries its full path.** Telling Rotem to run something "in the project folder" is no longer allowed; the command has to work pasted into any terminal. |
| 2026-09-04 | data | **The live database can hold a project order too.** Both copies now carry the same column, so dragging tiles will work on the live app the moment it is deployed. Nothing visible changed yet; the deployed code does not read the column. |
| 2026-09-04 | app | **Notes can be dragged into any order inside a project.** Each section is arranged on its own, so a row cannot be pinned by dropping it; the pin button still does that. A new note still arrives at the top, and pinning still puts a note on top of Pinned. |
| 2026-09-04 | deploy | **Both drag orders are live.** Projects on Home and notes inside a project can be dragged on rogers.rotem914.workers.dev, and the login still stands in front of everything. |
| 2026-09-04 | done | **Rotem confirmed both drags work on the live app.** The live check the deploy row was waiting on. |
| 2026-09-04 | app | **The composer opens with the caret in the title.** Rotem's own edit; it used to open in the body the way Keep does. |
| 2026-09-04 | app | **Correction: that title focus was my change, and it was checked.** The row above credits Rotem and records it as unchecked. It was made in this session, on his request, and driven in the browser. |
| 2026-09-04 | app | **The composer's leftover body-focus handle is gone.** Dead since the caret moved to the title. Nothing a person can see changed. |
| 2026-09-05 | app | **No more placeholder rows while a project or a note opens.** The load is instant, so the grey shapes only flickered. Home still has its own. |
| 2026-09-05 | app | **Home's tile placeholders are gone too, and the placeholder components with them.** `Feedback.tsx` is now the toast alone. |
| 2026-09-05 | design | **Every screen is set in Google Sans.** Loaded from Google Fonts with its Hebrew alphabet, so a note that mixes Hebrew and English stays in one typeface. The note field also remeasures itself once the font lands, so a first visit cannot hide the last line of a note. |
| 2026-09-05 | design | **Header, note page and Home re-laid out to Rotem's own measurements.** The top bar lost its background and border and now shares a 1168px container with the page, its back arrow is a 24px icon in a 48px box, a note's title moved out of the bar into the 720px text column while its pictures run the full width, and Home's tiles are a fixed 272px in a 1152px grid. |
| 2026-09-05 | design | **Google Sans is served by Rogers itself now, not by Google.** No page load calls out any more, and the licence ships beside the font files as the licence requires. Latin, Latin extended and Hebrew are covered; any other alphabet falls back to the system font. |
| 2026-09-05 | deploy | **Everything waiting is live.** Google Sans served by Rogers itself, the loading placeholders gone, and another session's header, note page and Home layout work, all in one deploy. The login still stands in front of every address, the font files included. |
| 2026-09-05 | done | **Google Sans really does render on the live site.** Rotem checked it signed in; the font files serve through the login, which was the one thing that could only be seen there. |
| 2026-09-05 | deploy | **Rogers has its own address, rogers.rotem-e.com, because mobile carriers block workers.dev.** Rotem's iPhone hung on the old address over 4G, and so did Cloudflare's own workers.dev demo page, so the fault was the domain family, not Rogers. The login covers the new address; the old workers.dev address went dark as a side effect, pending his verdict. |
| 2026-09-05 | done | **Rogers opens on the iPhone over 4G and sits on its Home Screen as an app.** Rotem checked the new address on the phone; the projects grid loaded, and Add to Home Screen gave it an icon and its own window, which is what the workers.dev block had been hiding. |
| 2026-09-05 | deploy | **The old workers.dev address is retired on purpose.** Rotem's verdict on the side effect: it stays dark, and the config now says so explicitly instead of by omission. |
| 2026-09-05 | design | **Home is laid out to Rotem's mobile and desktop measurements, and the note count is gone from the tiles.** Cards round at 13px and pad at 24px, the grid gaps 20px inside a 1180px column with 72px of desktop breathing room, breakpoint A at 900px lets tiles stretch and breakpoint B at 430px stacks them one per row at 144px tall. Clicking off the new-project card now saves the name instead of losing it, and tile, note row and "Take a note" fade their hover over 144ms. |
| 2026-09-05 | design | **A note row's picture thumbnails are 216px instead of 48px.** Only the row size changed; the composer's 96px thumbnails and the note page's full-width pictures are untouched. |
| 2026-09-05 | app | **Undo and redo, for text and pictures, on the note page and in the composer.** Ctrl+Z, Ctrl+Shift+Z or Ctrl+Y, Cmd on a Mac. Alongside it the note row got 16px top and bottom, its pictures a 16px gap, the note page pictures a 24px gap, and the remove-picture button is a 32px circle with a 24px cross. |
| 2026-09-06 | design | **The page ground is #14141A and the three main cards are #23242F with no hairline.** The project tile, the note row and the collapsed composer lost their border and took the new card colour; the top bar, menus, the expanded composer and the thumbnails keep `surface` and their borders. |
| 2026-09-06 | design | **The three cards hover at #2B2D3B.** The project tile, the note row and the "Take a note" button use their own hover colour now instead of the shared `surface-hover`; everything else still hovers at `#2a2b2f`. |
| 2026-09-06 | app | **A project can be split into tabs, each with its own list of notes.** A plus above "Take a note…" makes the first one: the project's own list becomes a tab called Main and "New tab" opens beside it, empty. A note typed in a tab stays in it, the back arrow from a note returns to its tab, and removing a tab sends its notes back to Main. The browser pass is still owed: the dev server was down. |
| 2026-09-06 | app | **Tabs checked in the browser, and a tab can be renamed.** The open tab's name is a field on click: Enter or a click outside saves, Escape discards, Hebrew survives. The pass also caught my own guard against a stale list failing, and its replacement holds: switching tabs never shows the last tab's notes under the new name. |
| 2026-09-06 | design | **The tabs moved up beside the project name, and the strip got its shape.** The project name now starts exactly where Rogers starts on Home, with the back arrow hanging out of the flow to its left. Tabs are 18px text in a 39px chip, the plus is a round 39px button that only appears on hover, removing a tab is on its right-click menu, and Main can be renamed like any other tab. |
| 2026-09-06 | design | **Rotem's own two edits: the open composer and the back arrow.** The expanded composer lost its hairline for the card colour, and shows an accent ring while a picture is dragged onto it. The back arrow is a full circle. |
| 2026-09-06 | design | **The page ground is #111217, everywhere the app paints a ground.** The token, both copies in the page shell, the installed app's splash and the offline page all moved together, so the first frame and the launch screen are the same dark as the app. |
| 2026-09-06 | design | **Every card is #21222C, and hovers at #252632.** One token pair carries the project tile, the note row, the composer open and closed, and the open tab, so they all moved together. |
| 2026-09-06 | design | **Home's new-project tile draws the same plus the tabs use, inside a solid 2px edge.** The typed plus character is gone, so one drawn shape now means add in both places. Hovering it fills the tile instead of lighting its edge, over the same 144ms the other cards use. |
| 2026-09-06 | deploy | **The logo and the taller bar are live.** Version `84d57745`, no schema involved. |
| 2026-09-06 | done | **The logo really does render on the live site.** Rotem checked it signed in, which is the one thing a shell with no session could not answer. |
| 2026-09-06 | design | **The bar sits 56px from the top of the window instead of 32.** Every screen moved together, so the content below each one starts 24px lower. |
| 2026-09-06 | design | **Home's title is Rotem's logo now, drawn, not typed.** The mark and the wordmark take the bar's own text colour, and the bar kept its height, so nothing below it moved. |
| 2026-09-06 | deploy | **The colour pass and the new-project tile are live.** The darker ground, the card colours, the splash and offline page, and the tile with its drawn plus, all in one deploy, version `6d12171d`. |
| 2026-09-06 | deploy | **Tabs are live at rogers.rotem-e.com.** The live database got the tabs table and the note column first, so the new list query had something to read, then the deploy, version `e1ac5d1f`. The login still fronts every address, the new tabs route included. |
| 2026-09-06 | design | **The back arrow is a 32px icon in a fully round hover.** Only the top bar arrow changed: the glyph went from 24px to 32px inside the same 48px hit area, and its hover shape went from the 13px card radius to a full circle. |
| 2026-09-06 | design | **The open composer sits on the same card as the collapsed one.** It takes `bg-card` instead of `bg-surface` and has no border at all; the picture drag now shows a 2px accent ring instead of an accent border. |
| 2026-09-07 | app | **Going back to a list is instant.** Home, a project and its tabs show the list they last showed the moment they open, then quietly refresh it, so returning from a note no longer shows "Take a note" alone and the notes a beat later. The note editor is deliberately not remembered: it always opens from the server copy. On a first visit Main's notes are fetched beside the tabs instead of after them. |
| 2026-09-07 | app | **A project opens with its list already there on the first visit too.** Hovering or focusing a tile on Home fetches that project's tabs and Main's notes ahead of the click, once, so the project page paints in one piece even when nothing was remembered yet. |
| 2026-09-07 | app | **A note row shows a small copy of each picture instead of the original.** When a picture is added, the browser draws a WebP copy no taller than 480px and stores it beside the original; the row and the composer ask for that copy and get the original where none exists, so pictures added before today still show, at their old cost. The note page and the lightbox always show the original. |
| 2026-09-07 | app | **The remembered lists survive a reload.** Home, a project and its tabs keep their last lists in the tab's session storage, so a reload, or the trip through the login when the session expires, paints the lists at once and refreshes them behind. Session storage on purpose: it dies with the tab, so a preview of a private note never outlives the window. The note itself is still never stored. |
| 2026-09-07 | app | **The first switch to a tab is instant.** Opening a project fetches the lists of its other tabs, and Main's when a tab is open, right after the tabs are known, so switching paints from memory and refreshes behind. Each list is asked for once per page; the switch itself keeps it fresh. |
| 2026-09-07 | app | **The remembered lists now live in the browser's long-term memory.** A new tab, a restarted browser and a reload all paint the lists at once. Rotem's call, knowing the cost: titles and previews stay on the machine until Rogers overwrites them, logged out or not. |
| 2026-09-07 | app | **Old pictures get their small copies by themselves.** A few seconds after Rogers opens, the browser asks which pictures have no copy, makes each one and stores it, one at a time; once all have one, the check costs one request. A picture standing in for a missing copy is no longer kept by the browser for a year, so the copy takes over as soon as it exists. |
| 2026-09-07 | deploy | **Instant navigation and small picture copies are live at rogers.rotem-e.com.** The commit `0111a26` deployed as version `442c6ceb`; the backfill of old pictures runs the first time Rotem opens the live app, a few seconds in. |
| 2026-09-07 | design | **The logo is Rotem's new two-colour mark.** The disc is brand blue behind the light rings and the wordmark, which still take the bar's own text colour. |
| 2026-09-07 | design | **The browser tab and the installed app carry the mark.** The tab draws the mark itself at whatever size it needs, and the installed app icon replaces the yellow ring left over from setup. |
| 2026-09-07 | deploy | **The new mark is live at rogers.rotem-e.com.** The logo renders in Rotem's own signed-in browser, and the tab icon is served from the same address. |
| 2026-09-07 | design | **The mark is Rotem's redrawn version.** One ring instead of two, a rounded triangle with a diagonal cut through it, and the same artwork now serves as the tab and app icon. |
| 2026-09-07 | deploy | **The redrawn mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser; the installed Windows app still needs a reinstall to take the new icon. |
| 2026-09-07 | design | **The mark got a thicker ring and a lower triangle.** Rotem's next pass on the same artwork, in the logo and in the tab and app icons together. |
| 2026-09-07 | deploy | **The third pass of the mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser. |
| 2026-09-08 | design | **The mark's weights were tuned again.** A thinner ring, a slightly wider disc and a heavier triangle, in the logo and in the tab and app icons together. |
| 2026-09-08 | deploy | **The tuned mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser. |
| 2026-09-08 | ui | **A tab chip is clickable across its whole box.** Clicking anywhere on a tab, its padding included, opens it; before, only the letters themselves answered. |
| 2026-09-08 | deploy | **The clickable tab chip is live at rogers.rotem-e.com.** Rotem confirmed it in his own running app before the deploy; the live copy itself still answers the login to anyone without a session. |
| 2026-09-08 | app | **A project list comes back where you left it, but only from a note.** Scrolling well down, opening a note and pressing back now lands on the same row instead of at the top, and every tab keeps its own place. Opening the project from Home starts at the top, Rotem's call; so does a reload. |
| 2026-09-08 | deploy | **The remembered scroll place is live at rogers.rotem-e.com.** Coming back from a note lands on the same row; opening a project from Home still starts at the top. |
| 2026-09-08 | ui | **A pasted picture keeps its own size on the note page.** A small image now sits at its real pixel size at the top left of the note instead of being stretched across the column; anything wider than the column still shrinks to fit. |
| 2026-09-08 | ui | **The tabs sit on their own block of the page's colour.** Scrolling a project's list no longer sends note cards through the tab names; they pass behind a padded background instead. |
| 2026-09-08 | ui | **The tab background lost its side padding.** The tabs are back on the notes' own edge, and the block starts 16px above them instead of 24. |
| 2026-09-08 | ui | **The tab block sits tighter.** 12px of air above and below the tabs instead of 16 and 24. |
| 2026-09-08 | ui | **The tabs now stop at the very top of the window.** Scrolling a project pins the tab block to the top edge instead of leaving a see-through band above it, and the page at rest looks exactly as it did. |
| 2026-09-08 | ui | **The title is back where it sits, and the bar itself is opaque.** Correcting the row above: pinning the block to the window's edge dragged the project title up with it, which Rotem did not ask for, so the bar is pinned where it always was and its own box now hides what scrolls behind it. |
| 2026-09-08 | ui | **The tabs pin to the top on their own, and the title holds its place.** The strip left the bar and became its own sticky box, so scrolling sends the tabs to the window's edge while the project name and the back arrow stay exactly where they sit. On a narrow window the two pin one under the other, title first. |
| 2026-09-08 | ui | **A note's pin and menu moved onto its right-click.** The star and the three dots are gone from every row on a project page; right-clicking a note opens Pin and Archive where the cursor is. |
| 2026-09-08 | app | **Tabs can be dragged into the order you want.** A tab chip dragged along the strip rearranges it under the mouse, and the order is saved for the project, so it survives a reload and follows every screen. Main stays first: it has no row of its own, so it has nowhere to keep a place. |
| 2026-09-08 | ui | **A note row's text runs the full width of its card.** The 64px gutter the pin and the menu used to sit in is gone, so a title now stops at the same edge as the composer above it. |
| 2026-09-08 | ui | **The dead band under the tabs is gone.** The title's bar no longer paints or catches clicks over the notes column on a wide window, so the cards run right up under the pinned tabs and a card in that band opens on a click. |
| 2026-09-08 | ui | **A narrow window loses half its head room.** The bar starts 24px from the top instead of 56px on anything under 900px wide, and the tab strip moved up with it so the two stay glued. A wide window is untouched. |
| 2026-09-08 | deploy | **The bar and the pictures are live at rogers.rotem-e.com.** The tabs pin to the top on their own, the project title holds its place, a narrow window has less head room, and a pasted picture keeps its own size. |
| 2026-09-08 | data | **The live database got the tab order column it was missing.** The deploy above shipped code that sorts tabs by a column the live database did not have yet, so every project with tabs would have failed there; the migration was applied straight after and the live database now reports nothing pending. |
| 2026-09-08 | design | **Rotem's newest mark, in the logo and every icon.** The wordmark and the mark are redrawn from his files, the favicon is his icon file byte for byte, and the iPhone home screen now has an icon of its own at the size iOS asks for, instead of the letter tile it was drawing. |
| 2026-09-08 | deploy | **The fifth mark is live at rogers.rotem-e.com.** The logo, the favicon and the app icons, plus the new iPhone tile at the size iOS asks for. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate-history.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-08 | Whole tab chip clickable | `src/web/components/TabStrip.tsx`: the chip's `px-4 py-[6px]` moved off the wrapper onto the name inside it, so the link, the rename button and the rename field each carry it and each fills the chip. No other class, size or colour changed. | `npm run check` green. The built stylesheet was read back: the reset gives every element `padding:0`, the rename field included, so relocating the two utilities leaves the 39px chip box exactly as it was. NOT driven in the running app: nothing answers on localhost:5173, so no chip was clicked; the server is Rotem's to start (rule 16). Review per Code_review.md over this diff: no findings; reach is Local, `TabStrip` has one consumer, the project page. Named side effect: the open tab's rename target now fills its chip too, since all three states share the same padding. | Pass, the app itself unopened | medium | `6af30e1` | `git checkout 6af30e1 -- src/web/components/TabStrip.tsx` |
| 2026-09-08 | Deploy the clickable tab chip | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `3374908c-f251-45a7-8417-36cd2140c6ff`, 2 assets uploaded. It carries the tab chip whose whole box is the link. | No migration check needed: one component, four class tokens, no schema. `npm run check` was green on this code earlier today, and Rotem drove the chip himself in his running app and passed it. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit below closes that. Not checked, and it needs his own signed-in browser: the chip clicked on the live copy. | Pass | high | version `6624cb20` | `npx wrangler rollback` to version `6624cb20-96b8-407c-95e7-0769d5250774` |
| 2026-09-08 | Back to the place a project list was left | `src/web/pages/Project.tsx`: a module map of scroll offsets keyed by the address, the project id plus the tab named in the URL. A scroll listener follows the offset in a ref, and the effect's cleanup decides what to do with it by reading the address bar, which has already moved by then: a note keeps the offset, anything else drops it, and the page still being itself means React only re-ran the effect, on a tab switch or on its development double mount. A layout effect puts the page back once, the first time the project and its rows are both on screen, so the restore is never spent on the empty loading shell and never runs on a tab switch. `project-os/QA.md`: section 10f, the round trip's own check. | `npm run check` green, exit 0, no error or warning lines. NOT driven in the running app: nothing answers on localhost:5173 and no other port is listening, so the round trip was never clicked; the server is Rotem's to start (rule 16). Review per `project-os/Code_review.md` over the uncommitted diff: one finding, introduced and fixed before delivery, the restore fired while the loading shell was on screen, where the page is empty and short, spending the single attempt on a scroll to 0; it now waits for the rows. Reach is Local: the map is private to this page, `Project.tsx` has one consumer, the route in `App.tsx`, and no shared component, token, data shape or storage key is touched. Rotem vetoed the first shape of this, which restored on every arrival at the address, including from Home; the departure test above is what replaced it. | Pass, the app itself unopened | medium | `1d47565` | `git checkout 1d47565 -- src/web/pages/Project.tsx` |
| 2026-09-08 | Deploy the remembered scroll place | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `c8823d27-b815-4984-a409-8a737352ee8c`, 2 assets uploaded. It carries the project page that comes back to the row a note was opened from. | No migration check needed: one component file, no schema. `npm run check` green on this code before the deploy, exit 0, no error or warning lines. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit below closes that. Not checked, and it needs his own browser: the scroll round trip itself, live or local, since nothing answers on localhost:5173 and the deployed copy is behind the login. | Pass | high | version `3374908c` | `npx wrangler rollback` to version `3374908c-f251-45a7-8417-36cd2140c6ff` |
| 2026-09-08 | Pictures at their own size on the note page | `src/web/components/ImageStrip.tsx`, the `lg` branch only: the box `w-full max-w-[1160px]` -> `w-fit max-w-[1160px]`, and the picture `block w-full` -> `block h-auto max-w-full`, so it draws at its natural pixels and shrinks only when it is wider than the column or past 1160px. The list keeps one picture per row, which the old full-width item used to give for free: the `lg` list is now `flex-col items-start` and its item `w-fit max-w-full`. That pair is a named side effect and the reason for it: the remove x is positioned on the item, so on a full-width item it would have floated to the far right of the row instead of sitting on the picture's corner. The `sm` and `md` branches are untouched. | `npm run check` green, exit 0. The built stylesheet was read back and carries all four new utilities, `w-fit`, `h-auto`, `flex-col`, `items-start`, which was the real risk here, a class Tailwind never generates and a rule that silently does nothing. NOT driven in the running app: localhost:5173 serves the shell but the app answers "Rogers is offline", so no note page was opened and no picture was seen at its own size; the server is Rotem's to start (rule 16). Review per `project-os/Code_review.md` over this diff: no findings; reach is Local, `ImageStrip` has three consumers and only the note page passes `lg`, so the list thumbnails and the composer cannot be reached from this change. | Pass, the app itself unopened | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/ImageStrip.tsx` |
| 2026-09-08 | A background under the tabs | `src/web/components/TabStrip.tsx`: the strip's row is now wrapped in one more div, `bg-bg p-6`, Rotem's ask, so the sticky bar has something opaque under the tab names. Nothing inside the strip changed; the wrapper is the whole diff apart from the re-indent it forces. | `npm run check` green, exit 0. The built stylesheet was read back and carries both utilities, `.bg-bg{background-color:var(--color-bg)}` and `.p-6{padding:calc(var(--spacing) * 6)}` with `--spacing:.25rem`, so the padding really is the 24px asked for and the colour really is the page's own `#111217`. NOT driven in the running app: localhost:5173 serves the shell but the app answers "Rogers is offline", so no project list was scrolled under the bar; the server is Rotem's to start (rule 16). Review per `project-os/Code_review.md` over this diff: no findings; reach is Local, `TabStrip` has one consumer, the project page's bar. Three consequences worth his verdict, all inherent in the ask: the tabs now start 24px right of the cards' left edge, which `TopBar` deliberately lined up; the bar is 48px taller; and the block covers only the middle column, so anything scrolling under the title or the gutters still shows through. | Pass, the app itself unopened | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/TabStrip.tsx` |
| 2026-09-08 | The tab background, second pass | `src/web/components/TabStrip.tsx`: the wrapper's `p-6` -> `pt-4 pb-6`, Rotem's correction. The side padding was what pushed the tabs 24px off the notes' left edge, so it is gone and the block now spans exactly the 720px the cards span; the top is the 16px he asked for and the bottom keeps his 24. | `npm run check` green, exit 0. The built stylesheet carries `.pt-4{padding-top:calc(var(--spacing) * 4)}` and `.pb-6`, and with `--spacing:.25rem` that is 16px and 24px. NOT driven in the running app: the app was still answering "Rogers is offline" when this was written, so the bar was not seen; the server is Rotem's to start (rule 16). Reach is Local, `TabStrip` has one consumer. Read as his words meant the block I had just added, not the bar's own 56px of head room; if he meant the bar, that is one more line. | Pass, the app itself unopened | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/TabStrip.tsx` |
| 2026-09-08 | The tab background, third pass | `src/web/components/TabStrip.tsx`: the wrapper's `pt-4 pb-6` -> `py-3`, Rotem's number, 12px on both sides. Nothing else moved. | `npm run check` green, exit 0, and the stylesheet carries `.py-3{padding-block:calc(var(--spacing) * 3)}`, which is 12px on the 4px scale. NOT driven in the running app from here; Rotem is reading the live box himself, 720 x 79 before this change. Reach is Local, `TabStrip` has one consumer. | Pass, his own eyes on the box | low | `59091a2` | `git checkout 59091a2 -- src/web/components/TabStrip.tsx` |
| 2026-09-08 | The tab block pinned to the top edge | `src/web/components/TopBar.tsx`, the centre variant only, line 98: `sticky top-0` -> `sticky -top-14`. The bar's 56px of head room is padding inside the sticky box, so at `top-0` it stayed on screen as a see-through band with cards sliding through it above the tabs. At -56px the box's own top goes off screen and the tab block lands exactly on the viewport's top edge, while the resting layout is untouched because padding, not position, still sets the head room. The plain variant on Home and a note keeps `top-0`, since it has no opaque block and pinning it tighter would only move the same see-through band. | Driven in Rotem's running app at 1288px, the project `8f96078e`: scrolled down, the block sits flush on the top edge and the note cards pass behind it with nothing showing above; scrolled back up, the title, the arrow and the tabs are where they were, the tabs still on the cards' left edge. No console errors. `npm run check` green, exit 0, and the stylesheet carries `.-top-14{top:calc(var(--spacing) * -14)}`, which is -56px, the exact height of the padding. Reach is Shared but bounded: `TopBar` serves three screens and only the project page passes `center`. Worth his verdict, not fixed: below 900px the columns stack, so the pinned row is the title, which has no block of its own and lets cards through; that band existed before this change too, 56px lower. | Pass | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/TopBar.tsx` |
| 2026-09-08 | The bar opaque instead of raised | `src/web/components/TopBar.tsx`, the centre variant only: `-top-14` back to `top-0`, and `bg-bg` added to the header. The row above pinned the whole box to the window's edge, and the title rides in that box, so it moved up with the tabs; Rotem asked for the menu, not the title. Painting the header instead leaves every position exactly as it has always been and closes the same see-through band, because the band was the header's own padding. The title has been pinned since the bar was written, at its resting place, which is why it reads as not moving at all. | Driven in Rotem's running app at 1288px on project `8f96078e`: scrolled down, the title sits at its usual place with the tabs beside it and the cards disappear cleanly behind the bar, nothing showing through above the tabs; scrolled up, the page is unchanged. `npm run check` green, exit 0. Reach is Shared but bounded: only the project page passes `center`, and Home and a note keep the transparent `top-0` bar they had. Named side effect: the tab block's own `bg-bg` is now redundant, since the header behind it carries the same colour; it is left alone because it is his and it costs nothing. | Pass | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/TopBar.tsx` |
| 2026-09-08 | Note actions on the right-click | `src/web/components/NoteRow.tsx`: the hover chrome, the pin star and the `Menu` trigger, is gone, and the row now opens its own menu on `contextmenu`, at the point the click landed, clamped so a click at the right edge keeps the 144px popup inside the row. Two items, Pin or Unpin and Archive, calling the same `save` and `archive` the old buttons called, unchanged. Same shape as the tab chips in `TabStrip.tsx`: a pointerdown outside and Escape close it, and the first item takes focus. `Menu.tsx` is untouched and still serves the note page. | In Rotem's running app, project Dig: hovering a row now shows no star and no dots; right-click opens Pin and Archive at the cursor; Pin sent PATCH 200 and the list resectioned into PINNED and OTHERS; right-clicking the pinned row read Unpin and put the list back exactly as it was. A throwaway note made in the composer was removed by right-click Archive. Escape closes it, a click outside closes it, a right-click on a second row leaves only one menu open, and a right-click at the row's right edge keeps the popup inside the card. Left click still opens the note page. Console: only the pre-existing service worker failure of the dev pane, and the tabs 500 below. `npm run check` green, exit 0. Reordering re-driven the way QA.md section 10c prescribes, with a dispatched drag sequence and not a mouse drag: row 2 onto row 3 swapped them on dragenter, the swap held through the drop, the notes endpoint answered with the new order, and dragging it back left Rotem's list exactly as it was, in the page and on the server. Review per `project-os/Code_review.md` over this diff: one nit, not fixed, Escape leaves focus on the body instead of returning it to the row, which is exactly what the tab chips do today, so fixing it here alone would make a third pattern. Reach is Local: `NoteRow` has one consumer, `Project.tsx:560`. Pre-existing, reported and not fixed: `GET /api/projects/:id/tabs` answers 500 on the local dev database, whose `tabs` table has no `position` column, so migration 0006 was never applied to it. Named side effect: the row keeps the 64px right gutter the removed buttons sat in, because a spacing value was not part of the ask. | Pass | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/NoteRow.tsx` |
| 2026-09-08 | Dragging the tabs into order | `migrations/0006_tab_position.sql`: one nullable INTEGER on `tabs`, additive, no row read or rewritten; null sorts LAST, so a tab made after the strip was arranged lands at the end beside the plus that made it. `src/api/tabs.ts`: the list query orders by that column then by creation date, and a new `PUT /api/projects/:id/tabs/order` writes a position for every id in one D1 batch, scoped to the project and to live tabs, with the same id and duplicate guards the projects order route uses. `src/shared/types.ts`: `ReorderTabsBody`. `src/web/components/TabStrip.tsx`: the drag handlers, the same shape Home's tiles use, each tab chip wrapped in a draggable div and the chip's link told `draggable={false}` so it cannot drag its own address instead; Main carries none of it. `src/web/pages/Project.tsx`: `reorderTabs`, which sends the order, shows the toast on failure and rethrows so the strip can put itself back. | `npm run check` green, exit 0. `npm run lint`: 1 error and 5 warnings, every one pre-existing in `Composer.tsx`, `useApi.ts`, `Note.tsx` and the generated worker types; none in the four files this change touches. `npm run db:migrate:local` applied 0006 to the local database. Driven in Rotem's running app at localhost:5173, in a throwaway project of my own with three tabs, never his, and with a dispatched drag sequence rather than a mouse drag, per QA 10c. The DOM first: Main carries no `draggable`, each tab wrapper carries `draggable="true"` and its link `draggable="false"`. Dragging Gamma onto Alpha rearranged the strip on `dragenter`, the order held through the drop, and after a reload both the page and `GET .../tabs` read Gamma, Alpha, Beta. The open tab, whose name is a button and not a link, drags too and stays open. A tab added afterwards landed at the end. A drag begun inside the open rename field was refused, `defaultPrevented` true, and the chip did not move. The right-click Remove menu still opens and Escape still closes it. All six projects listed their tabs 200 under the new ORDER BY, the Hebrew-named ones included, so rows with no position keep creation order. Failure path forced by stubbing only the order PUT to 500: the preview moved, the strip snapped back to the Worker's order and the toast read "Could not save the new tab order."; the stub died with the reload that followed. At 375 wide, resized before loading: no horizontal overflow, the strip wraps to two rows and a drag still saves. At 1280: one row, no overflow, and every chip 39px tall including Main's unwrapped one, so the new wrapper div moved no geometry. Console: passed, known existing error only, the service worker failing to register inside the browser pane. The throwaway project was archived and the QA tab closed. Review per `project-os/Code_review.md` over this change, `migrations/0006`, `tabs.ts`, `types.ts`, the drag part of `TabStrip.tsx` and `Project.tsx`: one nit, introduced and fixed before delivery, an import line needlessly reshuffled. Reach is Shared and traced: `TabStrip` has one consumer, the project page's bar, and the tabs list is also prefetched from `ProjectTile` on Home, which only warms the cache and takes whatever order the Worker sends; the `Tab` shape is unchanged, so nothing downstream shifts. Reported, not fixed, pre-existing pattern: the order route accepts an unbounded list of ids, exactly as the projects order route does. Named limitation: reordering is pointer-only, the same as Home's tiles and the note rows, so a keyboard cannot do it. Blocking before any deploy, and it is QA 10d.8: `npm run db:migrate:remote` runs first, or the live tabs list answers 500 for every project. | Pass | high | `59091a2` | `git checkout 59091a2 -- src/api/tabs.ts src/shared/types.ts src/web/pages/Project.tsx`, then take the drag parts out of `src/web/components/TabStrip.tsx` by hand, since that file carries other uncommitted work, and delete `migrations/0006_tab_position.sql`; the column can stay, nothing reads it once the list query is back |
| 2026-09-08 | The note row's right gutter | `src/web/components/NoteRow.tsx`: the content div's `pr-16` is gone, Rotem's call after the row above named it as a leftover. One class token, nothing else on the row moved. | In his running app, project Dig, read from the DOM: the content box is 688 wide inside the 720 row with `padding-right: 0`, so it is now symmetric with the row's own 16px on each side; before this it stopped 64px short. On screen the Hebrew titles sit flush on the card's right edge, level with the composer above them, and the right-click menu still opens with Pin and Archive. `npm run check` green, exit 0. Reach is Local: `NoteRow` has one consumer, `Project.tsx:560`. | Pass | low | `59091a2` | `git checkout 59091a2 -- src/web/components/NoteRow.tsx`, which also takes back the right-click menu |
| 2026-09-08 | The strip as its own sticky box | `src/web/components/TopBar.tsx`, the centre variant only. One sticky box can pin only one thing, and Rotem wants the tabs at the window's edge with the title unmoved, so the strip now sits in a sticky box of its own after the header, pulled back up by `-mt-[63px]`, the strip's own height, onto a bar row held at the same `h-[63px]`. Above 900px the two part on the way down; below 900px the row goes back to `h-auto`, the pull-up is cancelled and the strip pins at `top-[104px]` instead, which is the bar's 56px of head room plus a 48px title row, so the title stays above the tabs and both stick. The title's cell is told that 48px outright, and the trailing cell is rendered only when there is something in it, so it stops adding a phantom row to that sum. Rotem also killed the empty spacer div that held the row open in the first draft; the height moved onto the row itself. | Driven in Rotem's running app. At 1762px on project `8f96078e`, scrolled 391px, read off the page: the bar pinned at 0..119 with the title at its usual place, the strip pinned at 0..63, cards passing behind both. At 700px, in an iframe of that width so the media query really applied, scrolled 400px: the bar pinned at 0..104 and the strip at 104..167, title above tabs, both stuck; the iframe was removed afterwards. No console errors. `npm run check` green, exit 0. Reach is Shared but bounded: `TopBar` serves three screens and only the project page passes `center`, so Home and a note keep the single transparent bar they had. | Pass | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/TopBar.tsx` |
| 2026-09-08 | The title's bar stops covering the notes | `src/web/components/TopBar.tsx`, the centre variant: the header's `bg-bg` is now `max-[900px]:bg-bg`, and the header and the strip box are `pointer-events-none` with the title cell, the trailing cell and the strip's own cell put back to `pointer-events-auto`. The opaque header was mine, from an earlier pass, and above 900px it painted a 56px band of page colour across the notes column under the pinned tabs, which read as a gap, and swallowed the clicks of any card behind it. Below 900px it stays opaque and clickable, because there the bar sits over the cards by design. | Driven in Rotem's running app at 1762px on project `8f96078e`: scrolled down, the cards now run right up under the tab block with no band between them; a card in the old dead band was clicked and opened its note; the back arrow still goes Home; a tab chip still switches tab. At 700px, measured in an iframe of that width: the bar pinned at 0..104, opaque `rgb(17, 18, 23)`, pointer events auto, the strip pinned at 104..167 under it, and a hit test at the title's row lands on the title, not on a card behind it. No console errors. `npm run check` green, exit 0. Reach is Shared but bounded: only the project page passes `center`. | Pass | medium | `59091a2` | `git checkout 59091a2 -- src/web/components/TopBar.tsx` |
| 2026-09-08 | Less head room on a narrow window | `src/web/components/TopBar.tsx`, the centre variant: `max-[900px]:pt-6` on the header, Rotem's 24px, and the strip's narrow offset `top-[104px]` -> `top-[72px]` to follow it. That second number is not a free choice: the strip pins directly under the bar there, so it has to equal the bar's own height, now 24px of head room plus a 48px title row. | Measured in Rotem's running app, in an iframe of each width so the media query really applied. At 654px: padding-top 24px, the bar 0..72 and the strip 72..135, identical at rest and scrolled 400px, so the two stay glued with no gap between them. At 1400px, unchanged: padding-top 56px, transparent, pointer events none, the strip on the title's row at rest and climbing to 0..63 when scrolled while the bar holds 0..119. `npm run check` green, exit 0. Reach is Shared but bounded: only the project page passes `center`. | Pass | low | `59091a2` | `git checkout 59091a2 -- src/web/components/TopBar.tsx` |
| 2026-09-08 | Deploy the split bar and the picture size | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `3d067282-f170-4611-8786-487a54029493`, 3 assets uploaded. It carries the whole day's bar work, the tab strip as its own sticky box, and the note page's pictures at their own size. | No migration check needed: two components, no schema. `npm run check` green on this code before the deploy, and the layout was measured in the running app at 654px and 1400px. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit below closes that. Not checked, and it needs his own signed-in browser: the live copy scrolled. | Pass | high | `59091a2` | `npx wrangler rollback` to version `c8823d27-b815-4984-a409-8a737352ee8c` |
| 2026-09-08 | The tab order migration, applied live | No code change. `npm run db:migrate:remote` applied `migrations/0006_tab_position.sql` to `rogers-db`, one nullable `position` column on `tabs`. It was found pending right after the deploy, while the deployed `src/api/tabs.ts` already orders the strip by that column, so the live tab list was broken between the two commands. | `npx wrangler d1 migrations list --remote` named `0006` as pending before, and answered "No migrations to apply" after. The file was read first and is additive by rule 11: one `ALTER TABLE tabs ADD COLUMN position INTEGER`, nullable, no default, no existing row read or rewritten, so no note or tab can be lost by it. `npm run check` green, exit 0. Not checked, and it needs his own signed-in browser: a live project page with tabs, and a drag that sticks. Worth saying out loud: a deploy that carries a migration has to run this straight after, and the deploy shortcut does not do it on its own. | Pass, the live page unopened | high | `59091a2` | The column is additive and unused by older code, so the way back is the rollback in the row above; the column itself can stay. |
| 2026-09-08 | The fifth mark, and an icon for iOS | `src/web/components/Logo.tsx`: his new `rogers_logo.svg`. The box grows, viewBox 153x34 -> 157x37, the mark is centred at 18 on a 36 square with the disc at r16.84, and the wordmark moves with it; the clip rect follows to 36. Same two substitutions as every pass: `#E6E7E9` -> `currentColor`, the disc `#2D41D7` -> `var(--color-brand)`, and the clip id stays namespaced `rogers-logo-clip`. `public/icons/icon.svg`: his `rogers_logo_icon.svg`, byte for byte, which is the favicon. `icon-192.png` and `icon-512.png`: redrawn from it, same `#17181a` square, mark at 80%. New `public/icons/apple-touch-icon.png` at 180, the size iOS asks for by name, drawn the same way, and `index.html` points `apple-touch-icon` at it with `sizes="180x180"` instead of at the 192. | The six wordmark `d` values and the three mark shapes were diffed against his file and are identical; `icon.svg` compares byte identical with `cmp`. Rendered in the running app: Home's bar draws the new mark beside the wordmark, blue disc, light ring, the triangle with its diagonal, and `/icons/icon.svg` opened on its own draws the mark. The 180 PNG was opened and shows the mark centred on the dark square. `npm run check` green, exit 0, and the build carries all four icon files. Not checked, and it needs his own iPhone: the home screen tile, which iOS only redraws when the site is removed and added again. Worth his verdict: if the tile stays a letter after that, the cause is Access answering the icon fetch with the login page, and the fix is a public bypass for `/icons/*` in his Cloudflare dashboard, which is his to set. | Pass, the iPhone untested | medium | `3144e53` | `git checkout 3144e53 -- src/web/components/Logo.tsx public/icons/ index.html` then delete `public/icons/apple-touch-icon.png` |
| 2026-09-08 | Deploy the fifth mark | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `08b4d71b-78c8-45a9-b1fc-20e25cea6a61`, 6 assets uploaded, the new `apple-touch-icon.png` among them. | No migration needed, and the live database was asked anyway: "No migrations to apply". `npm run check` green, exit 0, before the deploy. After it, from a shell holding no session, the root, `/icons/icon.svg` and `/icons/apple-touch-icon.png` all answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. That last 302 is also the evidence for the iPhone question: an icon fetch carrying no session gets the login page, so if the home screen tile stays a letter after he removes and re-adds the site, the fix is a public bypass for `/icons/*` in his dashboard, which is his to set. Not checked, and it needs his own devices: the live tab icon and the iPhone tile. | Pass | high | `3144e53` | `npx wrangler rollback` to version `3d067282-f170-4611-8786-487a54029493` |
