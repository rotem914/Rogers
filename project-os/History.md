# Rogers — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate-history.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate-history.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-06 | design | **Rotem's own two edits: the open composer and the back arrow.** The expanded composer lost its hairline for the card colour, and shows an accent ring while a picture is dragged onto it. The back arrow is a full circle. |
| 2026-09-06 | design | **The page ground is #111217, everywhere the app paints a ground.** The token, both copies in the page shell, the installed app's splash and the offline page all moved together, so the first frame and the launch screen are the same dark as the app. |
| 2026-09-06 | design | **Every card is #21222C, and hovers at #252632.** One token pair carries the project tile, the note row, the composer open and closed, and the open tab, so they all moved together. |
| 2026-09-06 | design | **Home's new-project tile draws the same plus the tabs use, inside a solid 2px edge.** The typed plus character is gone, so one drawn shape now means add in both places. Hovering it fills the tile instead of lighting its edge, over the same 144ms the other cards use. |
| 2026-09-06 | deploy | **The logo and the taller bar are live.** Version `84d57745`, no schema involved. |
| 2026-09-06 | done | **The logo really does render on the live site.** Rotem checked it signed in, which is the one thing a shell with no session could not answer. |
| 2026-09-06 | design | **The bar sits 56px from the top of the window instead of 32.** Every screen moved together, so the content below each one starts 24px lower. |
| 2026-09-06 | design | **Home's title is Rotem's logo now, drawn, not typed.** The mark and the wordmark take the bar's own text colour, and the bar kept its height, so nothing below it moved. |
| 2026-09-06 | deploy | **The colour pass and the new-project tile are live.** The darker ground, the card colours, the splash and offline page, and the tile with its drawn plus, all in one deploy, version `6d12171d`. |
| 2026-09-06 | deploy | **Tabs are live at rogers.rotem-e.com.** The live database got the tabs table and the note column first, so the new list query had something to read, then the deploy, version `e1ac5d1f`. The login still fronts every address, the new tabs route included. |
| 2026-09-06 | design | **The back arrow is a 32px icon in a fully round hover.** Only the top bar arrow changed: the glyph went from 24px to 32px inside the same 48px hit area, and its hover shape went from the 13px card radius to a full circle. |
| 2026-09-06 | design | **The open composer sits on the same card as the collapsed one.** It takes `bg-card` instead of `bg-surface` and has no border at all; the picture drag now shows a 2px accent ring instead of an accent border. |
| 2026-09-07 | app | **Going back to a list is instant.** Home, a project and its tabs show the list they last showed the moment they open, then quietly refresh it, so returning from a note no longer shows "Take a note" alone and the notes a beat later. The note editor is deliberately not remembered: it always opens from the server copy. On a first visit Main's notes are fetched beside the tabs instead of after them. |
| 2026-09-07 | app | **A project opens with its list already there on the first visit too.** Hovering or focusing a tile on Home fetches that project's tabs and Main's notes ahead of the click, once, so the project page paints in one piece even when nothing was remembered yet. |
| 2026-09-07 | app | **A note row shows a small copy of each picture instead of the original.** When a picture is added, the browser draws a WebP copy no taller than 480px and stores it beside the original; the row and the composer ask for that copy and get the original where none exists, so pictures added before today still show, at their old cost. The note page and the lightbox always show the original. |
| 2026-09-07 | app | **The remembered lists survive a reload.** Home, a project and its tabs keep their last lists in the tab's session storage, so a reload, or the trip through the login when the session expires, paints the lists at once and refreshes them behind. Session storage on purpose: it dies with the tab, so a preview of a private note never outlives the window. The note itself is still never stored. |
| 2026-09-07 | app | **The first switch to a tab is instant.** Opening a project fetches the lists of its other tabs, and Main's when a tab is open, right after the tabs are known, so switching paints from memory and refreshes behind. Each list is asked for once per page; the switch itself keeps it fresh. |
| 2026-09-07 | app | **The remembered lists now live in the browser's long-term memory.** A new tab, a restarted browser and a reload all paint the lists at once. Rotem's call, knowing the cost: titles and previews stay on the machine until Rogers overwrites them, logged out or not. |
| 2026-09-07 | app | **Old pictures get their small copies by themselves.** A few seconds after Rogers opens, the browser asks which pictures have no copy, makes each one and stores it, one at a time; once all have one, the check costs one request. A picture standing in for a missing copy is no longer kept by the browser for a year, so the copy takes over as soon as it exists. |
| 2026-09-07 | deploy | **Instant navigation and small picture copies are live at rogers.rotem-e.com.** The commit `0111a26` deployed as version `442c6ceb`; the backfill of old pictures runs the first time Rotem opens the live app, a few seconds in. |
| 2026-09-07 | design | **The logo is Rotem's new two-colour mark.** The disc is brand blue behind the light rings and the wordmark, which still take the bar's own text colour. |
| 2026-09-07 | design | **The browser tab and the installed app carry the mark.** The tab draws the mark itself at whatever size it needs, and the installed app icon replaces the yellow ring left over from setup. |
| 2026-09-07 | deploy | **The new mark is live at rogers.rotem-e.com.** The logo renders in Rotem's own signed-in browser, and the tab icon is served from the same address. |
| 2026-09-07 | design | **The mark is Rotem's redrawn version.** One ring instead of two, a rounded triangle with a diagonal cut through it, and the same artwork now serves as the tab and app icon. |
| 2026-09-07 | deploy | **The redrawn mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser; the installed Windows app still needs a reinstall to take the new icon. |
| 2026-09-07 | design | **The mark got a thicker ring and a lower triangle.** Rotem's next pass on the same artwork, in the logo and in the tab and app icons together. |
| 2026-09-07 | deploy | **The third pass of the mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser. |
| 2026-09-08 | design | **The mark's weights were tuned again.** A thinner ring, a slightly wider disc and a heavier triangle, in the logo and in the tab and app icons together. |
| 2026-09-08 | deploy | **The tuned mark is live at rogers.rotem-e.com.** Seen rendering in Rotem's own signed-in browser. |
| 2026-09-08 | ui | **A tab chip is clickable across its whole box.** Clicking anywhere on a tab, its padding included, opens it; before, only the letters themselves answered. |
| 2026-09-08 | deploy | **The clickable tab chip is live at rogers.rotem-e.com.** Rotem confirmed it in his own running app before the deploy; the live copy itself still answers the login to anyone without a session. |
| 2026-09-08 | app | **A project list comes back where you left it, but only from a note.** Scrolling well down, opening a note and pressing back now lands on the same row instead of at the top, and every tab keeps its own place. Opening the project from Home starts at the top, Rotem's call; so does a reload. |
| 2026-09-08 | deploy | **The remembered scroll place is live at rogers.rotem-e.com.** Coming back from a note lands on the same row; opening a project from Home still starts at the top. |
| 2026-09-08 | ui | **A pasted picture keeps its own size on the note page.** A small image now sits at its real pixel size at the top left of the note instead of being stretched across the column; anything wider than the column still shrinks to fit. |
| 2026-09-08 | ui | **The tabs sit on their own block of the page's colour.** Scrolling a project's list no longer sends note cards through the tab names; they pass behind a padded background instead. |
| 2026-09-08 | ui | **The tab background lost its side padding.** The tabs are back on the notes' own edge, and the block starts 16px above them instead of 24. |
| 2026-09-08 | ui | **The tab block sits tighter.** 12px of air above and below the tabs instead of 16 and 24. |
| 2026-09-08 | ui | **The tabs now stop at the very top of the window.** Scrolling a project pins the tab block to the top edge instead of leaving a see-through band above it, and the page at rest looks exactly as it did. |
| 2026-09-08 | ui | **The title is back where it sits, and the bar itself is opaque.** Correcting the row above: pinning the block to the window's edge dragged the project title up with it, which Rotem did not ask for, so the bar is pinned where it always was and its own box now hides what scrolls behind it. |
| 2026-09-08 | ui | **The tabs pin to the top on their own, and the title holds its place.** The strip left the bar and became its own sticky box, so scrolling sends the tabs to the window's edge while the project name and the back arrow stay exactly where they sit. On a narrow window the two pin one under the other, title first. |
| 2026-09-08 | ui | **A note's pin and menu moved onto its right-click.** The star and the three dots are gone from every row on a project page; right-clicking a note opens Pin and Archive where the cursor is. |
| 2026-09-08 | app | **Tabs can be dragged into the order you want.** A tab chip dragged along the strip rearranges it under the mouse, and the order is saved for the project, so it survives a reload and follows every screen. Main stays first: it has no row of its own, so it has nowhere to keep a place. |
| 2026-09-08 | ui | **A note row's text runs the full width of its card.** The 64px gutter the pin and the menu used to sit in is gone, so a title now stops at the same edge as the composer above it. |
| 2026-09-08 | ui | **The dead band under the tabs is gone.** The title's bar no longer paints or catches clicks over the notes column on a wide window, so the cards run right up under the pinned tabs and a card in that band opens on a click. |
| 2026-09-08 | ui | **A narrow window loses half its head room.** The bar starts 24px from the top instead of 56px on anything under 900px wide, and the tab strip moved up with it so the two stay glued. A wide window is untouched. |
| 2026-09-08 | deploy | **The bar and the pictures are live at rogers.rotem-e.com.** The tabs pin to the top on their own, the project title holds its place, a narrow window has less head room, and a pasted picture keeps its own size. |
| 2026-09-08 | data | **The live database got the tab order column it was missing.** The deploy above shipped code that sorts tabs by a column the live database did not have yet, so every project with tabs would have failed there; the migration was applied straight after and the live database now reports nothing pending. |
| 2026-09-08 | design | **Rotem's newest mark, in the logo and every icon.** The wordmark and the mark are redrawn from his files, the favicon is his icon file byte for byte, and the iPhone home screen now has an icon of its own at the size iOS asks for, instead of the letter tile it was drawing. |
| 2026-09-08 | deploy | **The fifth mark is live at rogers.rotem-e.com.** The logo, the favicon and the app icons, plus the new iPhone tile at the size iOS asks for. |
| 2026-09-08 | design | **Two more pixels between the mark and the word.** The wordmark sits 2px further from the disc inside the logo itself, so every size of it keeps the same air. |
| 2026-09-08 | deploy | **The wider logo gap is live at rogers.rotem-e.com.** One component, the wordmark 2px further from the mark. |
| 2026-09-08 | ui | **A project tile's menu moved onto its right-click too.** The three dots are gone from Home; right-clicking a tile opens Rename, Colour and Archive where the cursor is, and the swatch row opens in the same place. |
| 2026-09-08 | ui | **A project tile's name runs the full width of its tile.** The 32px gutter the three dots used to sit in is gone, so a Hebrew name that wrapped to two lines now fits on one. |
| 2026-09-08 | ui | **A note's title wraps instead of ending in three dots.** A title too long for one line now runs onto as many lines as it needs, and the row grows to hold it. The text under it still stops at three lines. |
| 2026-09-08 | ui | **A picture in a note row opens by itself instead of opening the note.** Clicking one now shows it large over the page, with 96px of air around it; clicking the row anywhere else still opens the note. The same window on the note page and in the composer gained that air too. |
| 2026-09-08 | app | **A tab can be given a checklist, and every row in it draws a circle you can tick.** "Add checklist" is on the tab's own right-click menu, above Remove. The mark is the whole feature: it saves, it survives a reload and a reorder, and it does nothing else, it hides no row and changes no order. Main cannot have one, because it is not a row on the server. |
| 2026-09-08 | ui | **The picture window keeps 56px of air, not 96.** Rotem's second number, on the same window everywhere it opens from. |
| 2026-09-08 | ui | **Clicking the picture itself closes the picture window.** It looked like it already did, but a picture drags itself by default, so a press that moved a hair started a drag and swallowed the click. |
| 2026-09-08 | app | **The checklist item on a tab's menu now takes one away too.** It reads Add checklist on a tab that has none and Remove checklist on one that does. Taking it away leaves every mark on the notes untouched, so putting the checklist back finds them all where they were. |
| 2026-09-08 | data | **The live database can hold a checklist too.** Both copies now carry the two new columns, so the checkbox column works on the live app as well. |
| 2026-09-08 | deploy | **The checklist is live on rogers.rotem-e.com.** Add checklist and Remove checklist on a tab's right-click, the circles on its rows, and the marks that survive a reload. It carries the other session's picture work too, since a deploy ships the whole folder. The login still stands in front of every address. |
| 2026-09-09 | ui | **The back arrow stops moving when you step from a project into one of its notes.** On a note it now hangs in the same left gutter, at the same height, that the project bar already put it at, and the scrollbar's room is reserved on every screen, so a short page no longer sits 7.5px right of a long one. |
| 2026-09-09 | deploy | **The steady back arrow is live on rogers.rotem-e.com.** The arrow keeps its place from a project into a note, and every screen reserves the scrollbar's room. The login still stands in front of every address. |
| 2026-09-09 | ui | **The picture window closes on a click again, on the picture or on the dark around it.** Only Escape was closing it: the window is drawn inside the note row, which switches pointer events off for everything under its covering link, so every real click fell through the window instead of landing on it. |
| 2026-09-09 | ui | **A right-click on the open picture window no longer opens the note row's menu behind it.** The window keeps the browser's own menu instead, so a picture can still be saved from there; right-clicking the thumbnail in the row, with no window open, still opens Pin and Archive. |
| 2026-09-09 | deploy | **The picture window's clicks are live on rogers.rotem-e.com.** A click on the picture or on the dark around it closes the window again, and a right-click there leaves the row's menu alone. It carries another session's tab strip padding, 12px to 16px at the top, since a deploy ships the whole folder. The login still stands in front of every address. |
| 2026-09-09 | ui | **The tab strip sits 16px below the bar instead of 12px.** Only the space above the tabs changed; the space below them is still 12px. |
| 2026-09-09 | ui | **The tab strip's top space is 18px, not the 16 of the row above.** Rotem's second number in the same sitting; the space below the tabs is still 12px. |
| 2026-09-09 | ui | **The tab strip's top space settled at 20px.** Rotem dialled it 16, 18, 22 and back in one sitting; the two rows above are the numbers he passed through, this is the one that stands. The space below the tabs is still 12px. |
| 2026-09-09 | deploy | **The tab strip's 20px and the picture window's clicks are live on rogers.rotem-e.com.** The login still stands in front of every address. |
| 2026-09-09 | docs | **`Go backup` is wired: one command makes a verified ZIP snapshot of the whole project.** Pulled from Rotem's ProjectOS kit. The script zips everything including the git history, skips `node_modules/`, `dist/`, `.wrangler/` and the Tauri `target/`, never carries a real secret file, and either verifies every file back out of the archive or leaves no ZIP at all. |
| 2026-09-14 | design | **The installed app icon is the mark alone, edge to edge, with no dark square behind it.** Only the two app icons changed; the tab icon and the iPhone tile are untouched. |
| 2026-09-14 | deploy | **The edge to edge app icon is live on rogers.rotem-e.com.** The installed app takes it after a reinstall. The login still stands in front of every address. |
| 2026-09-16 | review | **A whole-repository code review, nothing fixed, eleven findings waiting for verdicts.** One blocking: the composer discards typed text on a click outside when the note could not be created. Three important, all on the save and upload paths. Seven nits. The document is `notes/2026-09-16-code-review.md`. |
| 2026-09-16 | app | **All eleven review findings fixed and checked in the browser.** The composer never throws away a draft it could not create: it stays open, red, and retries on the next close. A long note saves on blur and on the back arrow again, because keepalive is now only for a page that is really going away, and a reload mid-save parks what was typed. Leaving a note or closing the composer waits for a picture still uploading. Ctrl+Z with the composer open no longer undoes a tab too, a dragged order lets go when the list changes, the remembered lists are capped at 40, lint is green and part of the check, a note in an archived project answers 404, the thumbnail check runs at most once a day, and seven stale comments were corrected. |
| 2026-09-16 | deploy | **The eleven review fixes are live on rogers.rotem-e.com.** The composer that keeps its draft, the long-note save, the parked text on a reload, the wait for pictures, and the rest. No schema change. The login still stands in front of every address. |
| 2026-09-17 | design | **The page ground is #0D0E12, everywhere the app paints a ground.** The token, both copies in the page shell, the installed app's splash and the offline page moved together, so no frame and no page keeps the older dark. Cards, hovers and hairlines are untouched. |
| 2026-09-17 | deploy | **The #0D0E12 ground is live on rogers.rotem-e.com.** Four assets changed: the stylesheet, the page shell, the manifest and the offline page. No schema change. The login still stands in front of every address. |
| 2026-09-22 | design | **The page ground is #0C0C0E, everywhere the app paints a ground.** The token, both copies in the page shell, the installed app's splash and the offline page moved together, so no frame and no page keeps the older dark. Cards, hovers and hairlines are untouched. |
| 2026-09-22 | deploy | **The #0C0C0E ground is live on rogers.rotem-e.com.** Four assets changed: the stylesheet, the page shell, the manifest and the offline page. No schema change. The login still stands in front of every address. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate-history.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-08 | Deploy the checklist | No code change. `npm run deploy` on Rotem's "DEPLOY AND GO COMMIT"; version `66c8de5e-5601-4ec7-9a14-3162e095eee7`, 3 assets uploaded. It carries the tab checklist and its toggle, the note row's checkbox, the remembered-lists key at v2, and the other session's uncommitted picture work, because a deploy ships the whole folder rather than a commit. | The live migration in the row above ran first, and `npx wrangler d1 migrations list --remote` answered "No migrations to apply" before the deploy. `npm run check` green, exit 0, on this exact tree. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit that follows closes that. Not checked, and it needs his own signed-in browser: the live tab menu and a mark that survives a reload there. | Pass | high | `7dacfa7` | `npx wrangler rollback` to version `c84a12ae-8fa9-4df7-88f5-3c7a02fd2556` |
| 2026-09-09 | The back arrow's position on a note | `src/web/components/TopBar.tsx`: the bar without a middle column now hangs its arrow the way the bar with one does, a flex item of no width at the title's own 72px with the arrow absolutely pinned to its right edge, a negative margin cancelling the row's gap so the title still starts at 72px, and the row held at 63px only when there IS an arrow, so Home keeps its own height. Below 900px the arrow rejoins the row exactly as before. `src/web/index.css`: `html { scrollbar-gutter: stable }`, because the rest of the gap was a scrollbar, not a layout. | In his running app at 1550x1210, read from the DOM. Note `/n/97a35f8a` and project `/p/8f96078e` both answer arrow x 117.5, y 63.5; before the change the note answered x 177, y 56. Project not-found and Home both keep their title at x 177.5, and Home's bar is still 104px tall while a bar with an arrow is 119. At 800px the note's arrow is back at x 10, y 56 with the row 48px, unchanged. No `100vw` or `w-screen` anywhere in `src/web`, so the reserved gutter cannot overflow a page sideways. `npm run check` green, exit 0. Review: reach is Shared, `TopBar` has four consumers, Home passes no `backTo` and is untouched, Note and both of Project's loading and not-found bars take the new gutter position, and the CSS line reaches every screen; no finding introduced. Not checked: Safari below 18.2, which ignores `scrollbar-gutter` and simply keeps today's behavior. | Pass | medium | `b825fcc` | `git checkout b825fcc -- src/web/components/TopBar.tsx src/web/index.css` |
| 2026-09-09 | Deploy the steady back arrow | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `59f0c0a0-fee4-46c6-85d3-6aeaa755dca6`, 3 assets uploaded. It carries the gutter-hung arrow on the bar without a middle column and the reserved scrollbar gutter, and nothing else was waiting in the tree. | `npm run check` green, exit 0, on this exact tree, and the browser pass in the row above ran before it. After the deploy, from a shell holding no session, `/` and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. No migration was involved: this change touches only `TopBar.tsx` and `index.css`. This deployed the working tree; the commit that follows closes that. Not checked, and it needs his own signed-in browser: the live arrow on his iPhone, where iOS uses overlay scrollbars and the gutter line does nothing. | Pass | medium | `b825fcc` | `npx wrangler rollback` to version `66c8de5e-5601-4ec7-9a14-3162e095eee7` |
| 2026-09-09 | The picture window takes its own clicks back | `src/web/components/ImageStrip.tsx`: the lightbox's own box gains `pointer-events-auto`, one class token, plus the comment that says why. Nothing else moved. The row's wrapper is `pointer-events-none` so the covering link keeps taking the row's clicks, and that property is inherited, so the modal rendered inside it could not be clicked at all; the thumbnail button already carried the same guard for the same reason. `project-os/QA.md` section 10g item 9 now carries both causes and the rule that the three closes are driven with REAL clicks. `project-os/BugAtlas.md` row 6 records the class. | In his running app at 1400 wide, project `9c6fc7b5`, on the note holding two pictures. Every click below was a real click at coordinates, never dispatched, which is the whole point. Row picture clicked: the window opened, read back `pointer-events: auto` on the box and on the picture, `padding: 56px`, `draggable="false"`, and `elementFromPoint` at the centre returned the IMG. A click on the picture closed it; reopened, a click on the dark background closed it; reopened, Escape closed it. All three left the address on `/p/9c6fc7b5...`, so no note was opened behind it. Note page, the same one component at `lg`: the window opened and a real click on the picture closed it, address still `/n/9a2cee1b...`. Before the fix the same real clicks did nothing, which is how the 2026-09-08 fix passed on dispatched clicks and shipped broken. `npm run check` green, exit 0. Console: known existing error only, the dev pane's service worker registration failure. Review per `project-os/Code_review.md` over this diff: no findings of my own. Reach is Shared and traced: one lightbox behind all three strips; the row and the note page were both driven, and the composer's strip sits in no `pointer-events-none` wrapper, so `auto` is what it already computed to and its behaviour is unchanged. Pre-existing, reported not fixed: a right-click on the open window opens the row's own Pin and Archive menu behind it at z-20, and that menu needs one further click outside to go, since the window counts as inside the row. Not run, and not claimed: the narrow-width pass, since no geometry changed. | Pass | medium | `32aef12` | `git checkout 32aef12 -- src/web/components/ImageStrip.tsx` |
| 2026-09-09 | The picture window keeps its own right-click | `src/web/components/ImageStrip.tsx`: the lightbox's box gains `onContextMenu={(event) => event.stopPropagation()}`, plus the comment that says why. Stopped and not prevented on purpose, so the browser's own menu still offers Save image as; the same bargain `ProjectTile.tsx` already makes while a name is being typed. The event was bubbling out of the modal into the row, which is its DOM parent, and `NoteRow`'s handler opened Pin and Archive underneath. Nothing in `NoteRow.tsx` was touched. `project-os/QA.md` section 10g gains item 10 for this. | In his running app at 1400 wide, project `9c6fc7b5`, real clicks throughout. With the window open, a right-click on the picture: the row's menu count stayed 0 and the window stayed open, where before this it opened Pin and Archive behind it. A capturing listener read the same event back as `defaultPrevented: false` on target IMG, which is what proves the browser's own menu is still offered rather than swallowed; the bubble-phase listener on `window` saw nothing, which is the stopPropagation itself. Right-click on the thumbnail with no window open: Pin and Archive, unchanged. All three closes re-driven after the change and still close, picture, dark background and Escape, each leaving the address on `/p/9c6fc7b5...`. The note page, the same component at `lg`: right-click left our menus at 0, and a real click on the picture still closed the window with the address still on `/n/9a2cee1b...`. `npm run check` green, exit 0. Console: known existing error only, the dev pane's service worker registration failure. The two test listeners were added from the console, never to the source, and a reload cleared them, read back as undefined. Review per `project-os/Code_review.md` over this diff: no findings. Reach is Shared and traced: one lightbox behind all three strips; the row and the note page were driven, and the composer's strip has no ancestor context-menu handler at all, so stopping there stops nothing. | Pass | medium | `32aef12` | `git checkout 32aef12 -- src/web/components/ImageStrip.tsx`, which also takes back the pointer-events fix in the row above |
| 2026-09-09 | The tab strip's top padding | `src/web/components/TabStrip.tsx`: the strip's wrapper goes from `py-3` to `pt-4 pb-3`, Rotem's 12 to 16 on the top only. One class token split in two, nothing else moved. | In his running app, project `8f96078e`, read from the DOM on the strip's own wrapper: `padding-top: 16px`, `padding-bottom: 12px`, class `bg-bg pt-4 pb-3`, and the wrapper is 28px taller than the tab row it holds, which is 16 plus 12. The tabs Main and דד still draw and the page below them is unmoved. `npm run check` green, exit 0. Console: the known dev-pane service worker failure only, plus 404s from my own first visit to a project id that exists live but not locally. Reach is Local: `TabStrip` has one consumer, `Project.tsx`. | Pass | low | `32aef12` | `git checkout 32aef12 -- src/web/components/TabStrip.tsx`, which also takes back the checklist toggle, since that file carries other uncommitted work |
| 2026-09-09 | The tab strip's top padding, second number | `src/web/components/TabStrip.tsx`: `pt-4` becomes `pt-4.5`, Rotem's 18, over the row above's 16. `pb-3` untouched. One class token, nothing else moved. | The real risk here was the class itself, since 18 is off Tailwind's 4px step: the built stylesheet carries `.pt-4\.5{padding-top:calc(var(--spacing) * 4.5)}`, which is 18px. In his running app, project `8f96078e`, read from the DOM on the strip's wrapper: `padding-top: 18px`, `padding-bottom: 12px`, class `bg-bg pt-4.5 pb-3`, and the wrapper is 30px taller than the tab row it holds, which is 18 plus 12. The tabs Main and דד still draw. `npm run check` green, exit 0. Reach is Local: `TabStrip` has one consumer, `Project.tsx`. | Pass | low | `32aef12` | `git checkout 32aef12 -- src/web/components/TabStrip.tsx`, which also takes back the checklist toggle, since that file carries other uncommitted work |
| 2026-09-09 | Deploy the picture window's clicks | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `d01c2ccb-b225-4331-afb3-87cfc54708c9`, 3 assets uploaded. It carries the two lightbox fixes above and one change that is not mine and was flagged to him before this ran: `src/web/components/TabStrip.tsx` went from `py-3` to `pt-4 pb-3`, another session's or his own, appearing in the tree mid-task. | `npx wrangler d1 migrations list rogers-db --remote` answered "No migrations to apply" before the deploy, and nothing in this tree changes the schema anyway. `npm run check` green, exit 0, on this exact tree, run again after the TabStrip change appeared. After the deploy, from a shell holding no session, the root and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. This deployed the working tree; the commit that follows closes that. Not checked, and it needs his own signed-in browser: the live window closing on a real click there. | Pass | high | `32aef12` | `npx wrangler rollback` to version `59f0c0a0-fee4-46c6-85d3-6aeaa755dca6` |
| 2026-09-09 | The tab strip's top padding, settled at 20 | `src/web/components/TabStrip.tsx`: the wrapper reads `bg-bg pt-5 pb-3`. This supersedes the 16 and the 18 in the two rows above, which never left the working tree; only the last number was ever committed or seen anywhere but his running app. `pb-3` untouched throughout. | Every number was read back from the DOM in his running app, project `8f96078e`, on the strip's own wrapper, and `npm run check` was green at each: 16, 18, 22, then 20. The one that stands reads `padding-top: 20px`, `padding-bottom: 12px`, class `bg-bg pt-5 pb-3`, wrapper 32px taller than the tab row it holds, which is 20 plus 12. The stylesheet check mattered for the off-step numbers only, 18 and 22, and both emitted; 20 is `.pt-5{padding-top:calc(var(--spacing) * 5)}`, on Tailwind's own 4px step. Reach is Local: `TabStrip` has one consumer, `Project.tsx`. | Pass | low | `32aef12` | `git checkout 32aef12 -- src/web/components/TabStrip.tsx`, which also takes back the checklist toggle, since that file carries other uncommitted work |
| 2026-09-09 | Deploy the tab padding and the picture window | No code change. `npm run deploy` on Rotem's "DEPLOY"; version `c68e1c07-a91e-42c2-92b6-aaa69d1f3be4`. It carries the tab strip's 20px top padding and the lightbox taking its own clicks back. | `npm run check` green, exit 0, on this exact tree, and both changes had their browser pass in the rows above. Wrangler reported "No updated asset files to upload", which is correct and not a failure: a parallel session had deployed the identical tree three minutes earlier as `d01c2ccb`, so the asset hashes already matched; `npx wrangler deployments list` shows mine as the current version. No migration was involved, `migrations/` still ends at `0007_checklist.sql` and that was applied live on 2026-09-08. After the deploy, from a shell holding no session, `/` and `/api/projects` both answered 302 to `roteme-pages.cloudflareaccess.com`. Not checked, and it needs his own signed-in browser: the live strip's spacing and a real click on a live picture. | Pass | medium | `ab2e06a` | `npx wrangler rollback` to version `59f0c0a0-fee4-46c6-85d3-6aeaa755dca6`, the last one before either change |
| 2026-09-09 | Pull `Go backup` in from the ProjectOS kit | Added `project-os/backup.ps1` from `github.com/rotem914/ProjectOS`, with `.wrangler` added to its exclude list for this stack. Added the `Go backup` and `Go commit and backup` shortcut sections to `CLAUDE.md`, between `Go commit` and `Backlog`. Added `backups/` to `.gitignore`. | Ran the script on this machine: `OK: J:\Projects\Rogers\backups\Rogers_2026-09-09_02-46.zip`, 156 files added and all 156 read back out of the finished archive. Read the ZIP's own entry list: 68 entries are `.git/*`, so the history travels, and zero entries match `.env` or `.dev.vars`. 1.67 MB. `git status` does not list `backups/`, so the ignore holds. Not exercised: the secret-file skip, because this project has no `.dev.vars` or `.env` on disk right now. | Pass | low | `af9267c` | Delete `project-os/backup.ps1`, revert the `CLAUDE.md` shortcut block, drop the `backups/` line from `.gitignore` |
| 2026-09-14 | The app icon loses its square | `public/icons/icon-192.png` and `icon-512.png` re-rendered with sharp from `public/icons/icon.svg`, which already fills its own box: disc r16.84 plus half its 2.32 ring is 18. Transparent ground, no padding; before, a `#17181a` square with the mark at 80%. `icon.svg`, `apple-touch-icon.png` and the manifest untouched. | Read back from the files: 512 and 192, 4 channels, corner alpha 0, alpha 252 to 255 at the middle of the left and top edges, so the ring touches the edge. The 512 opened as an image: the mark fills the box. `npm run check` green, exit 0, and the build carries both new files. Not checked: the local app was down, no answer on 5173, so the served copy was not fetched; the Windows shortcut itself needs a deploy and a reinstall. Named side effect: the manifest also offers `icon-512.png` as `maskable`, and an Android launcher will now crop the ring's edge. | Pass, the served copy and the shortcut unchecked | low | `a6eeac0` | `git checkout a6eeac0 -- public/icons/icon-192.png public/icons/icon-512.png` |
| 2026-09-14 | Deploy the edge to edge app icon | No code change. `npm run deploy` on Rotem's "DEPLOY AND GO COMMIT"; version `cbe2ea90-4428-46c9-b339-f068f77de965`. It carries only the two re-rendered app icons. | `npx wrangler d1 migrations list rogers-db --remote` answered "No migrations to apply". `npm run check` green, exit 0, on this tree in the row above. After the deploy, from a shell holding no session, `/`, `/icons/icon-512.png` and `/api/projects` all answered 302 to `roteme-pages.cloudflareaccess.com`. Not checked, it needs his machine: the Windows shortcut after a reinstall. | Pass | low | `a6eeac0` | `npx wrangler rollback` to the version before `cbe2ea90` |
| 2026-09-16 | Go code review, whole repository | No code change. `notes/2026-09-16-code-review.md`: eleven findings, one block each, at commit `c384ffd` on a clean tree. Scope was the whole repository, never a diff, one agent on Rotem's "one agent, don't spread": every file under `src/`, `migrations/`, `public/`, the page shell and the build config, read in full. Findings: T1 blocking, the composer resets on a click outside, Escape or Enter while the create request has failed, so typed text is discarded with no park; T2 to T4 important, keepalive on every flush caps a note at 64 KB, a pagehide during an in-flight save parks nothing, and leaving during an upload orphans the picture or makes a second note; T5 to T11 nits, a double undo, a stale dragged order, unbounded remembered lists, red lint, a note reachable inside an archived project, the bucket walk on every open, and seven drifted comments. All pre-existing, none fixed. | `npm run check` green, exit 0. `npm run lint` red, exit 1, one error and five warnings, recorded as T8. Each finding was read at its lines and traced to its consumers; the reach line in the document names Shared for the four save-path findings and Local for the rest. No browser pass: nothing changed on screen. | Pass, verdicts owed | low | `c384ffd` | `git rm notes/2026-09-16-code-review.md`; the two rows stay, a log is never edited |
| 2026-09-16 | Fix all eleven review findings | `src/web/lib/autosave.ts`: `flush(leaving)` sends keepalive only when asked, and only under 60 KiB of body; `park()` also parks the patch on the wire; a leaving flush that arrives mid-save marks the follow-up send keepalive. `src/web/pages/Note.tsx`: pagehide parks first, then flushes keepalive; uploads are kept in a set and `goBack` waits for them; the `undo` dependency lint asked for. `src/web/components/Composer.tsx`: `close()` waits for uploads, retries the create once when there is no saver and the draft has text, and stays open with "Could not save" if it still fails; the wait on an in-flight create is caught; an expiring login parks a draft with no note under `composer:<project>:<tab>` in session storage and the composer picks it up on the next mount of that list; the handle gains `isOpen()`. `src/web/pages/Project.tsx`: the tab undo stands aside while the composer is open; the note sections, `Home.tsx` and `TabStrip.tsx` let a dragged order go when the set of ids changes, during render, never on the drop. `src/web/lib/useApi.ts`: the remembered map keeps the newest 40 paths and forgets a path that answered 404; the one lint error carries an inline disable with its reason. `src/api/notes.ts`: GET, PATCH and DELETE on one note answer 404 when its project is archived; the unused re-export is gone. `src/web/lib/upload.ts`: an empty missing-thumbnails answer is remembered in local storage for a day. `package.json`: `check` runs eslint after tsc; `eslint.config.js` ignores the generated `worker-configuration.d.ts`. Comments corrected in `index.css`, `migrations/0006_tab_position.sql`, `src/api/projects.ts`; `project-os/Map.md` says what the check runs and who may PATCH a note; `CLAUDE.md` names lint in the check. `project-os/QA.md` gains 10j, `project-os/Code_review.md` its first always-check rows, `project-os/Decisions.md` the entry for the composer and keepalive. | `npm run check` green, exit 0, with eslint now inside it: 0 errors, 0 warnings. Review per `project-os/Code_review.md` over this diff: no finding of my own. Reach is Shared and traced: `NoteSaver.flush` has two callers, the note page and the composer, both changed to match; `ComposerHandle` has one consumer, `Project.tsx`, changed; `useApi` serves every screen and its answers are unchanged, only the cap and the 404 forget are new; the single-note routes serve the note page and the row's pin, mark and archive, none of which can reach a note in an archived project from the screen. Browser pass, once Rotem started the app, on a throwaway project made and archived at the end, never on one of his: with the create request made to fail from the page, text typed in the composer survived a click on the page, Escape and a second click, the status read Could not save, and once requests went through the next click made the note, in the list and on the Worker. With a tab added, the composer open and focus on its pin, Ctrl+Z emptied the title and the tab stayed, on screen and on the Worker. A picture dropped into the composer with its upload delayed 3 s, then a click outside: one note with one picture, no second note. On the note page a 68,472 byte body saved on blur as a plain request, 200, status Saved, the Worker holding all 68,472 characters. Typing then a dispatched pagehide: the session storage held the parked title before the flush, and the flush went keepalive with 200; typing, waiting 700 ms, typing more and pagehide again: the parked patch and the Worker both carried the newer title, the follow-up send keepalive. A picture dropped on the note page with a 3 s upload and the back arrow clicked 150 ms later: the page waited, Uploading showing through 2.7 s, then left at 3.6 s with both pictures on the note. Three rows in Main dragged the QA 10c way, then B pinned and unpinned from its own menu: the rows read B, T3, C, the Worker's order, where the stale dragged order would have read T3, C, B. A reload plus six seconds added no request for the missing thumbnails, the day stamp in place. Console: known existing error only, the dev pane's service worker registration. Two things the pane got wrong, not the app: a coordinate click at the arrow landed on a div because the pane's frame was 1024 wide while the screenshot claimed 800, a click by element reference reached it; and the composer's mid-upload state was not captured on the first try because the tool's own latency outran the 3 s delay, so the note-page run drove the click from the page itself. Not exercised: the mid-flight keepalive path on its own, since a local save answers before the 700 ms wait ends. Residual, by design: a note whose later save fails after the composer closed keeps its newer text parked under the note id, restored on the next open of that note in the same tab, as before. | Pass | high | `c384ffd` | `git checkout c384ffd -- src/ package.json eslint.config.js migrations/0006_tab_position.sql`, then the docs by hand |
| 2026-09-16 | Deploy the review fixes | No code change. `npm run deploy` on Rotem's "DEPLOY, GO COMMIT"; version `c27aec4d-0a32-40d0-8aa9-4e24a83b13e1`, 2 assets uploaded, the page shell and the app bundle. It carries the eleven fixes in the row above and nothing else was waiting in the tree. | `npx wrangler d1 migrations list rogers-db --remote` answered "No migrations to apply", and no migration is part of this change. `npm run check` green, exit 0, on this tree, lint inside it. After the deploy, from a shell holding no session, `/`, `/api/projects` and `/api/notes/x` all answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. Not checked, it needs his signed-in browser: the live composer under a failed create, which the local pass covered. | Pass | high | `c384ffd` | `npx wrangler rollback` to version `cbe2ea90-4428-46c9-b339-f068f77de965` |
| 2026-09-17 | Page ground to #0D0E12 | Rotem's ask, "the app background on every page". `src/web/styles/tokens.css`: `--color-bg` from `#111217` to `#0D0E12`. `index.html`: the `theme-color` meta and the inline first-paint background, the same move, which keeps the dark-first-frame invariant true. `public/manifest.webmanifest`: `background_color` and `theme_color`. `public/offline.html`: its meta and the `html` background. Seven lines, one hex each, nothing else. Left alone on purpose: the `--color-faint` comment's contrast numbers were measured on the old ground; the new one is darker, so the ratio only rises and the "do not darken" note still holds. | `npm run check` green, exit 0. The built output was read back: the stylesheet carries `--color-bg:#0d0e12`, the shell carries the new hex twice, and neither carries `#111217` any more. NOT driven locally: localhost:5173 refused the connection, the dev server was not running and is Rotem's to start (rule 16). Not driven live either: the browser pane holds no Access session and signing in is his, so the rendered ground on Home, a project and a note is Rotem's check in his own browser. | Pass, the rendered look unopened | low | `5777b6d` | `git checkout 5777b6d -- src/web/styles/tokens.css index.html public/manifest.webmanifest public/offline.html` |
| 2026-09-17 | Deploy the #0D0E12 ground | No code change. `npm run deploy` on Rotem's "DEPLOY, GO COMMIT"; version `9f3fc7fd-f6d9-4dee-8a3b-b6905cd84d3c`, 4 assets uploaded: the stylesheet, the page shell, the manifest and the offline page. It carries the row above and nothing else was waiting in the tree. This deployed the working tree, so the ground went live before it was committed, as on 2026-09-06. | `npm run check` green before the deploy, in the row above. The remote migrations list was NOT consulted this time: the change is four static files and no migration exists to apply. After the deploy, from a shell holding no session: the app root, `/api/projects`, the manifest and the offline page all answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. The browser pane also landed on the Access login, so the live render is Rotem's. | Pass, the live look is Rotem's | high | version `c27aec4d` | `npx wrangler rollback` to version `c27aec4d-0a32-40d0-8aa9-4e24a83b13e1` |
| 2026-09-22 | Page ground to #0C0C0E | Rotem's ask, "the app background on every page". `src/web/styles/tokens.css`: `--color-bg` from `#0D0E12` to `#0C0C0E`. `index.html`: the `theme-color` meta and the inline first-paint background, the same move, which keeps the dark-first-frame invariant true. `public/manifest.webmanifest`: `background_color` and `theme_color`. `public/offline.html`: its meta and the `html` background. Seven lines, one hex each, nothing else. The `--color-faint` contrast note was left alone: the new ground is darker, so the ratio only rises. | `npm run check` green, exit 0. The built output was read back: the stylesheet, the shell, the manifest and the offline page carry `#0C0C0E` seven times between them, and `#0D0E12` nowhere. NOT driven locally: localhost:5173 was answering with a different project, "DS Tiger", so Rogers' dev server was not the one running there, and starting one is Rotem's (rule 16). The tab opened for that look was closed. The rendered ground on Home, a project and a note is Rotem's check in his own browser. | Pass, the rendered look unopened | low | `8681fdc` | `git checkout 8681fdc -- src/web/styles/tokens.css index.html public/manifest.webmanifest public/offline.html` |
| 2026-09-22 | Deploy the #0C0C0E ground | No code change. `npm run deploy` on Rotem's "DEPLOY, GO COMMIT"; version `09de4eee-a151-4701-a577-1b11383ac9e3`, 4 assets uploaded: the stylesheet, the page shell, the manifest and the offline page. It carries the row above and nothing else was waiting in the tree. This deployed the working tree, so the ground went live before it was committed. | `npx wrangler d1 migrations list rogers-db --remote` answered "No migrations to apply". `npm run check` green before the deploy, in the row above. After the deploy, from a shell holding no session: the app root, `/api/projects`, the manifest and the offline page all answered 302 to `roteme-pages.cloudflareaccess.com`, so nothing became public. The live render is Rotem's, behind his login. | Pass, the live look is Rotem's | high | version `9f3fc7fd` | `npx wrangler rollback` to version `9f3fc7fd-f6d9-4dee-8a3b-b6905cd84d3c` |
